# doc/ 文档索引

> **本文件是 `doc/` 的入口索引**——先读本文件，再决定打开哪一篇。新增、移动或归档文档后请同步更新本文件。
>
> **权威性声明**：本目录下任何文档都**不构成技术事实的权威源**。技术事实（架构、参数、行为、踩坑、流程规约）一律以 **`AGENTS.md` 与源码**为唯一权威源；本目录文档与二者冲突时，以 `AGENTS.md` 与源码为准，并请修正本目录文档。
>
> 表述规范（用词、术语、中英混排、语种分工）见 `doc_output_standards_guide.md`；测试台账见根目录 `TEST_RECORD.md`。

## 怎么用

1. **只打开状态为 🟢 / 🟡 / ⏳ 的文档**。⚠ / 📦 / ❌ 的文档仅在需要追溯历史时打开。
2. **大文档不要整篇读**——`auto_run_loop_production.md`（45KB）、`ui_interlock_plan.md`（29KB）按标题定位到具体小节即可。
3. 查「某项功能现在怎么实现」→ `architecture.md` + 源码；查「某项改动是否已验收」→ `TEST_RECORD.md`。

### 状态图例

| 标记 | 含义 |
| --- | --- |
| 🟢 现行 | 描述当前生效的规范或已验收的实现，可直接引用 |
| 🟡 已实施待真机 | 代码已完成并双编译通过，真机用例尚未执行 |
| ⏳ 计划中 | 尚未实施，仅方案 |
| ⚠ 部分失效 | 仍有有效内容，但部分段落已被后续演进取代（正文内多有注记） |
| ❌ 过时 | 内容与现行事实冲突，**勿引用** |
| 📦 归档 | 已移入 `doc/archive/`，仅供追溯推导过程 |

## 目录约定

| 位置 | 放什么 | 什么时候动 |
| --- | --- | --- |
| `doc/*.md` | 现行生效的规范、方案与说明 | 方案实施后按状态标记保留，结论沉淀后归档 |
| `doc/test/` | 真机联调计划与测试用例 | 按阶段累加 |
| `doc/worklog/` | 每日工作日志（人工精读） | 每日新增，**不改历史** |
| `doc/archive/` | 已归档的原始档案 | 见下方「归档规则」 |
| `doc/robot-pictures/` | 机械臂实物照片（标定、限位参考） | 需要时新增 |

## 一、规范与说明（常读）

| 文档 | 职责 | 状态 | 备注 |
| :--- | :--- | :--- | :--- |
| `architecture.md` | 系统架构导航图：目录职责、分层依赖、HAL 多品牌体系、三条数据流、关键不变量 | 🟢 现行 | 事实以源码为准，本文是导航图 |
| `config.md` | `config/config.json` 配置项 schema 说明 | 🟢 现行 | 参数**现行值**一律以该 json 为准，不以本文示例为准 |
| `process.md` | `config/process.json` 工艺方案与动作数据 schema | 🟢 现行 | 同上 |
| `compile_guide.md` | 编译方案：沙箱手工环境构造、开发机一键脚本、LNK1168 与 windeployqt 处理 | 🟢 现行 | **编译问题的唯一权威参考**，勿凭印象重推工具链路径 |
| `doc_output_standards_guide.md` | AI 输出与文档表述规范：核心术语表、机翻黑名单、语种分工、多义陷阱表 | 🟢 现行 | **写文档 / 代码 / review 前必读**（`AGENTS.md` §2 强制） |

## 二、方案文档（按状态排列）

| 文档 | 职责 | 状态 | 台账 / 备注 |
| :--- | :--- | :--- | :--- |
| `auto_run_button_refactor.md` | 自动运行页 5 按钮 + `WorkerState` 显式状态机重构 | 🟢 已实施 | TR-073。**按钮组与接线定义以本文为准** |
| `ui_interlock_plan.md` | UI 权限与状态机互锁：模式门禁、导航白名单、视觉页只读、急停锁存 | 🟢 已实施 | TR-075。§5.3/5.4/5.5 为评审过程存档 |
| `global_safe_position.md` | 全局安全位：回零后自动 + 手动按钮 + 示教安全位 | 🟢 已实施 | TR-074 / 079 / 080 / 083 / 088 |
| `kinematics_sequenceworker_plan.md` | 「小脑」运动学与 TCP 核心库重构 + 「大脑」引擎执行计划 | 🟢 已实施 | TR-013 ~ TR-018。文首有 2026-09-14 历史注记 |
| `kinematics_sequenceworker_test.md` | 上述重构的配套测试方案 | 🟢 已实施 | 配套 `kinematics_sequenceworker_plan.md` |
| `refactor_plan.md` | HAL 目录重组 + HardwareManager 拆分 | 🟢 已实施 | 现行 `src/HAL/` 六子目录结构即其结果 |
| `auto_run_loop_production.md` | 循环生产（单轮 / 指定次数 / 无限循环 + 回安全位）需求方案 | 🟡 已实施待真机 | TR-091。用例 8.18–8.26、10.21–10.26 需真机执行 |
| `physical_buttons_plan.md` | 物理蘑菇头按钮接入（初始化 / 启动 / 停止 / 复位 / 急停） | ⏳ 计划中 | 待现场核对手册端子号与极性后实施 |
| `ui_wiring_plan.md` | 阶段 2 轮 B UI 接线（T7–T10） | ⚠ 部分失效 | T8 按钮映射已被 5 按钮重构取代；**T7 / T9 / T10 及 D 修复清单仍有效** |
| `开发文档.md` | 初版页面功能需求说明（顶栏 + 各页面功能） | ⚠ 部分失效 | 成稿于 4 按钮时代，按钮组与互锁语义已演进，需求背景仍可参考 |

## 三、子目录

| 位置 | 职责 | 状态 | 备注 |
| :--- | :--- | :--- | :--- |
| `doc/test/` | 真机联调计划与测试用例（5 篇） | 🟢 现行 | 阶段 2 / 阶段 3 计划、卡轴手动测试、回零调试报告、8 阶段手动测试流程 |
| `doc/worklog/` | 每日工作日志（18 篇，2026-08-13 起） | 🟢 现行 | 真机数据、问题与结论、次日计划 |
| `doc/archive/` | 已归档原始档案 | 📦 归档 | 见下方「归档规则」 |
| `doc/robot-pictures/` | 机械臂实物照片（Z 轴限位、h1 标定、夹爪位置） | 🟢 现行 | 标定与限位核对的图像参考 |

### 归档内容

| 文档 | 内容 | 归档理由 |
| :--- | :--- | :--- |
| `archive/gemini_qr.md` | 运动学建模早期的多轮 AI 对话**原始档案**（2800+ 行，含提问原文、推导过程、Python 验算脚本、图片引用） | 结论已沉淀进 `AGENTS.md`、`architecture.md`、`kinematics_sequenceworker_plan.md` §一 与源码注释。**保留价值仅在于追溯「为什么这么建模」**。⚠ 内含已被推翻的 L1 = 174.35mm，**勿引用其参数值** |

### 非 Markdown 资产

| 文件 | 职责 |
| :--- | :--- |
| `开发工作计划.xlsx` | 需求与工时计划表（人工维护，非 Agent 文档） |
| `开发工作计划.backup.xlsx` | 上表的历史备份 |

## 归档规则

**触发条件**：文档对应的功能已实施并验收，且其结论已沉淀进 `AGENTS.md`、`architecture.md` 或源码注释。

**操作步骤**：

1. **先沉淀**：把仍然生效的决策与踩坑挪进权威源（`AGENTS.md` 或对应现行文档），归档后历史文档不再需要被翻。
2. **整体移入** `doc/archive/`，**文件名不变**——只靠目录位置表达状态，不靠改名。
3. **加归档横幅**：在文件顶部写明「不要把它当权威源」+「结论已沉淀处」+「保留理由」。
4. **修引用**：全库 `grep` 该文件名，把路径改到新位置——**包括源码注释**（如 `src/Core/Kinematics.h`）。
5. **同步本文件**：更新「归档内容」表，并从上方方案表中移除。

## 维护约定

- 新增文档请在此登记，并如实填写状态；状态变化（如 🟡 → 🟢）时回来改。
- 发现文档内容与 `AGENTS.md` 或源码冲突时，**以 `AGENTS.md` 与源码为准**，按三级处理：
  1. **仍有有效内容** → 修正后保留；
  2. **仅剩历史价值** → 按「归档规则」移入 `doc/archive/`；
  3. **内容已被权威源完全覆盖、且存在错误值** → **直接删除**（先例：`机械臂参数.md` 于 2026-09-14 删除——其 L1 值错误，其余参数均已在 `config/config.json`，无信息损失）。

  不要留着让下一个 Agent 踩。
