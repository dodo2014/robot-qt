# 工作日志 2026-09-04

## 阶段 5 自动运行真机验证（5.6-5.15）落盘

用户真机跑完阶段 5 自动运行全部用例，逐项结果（均 🟢 通过）：

| 用例 | 结果 | 备注 |
|---|---|---|
| 5.6 Move 整跑 | ✅ | 逐点 InverseSmart→MoveAbs(J2→R→J1→Z) 无大甩臂 |
| 5.7 Gripper 开/合 | ✅ | 按 GetLimitMax/Min 到位 |
| 5.8 Delay 1s | ✅ | 不卡 UI |
| 5.9 Vision | ✅（降级）| **真实相机未接，走无相机模拟延时路径**；接相机后需复测 |
| 5.10 schemeFinished | ✅ | 状态「✅ 完成」、启动按钮恢复 |
| 5.11 AutoRunPage 停止 | ✅ | 自动流程中测：当前动作安全停止 + interrupted + 保持使能；**跨页互斥仍待完善**（工艺流程单步调试中不允许操作 AutoRunPage 按钮，逻辑后续补，TR-066）|
| 5.12 运行中顶栏急停 | ✅ | 断使能 + worker 中断 + 自动页复位，需重新使能（收口 TR-040）|
| 5.13 未使能启动 | ✅ | 门禁拒绝 |
| 5.14 运行中改运动学参数 | ✅ | ReloadFromConfig 被 running 门禁跳过，下次启动生效 |
| 5.15 运行中关窗口 | ✅ | 进程不挂死、10s 内退出（ShutdownWorker）|

## 历史记录收口

- **TR-041**（ProcessPage 单步执行 UI 接线）：状态 🟡→🟢，用户 09-04 确认 5.1-5.5 用例已覆盖（实际 09-03 真机通过）。
- **TR-040**（全局急停升舱）：5.12 自动运行中急停由 ⏳ 未测→✅ 已测，状态收口为「5.4 + 5.12 两项均已真机验证」。
- 新增 **TR-069**：阶段 5 自动运行真机验证汇总（5.6-5.15，2026-09-04）。

## 文档同步

- `TEST_RECORD.md`：TR-041/TR-040 收口 + 追加 TR-069。
- `doc/test/real_machine_plan_phase2.md`：5.2 补 🟢；5.6-5.15 逐行标 🟢；5.9/5.11 补注（引用 TR-069/TR-066）。
- `AGENTS.md`：里程碑补「5.6-5.15 自动运行亦全通过」；下一步改为「阶段 5 全通过，待阶段 6-7 + 三页互斥 + 接相机后 5.9 复测」。

## 引用约定（台账编号化后）

`TEST_RECORD.md` 首列为唯一编号 `TR-###`（TR-001…TR-069，按追加递增，禁止重排/复用）。台账内部及跨文件引用一律用 `TR-###`，不得用行号（行号随增删漂移）。追加新记录取「当前最大号 + 1」（下一条为 TR-070）。

## 待办

1. 阶段 6：AutoRunPage UI 接线真机（6.1-6.6）。
2. 阶段 7：异常与回归（7.1-7.4）。
3. 三页操作互斥（手动/工艺流程/自动运行）逻辑补齐（TR-066）。
4. 真实相机（Orbbec）SDK 接入 `ICamera` 后，5.9 Vision 用例复测。
5. 本轮纯文档改动，未编译、未冒烟、未 commit。

## 舵机未上电误报「已连接」修复（TR-070，2026-09-04）

**现象**：舵机仅插 USB 未接电机电源，启动软件 UI 显示「舵机: 已连接」。用户日志实证另一 bug：上电启动时 `WriteUserData id=1` 两次、`Servo id 1 online` 两次，id=0 从未被探测。

**根因三层**：

1. `XRServo::Connect` 串口打开成功即返回 true，Ping 失败仅打 WARN；
2. `XRServo::IsConnected()` 语义 = 串口句柄有效，真实在线状态在 `IsOnline()` 但未接入"已连接"判定；
3. `HardwareManager::IsServoConnected()` 只查串口级 → UI 标签、EnableAll 门禁、PollTick 边沿检测全基于假"已连接"。

**探测 id 时序 bug**：`Initialize`/`ReconnectServos` 先 `Connect` 后 `SetServoId`，而 Connect 内部用默认 id=1 做 Ping/WriteUserData(addr=44)——J2(id=0) 的在线标志实际靠 ping id=1 判定。

**修复**（单文件 `HardwareManager.cpp` 三处）：

- `IsServoConnected()` 收紧为「串口打开 且 所有已创建舵机 `IsOnline()`」（SimServo 的 IsOnline==connected_，仿真不受影响）；
- `Initialize`/`ReconnectServos` 中 `SetServoId`/`SetSpeed` 移到 `Connect` 之前。

**验证**：Debug + Release 编译通过；Sim 冒烟 SMOKE_INIT_COMPLETE=True（日志时序证实 SetServoId 先于 Connect）；真机验证通过（2026-09-04 用户确认）：未上电显示"未连接"、上电 ~2s 自动恢复"已连接"、使能门禁正确拒绝。

**已知开口**：软件开着时上电、经「Ping ok skip reconnect」路径恢复的不重写 addr=44 accel 参数（舵机用户数据预期掉电保持）；真机点动若顿挫回归，再在恢复边沿补写。
