# 2026-08-31 — 单动作独立执行功能落地 + 冒烟/历史压缩规范建立

## 单动作独立执行（RunSingleAction，阶段 5 前置功能）

- **需求来源**：8-28 用户提出「能否选中流程动作列表中的某个动作单独执行」（当时 OpenCode 会话生成实施计划 `quantum-vortex-newton.md`）。
- **计划评估**（上午）：对计划逐条对照源码核验（SequenceWorker.h/.cpp、ProcessPage.h/.cpp、ProcessManager.h、MainWindow.cpp）——8 处"必须修正项"全部属实（m_actionList 非 axisList_、ReloadFromConfig 前置、拒绝路径无信号、m_stepActive 冲突、InMainThread 包裹、缺信号/槽、cancel 区分 interrupted/errorOccurred、atomic .store()）；另发现 1 处计划硬伤：ProcessPage.h 缺 `class QPushButton;` 前置声明（不加会编译失败）。
- **实施**（用户批准后）：
  - SequenceWorker.h/.cpp：+`RunSingleAction(scheme, index)`（镜像 RunSequence：running 门禁 → InMainThread 使能门禁 → 深拷贝 scheme → QueuedConnection 派发）、+`StartSingleExecution(index)`（镜像 ExecuteActions：cancel 区分 interrupted/errorOccurred，含 index 越界防御）、+`singleActionFinished(int)` 信号。失败双发 errorOccurred 为既有兜底语义（覆盖 MoveAbs 静默失败），保留并注释。
  - ProcessPage.h/.cpp：+`class QPushButton;` 前置声明、`OnRunSelectedAction` 槽、`m_runSelectedBtn` 成员；`OnRunSelectedAction`（未选动作弹窗 / 先 ResetStepSession 清单步会话 / ReloadFromConfig / 拒绝路径立即恢复按钮）；SetSequenceWorker 追加 singleActionFinished/interrupted/errorOccurred 三条按钮恢复 connect。
  - UI 位置（用户 11:33 要求调整）：按钮从顶栏挪到**动作列表按钮行「下移」之后**，绿色（#2f8f5f）40×32 仅图标（`makeActionIcon(5)` 白色实心播放三角）+ tooltip「执行选中动作」。
- **验证**：Debug+Release 编译通过无警告（用户 10:58 PowerShell 验证；挪位后 11:34 助手复编）；Sim 冒烟 8s 存活、Sim 全家桶初始化正常；TEST_RECORD 记 🟢。**GUI 点击交互逻辑待真机验证**（含与单步执行互斥、停止→interrupted 按钮恢复）。

## Sim 冒烟规范定稿（C 盘 2.1GB 事故驱动）

- **事故**：第一版冒烟脚本拷贝整个 Debug 输出目录到 `C:\Users\11318\.workbuddy\tmp\sim_smoke_*`，生成 **2.1GB** 副本；且开发机版程序路径硬编码 `PROJECT_SOURCE_DIR`（读工程根 config/config.json 与 log/），**副本 config 根本不生效**——纯浪费，已全部清理。
- **定稿规范**（已入 AGENTS.md Continuous QA 第 5 条 + WorkBuddy 记忆）：Sim 冒烟 = 就地临时改工程根 config.json（备份→改 SimCard/SimServo→启动 8s→try/finally 恢复）；标准脚本 **`out\smoke\sim_smoke.ps1`**（已实际运行验证两次通过）；禁止任何 C 盘大目录。
- **编译 shell 踩坑**：Git Bash 调 `cmd //c` 因引号嵌套/MSYS2 路径转换坏掉 vcvars 路径（`'ommunity' 不是命令`）；正确姿势 = PowerShell 执行 `cmd /c "call ""...vcvars64.bat"" >nul && ninja CreamPuffRobot"`，或 PowerShell `&` 调用现成脚本 `out\smoke\build_debug.bat`（已建）。

## 项目历史压缩 WorkBuddy 版（与 OpenCode 插件互通）

- 读懂 OpenCode 插件 `project-history.js`（936 行：chat.message 采集 → 3h/100 条阈值 → 临时会话 LLM 压缩 → history.txt；文件锁/原子写/噪音过滤/内部会话过滤）。
- 建立 WorkBuddy 版：automation `project-history-compress`（每天 08:30，prompt 自包含压缩流程：锁→收集 marker 前条目→滤噪→整合 history.txt→顶部插 compacted marker→更新 state.json→解锁）。
- **端到端验证通过**：处理 marker 前 4 条积压（1 有效 = 本条单动作执行需求 → history.txt 新增节「单动作独立执行（RunSingleAction）」记录需求+实施结果；3 噪音 hi×2/"执行git commit" 过滤）；marker/state/锁全流程正常。
- 采集约定：WorkBuddy 会话中用户提出的长期有效提问/决策 → 助手 prepend 到 pending.ndjson。两套工具（OpenCode 插件 / WorkBuddy automation）同文件同格式互通。

## 文档同步

- AGENTS.md：Build 节补 Shell 注意、项目记忆节补 WorkBuddy 版压缩、SequenceWorker 节补 RunSingleAction 接口与实现、ProcessPage 节补执行选中动作按钮、Continuous QA 补第 5 条冒烟规范。
- TEST_RECORD.md：RunSingleAction 条目按钮位置描述修正（顶栏→动作列表下方）。
- 本 worklog 新建。

## 明日计划 / 待办

- 真机复测：单动作独立执行 GUI 点击交互（执行/按钮禁用恢复/弹窗/与单步互斥）+ 阶段 4 遗留 🟡（单步执行 UI 接线 5.1-5.5、全局急停 5.4/5.12）。
- 阶段 5 单步→自动运行测试按计划推进。
- 舵机重连抖动治理继续真机观察（硬件定性已明，等供应商反馈）。
