# 2026-08-21 — 代码评审 + 缺陷修复（D1-D17）

## 背景
上午其他模型完成阶段 3 T7-T10 后，对 7 个 UI 文件做结构化评审（6 并行 agent 深度分析），确认 17 项问题（6 高优先 + 5 中 + 6 清理）+ 5 待调查。

## D1-D6 高优先修复（上一轮）
- D1 方案下拉永远"（无方案）"且静默运行第 1 个方案：ProcessManager::load() 提前到 MainWindow SetupUI 前 + AutoRunPage RefreshSchemeCombo()+showEvent 刷新
- D2 方案失败后启动按钮永久置灰：OnError 恢复 m_btnStart + 状态标签
- D3 ReloadFromConfig 跨线程数据竞争：入口加 running 门禁（运行中跳过）
- D4 运行中关闭窗口退出挂死：ShutdownWorker()（Stop 置 cancel → quit → wait(20)+processEvents 循环 → terminate 兜底）
- D5 RefreshCoordPanel 每 50ms 读舵机串口 40 次/秒 + 60 条日志/s：改用 stateUpdated/servoStateUpdated 缓存 + Kinematics 成员缓存（参数变化才重建）
- 验证：Debug+Release 编译通过；退出测试 10s 内退出无挂死；config 临时切 Sim 已恢复

## D7-D17 修复

## 完成
- D7 OnFrameReady 空结果跳过深拷贝（原先每帧 921KB copy + 空跑 DrawOverlays ≈18MB/s）
- D8 OnTeachRead：校验动作类型=Move + push 进 action.points + save + RefreshActionDetail（对齐 OnAddPoint，修复示教点位不持久化）
- D9 方案增删改后下拉同步：由 D1 的 showEvent 刷新覆盖（QStackedWidget 单页可见，切页即刷新）
- D10 OnResetClicked/OnInitClicked 检查 HomeAll()/Initialize() 返回值并提示失败
- D11 急停无条件先 HardwareManager::EmergencyStop()（安全关键不依赖 worker 存在）
- D12 ConfigPage Tab5 limitMin/limitMax emit paramsChanged（补齐 ReloadFromConfig 的 SetJointLimits 热更新）
- D13 日志框 setMaximumBlockCount(1000) 防无限膨胀
- D14 删 GetSequenceWorker() 死代码
- D15 删 AutoRunPage.h QTimer 前向声明 + .cpp QDebug include（均未使用）
- D16 新增 src/UI/KinematicsHelper.h（FromConfig/ReadConfigParams），消除 AutoRunPage+ProcessPage 两处重复且统一读 toolOffsetY（原 AutoRunPage 硬编码 Y=0）；放 UI 层遵守分层（Core 不依赖 Config）；SequenceWorker::ReloadFromConfig 为超集保留独立
- D17 wait 魔数/重复 quit 由 D4 ShutdownWorker（kMaxWaitMs 命名常量）已覆盖

## 验证
- Debug+Release 编译通过、无警告
- Sim 模式冒烟 7s 存活无崩溃；config 临时切 Sim 已恢复 Bopai/XRServo 真机模式
- GUI 完整联测待真机
