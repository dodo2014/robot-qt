# 工作日志 2026-09-10

> 承接 `2026-09-09.md`（阶段 8 收口 + 阶段 9 推进至 9.9，含 TR-079/080/081）。本日志覆盖 **阶段 9 收尾（9.10–9.17 全 🟢）+ 阶段 10 全程（10.1–10.20 全 🟢）+ 设计答疑（TR-085）**；当日修复 **TR-082 ~ TR-088** 共 7 项（2 项语义澄清 + 1 项安全处置 + 1 项结构性缺陷根治 + 1 项用例失效溯源 + 2 项显示缺陷）。**phase3 三阶段（8/9/10）真机验证全部收口**。

## 一、发现问题与修复总览

| TR | 问题 | 根因 | 处置 | 提交 |
|---|---|---|---|---|
| **TR-079 / 080 / 081** | 配置页示教不同步 + 回安全位误报「有任务执行中」+ 上升沿语义澄清（急停恢复链改手动） | 均于 **09-09** 发现并修复（含急停恢复链文档与实现矛盾的连带发现），详见 `2026-09-09.md` | 见 `2026-09-09.md` | `14c9455` `20341b3` `a28bd7e` `9224aa7` `be63b27` `1297c02` |
| **TR-082** | `safePos.enabled` 实为**总开关**（同时管自动触发与手动按钮），但 UI 名只写「回零后自动回安全位」 | `RunSafePos` 第一行即查 `enabled`，手动 `OnGoSafePos` 与自动触发**共用该函数**；且 `enabled` 兼作「已示教」标记 | 决策**方案 A**：语义统一为总开关，**逻辑不变仅改文案**——复选框改「**启用安全位**」+ tooltip 注明双入口；示教/拒绝提示同步。方案 B（分离控制、新增 `configured` 字段）否决 | `690d225` |
| **TR-083** | 工艺页单步停在 `Paused(Step)` → 一键回零 → 点「回安全位」误报「有任务执行中」 | 一键回零只走 `HardwareManager::HomeAxis`，**完全不触碰 SequenceWorker**；单步会话阻塞在 `WaitForStep`（只认 `stepGo`/`cancel`）→ 回零后 worker 仍 Paused | 新增 `PrepareHomingSession()`：**Paused → `Stop()` 终止后放行回零**；**Fault → 不动锁存仅预告**；**Running → 拒绝回零**（`Stop()` 不下发硬件停止属假安全）。`Stop()` 置于 `HomeAxis` 之**前**且不等待（回零耗时秒级 vs worker ≤20ms 收敛）；工艺页 UI 靠既有 `interrupted → ResetStepSession` 自动复位，**无跨页耦合** | `09a9705` |
| **TR-084** | 关闭软件时若存在未完成会话 → 崩溃 `0xc0000005` | **结构性**：worker 由 `connect(workerThread_, &QThread::finished, ..., deleteLater)` 交事件循环销毁，而 4 个观察者持**裸指针**——判空只防 null 不防悬垂；退出时 `ShutdownWorker:229` 的 `processEvents` 派发**已 post** 的 `interrupted`（Qt 不因 sender 析构回撤）→ 槽访问已释放内存 | 决策**方案 C**：4 处观察者指针改 `QPointer`（析构自动置空），**不动退出时序**（processEvents 是防 `BlockingQueuedConnection` 挂死的刻意设计）。方案 D（去 deleteLater / 重排析构）否决 | `d1d5346` |
| **TR-085** | 设计答疑：点动停止是否需要也做成减速停 | **隐含前提不成立**：卡轴点动松键 `BoPaiCard::StopJog`（:462）与自动暂停 `StopAxis`（:334）是**同一个 `MC_Stop(mask,0)`**，舵机两者同为 CMD24——点动停止本就是减速停 | 零代码改动，结论入档（`architecture.md` §5 停止曲线不变量）；真正可讨论的滑行距离：零成本手段=降点动速度，后续=`MC_SetStopDec` 调档 | `d5f7ccb` |
| **TR-086** | 用例 10.12「Paused+Step 点启动/继续放行下一步」场景不可构造 | 三重阻断：自动模式工艺页不可达（白名单 `{0,3}`）+ 手动模式自动页五钮全灰 + 带单步切自动强制清理 → Paused(Step) 与可点的启动/继续**永不同时存在**；用户实测"能点"实为 Idle 态重启新方案 | 用例失效溯源入档（照抄自 TR-073 设计矩阵，前提 `gate=true` 被同提交落地的互锁废弃）；真实现象解释（Idle 重启）+ <20ms 竞态假象说明 | `98c7997` |
| **TR-087** | 急停红字恢复指导被黄字「舵机已重连」覆盖（10.14） | `SetHint` 无优先级（后写必覆盖）；急停后舵机短暂离线→约 1s 恢复→边沿提示 + `anyServoDisabled` 恰好满足 | `OnServoStateUpdated` 边沿提示加 `IsEStopPending()` 守卫（急停锁存期间抑制）；**离线根因=COM 转 USB 转换器硬件不稳定**（用户确认，待换型复测） | `986a528` / `61d190c` |
| **TR-088** | 10.16 绿字③被「回零完成」覆盖 + 10.17 安全位完成显示「方案执行完成」误导 | ① `estopCleared` 与 `axisMoveFinished` 同拍先后 emit，后者覆盖前者（必然发生）；② 安全位临时方案不在方案列表 + 「⏸ 待机」期望系用例笔误（Idle 无写入路径） | ① `estopClearPending_` UI 层锁存（消费显式信号，不依赖 emit 顺序隐式契约——方案 B 硬件层调序经扩展性/长远分析后否决）；② AutoRunPage 在 `OnActionStarted` 记录会话标志，完成槽分流显示「✅ 已回安全位」 | `5a2f5ac` |

**阶段 10 真机验证（TR-075 互锁 + 急停恢复全链路，10.1–10.20 全 🟢，TR-089 收口）**：10.1–10.9（导航白名单 / 视觉页参数锁 / 运行中禁切模式 / 相机降级路径）、10.10/10.11（单步残留强制清理 + 无脏上下文，TR-083 的复测载体）、10.13–10.17（**急停恢复全链路**：急停 → 切手动 → 使能 → 回零 → **手动回安全位** → 切自动）、10.18–10.20（已知边界 + 手动全流程回归）。过程发现并修复 TR-086/087/088（上表），全部真机复测通过。**急停恢复闭环验证通过 → phase3 三阶段（8/9/10）全部收口**。

## 二、关键认知沉淀（本阶段新增）

1. **自动回安全位 = 软件启动后的第一次一键回零**。`axisHomed_` 只在启动初始化 / 回零被中断时清除，其后 `lastHomed_` 恒 true → 无上升沿。9.9（全轴重回零）、9.11（Paused 重回零）、9.12（单轴重回零）三条实测一致。**防误触发是刻意设计**（否则单轴回零会突然走安全位）。
2. **`safePos.enabled` 是总开关**，不是「自动开关」——命名必须覆盖其**全部**控制入口。
3. **不经过状态机的硬件操作（回零/点动/使能）必须回头确认是否要清理状态机残留**。异步挂起点的清理代码在执行循环**之后**，"操作完成即自动收敛"的假设不成立。
4. **跨线程/长生命周期 QObject 的观察者指针一律用 `QPointer`**，禁用裸指针长期持有——已沉淀至 `doc/architecture.md` §5 关键不变量。
5. **判空只防 null、不防悬垂**；且**已 post 的跨线程 Queued 信号在 sender 析构后仍会派发**。
6. 安全论证要基于事实：回零是**重建**坐标基准（非使其失效）；残留会话的真正危害是**位姿不连续**（从机械原点直冲旧方案下一绝对目标，四轴独立 MoveAbs 无「先抬 Z」前提）+ 异步竞争致 Fault 锁存。
7. **UI 正确性不应依赖生产侧 emit 相对顺序**——隐式契约无编译保护、分散在实现路径（卡轴/舵机各一处）并随轴类型线性增长、重构易碎；因果关系应编码在**消费侧状态**（TR-088 锁存）或**显式信号语义**中。
8. **用例必须随设计变更同步复审**：照抄设计矩阵/接口表时逐条核对前提假设（如"gate 与模式是否绑定"）在落地后是否仍成立——设计稿与互锁同批落地时（TR-073/075 同提交 `2be4083`），测试文档最容易遗漏前提失效（TR-086）。
9. **单条覆盖式提示条 + 多信号源 = 必然的提示竞争**：安全关键指导（红字）与普通提示（黄字）共占一条提示条时，必须有状态守卫或优先级；跨事件、跨时间的覆盖无法靠"改调用顺序"解决（TR-087）。
10. **硬件环境事实**：舵机偶发离线/重连抖动的根因是 **COM 转 USB 转换器不稳定**（廉价型号，用户确认，待换型）——换型前此类现象优先怀疑转换器硬件而非软件。

## 三、验证与提交

- **验证**：每次改动均 Debug + Release 双编译 `EXITCODE=0` + Sim 冒烟 `SMOKE_ALIVE=True` / `SMOKE_INIT_COMPLETE=True`。**Sim 覆盖不到**舵机回零、安全位走位、退出时序等路径（SimServo 不走 XRS 协议），相关结论以真机为准。
- **用例文档**：`doc/test/real_machine_plan_phase3.md` 阶段 9 新增 9.15（残留处置）/9.16（Running 拒绝），原 9.15 顺延 **9.17**（编号去重 `9103f6d`）；测试记录表阶段 9 填 🟢；工时表用例数 15 → 17。
- **提交链**（**均未 push**）：09-09 五个 commit（TR-079/080/081）见 `2026-09-09.md`；**09-10 当日 15 个**：`1297c02`（9.9 复测标记）→ `690d225`（TR-082）→ `09a9705`（TR-083）→ `9103f6d`（编号去重）→ `f55a7a6`（阶段 9 收口）→ `0e15297`（真机调参）→ `d1d5346`（TR-084）→ `f57a5a6`/`709560d`/`62ad7d2`（worklog 10/09/08 三篇）→ `d5f7ccb`（TR-085）→ `61d190c`（COM 转换器根因）→ `7341324`（10.1–10.9 标记）→ `98c7997`（10.10–10.12 + TR-086）→ `aa210a9`（10.16–10.20 标记）→ `be7e9c5`（**阶段 10 收口 TR-089**）。
- **`log/crash.txt`**：TR-084 定位后已清空（崩溃产物；崩溃栈记录保留于 TR-084 条目）。

## 四、待办

- ~~阶段 10 真机验证~~ **已完成（10.1–10.20 全 🟢，TR-089 收口）**。
- **循环生产真机验证（下一批次）**：`doc/auto_run_loop_production.md` 用例 **10.1–10.26**（独立编号，与 phase3 的 10.x **撞号**，引用须注明文档）；核心 = 循环运行互锁（10.6/10.21/10.22）+ 急停恢复链仍成立（10.23）。
- **换 COM 转换器后复测**：舵机离线/重连频率应显著下降（TR-028/TR-087 现象随之消失）；TR-087 的 estopPending 守卫保留。
- **TR-084 退出复测**：日常多轮退出未见新增崩溃（`log/crash.txt` 保持为空）；如条件允许再做一次"有残留会话时关软件"的定向验证。
- 未 push（需用户授权）。

## 五、过程说明

- worklog 08/09/10 三篇已齐：`2026-09-08.md`（phase3 用例文档新建 + 阶段 8 执行至 8.13 + TR-076 发现）、`2026-09-09.md`（阶段 8 收口 + 阶段 9 推进至 9.9）、本篇（阶段 9 收尾 + 阶段 10 全程收口）。更早的 2026-09-04 及以前见 TEST_RECORD.md。
- 本次多次遵循「先查证再定方案」：每项问题均先由只读探查拿到文件:行号级证据，再经 Plan 设计、用户裁决（TR-081~084/086/088 均给方案对比后由用户选定，TR-087 用户补关键硬件根因）。
- **编号撞车警示**：`auto_run_loop_production.md`（循环生产）与 `real_machine_plan_phase3.md` 阶段 10 均用 10.x 编号且内容相近（急停恢复链），引用/标记时**务必注明文档**。

## 六、设计评审答疑（TR-085，零代码改动）

用户问"自动暂停是减速停，点动停止有必要也做成减速停么"。查证发现**隐含前提不成立**：卡轴点动松键 `BoPaiCard::StopJog`（`:462`）与自动暂停 `StopAxis`（`:334`）是同一个 `MC_Stop(mask,0)` 减速斜坡（同指令同实参），舵机两者同为 CMD24 立即停+锁力——**点动停止本就是减速停，无需改动**。真正可讨论的是滑行距离（`jogSpeed==maxSpeed` → 与自动全速停相当）：零成本手段=降点动速度；后续手段=`MC_SetStopDec` 调陡（按轴全局，须防与自动流程并发改档，排跨页互斥后）。**决策：维持现状**，结论入 `TEST_RECORD` TR-085、`architecture.md` §5 停止曲线不变量、phase3 风险节。
