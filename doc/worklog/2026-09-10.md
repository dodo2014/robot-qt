# 工作日志 2026-09-10

> 承接 2026-09-09 晚起的真机执行：本日志覆盖 **阶段 9（TR-074 全局安全位）真机验证闭环**，用例 **9.1–9.17 全通过 🟢**。期间真机暴露并修复 **6 项问题（TR-079 ~ TR-084）**，其中含 2 项安全语义纠正与 1 项结构性缺陷根治。

## 一、发现问题与修复总览

| TR | 问题 | 根因 | 处置 | 提交 |
|---|---|---|---|---|
| **TR-079** | 示教安全位后设备配置页不同步（重启才显示）；config 写入科学计数法 | `ConfigPage` 安全位控件只在**构造时**读一次 config，且该类**无 showEvent**；示教走 `ConfigManager::set` 按设计不 emit `paramsChanged` | 新增 `RefreshSafePosFields()` + `showEvent` 重写（复选框用 `QSignalBlocker` 防反向写回）；示教写入前量化两位小数并消 −0 | `14c9455` / `20341b3` |
| **TR-080** | 点「回安全位」报「有任务执行中」（实际无轴在动） | 三层叠加：① 舵机重复回零时 `homingActive_` 走不到清除分支（`!axisHomed_` 条件挡住）滞留 5s；② 全轴 homed 上升沿自动回安全位，首条 `MoveAbs` 撞回零门禁被拒 → `ExecuteActions` 置 **Fault 锁存**；③ 手动页把 Fault 误报成「有任务执行中」 | `checkServoHomeDone` 去掉 `!axisHomed_` 条件（与卡轴路径对齐，重复回零 ≤250ms 解锁）；`OnGoSafePos` 区分 Fault 并指引清报警；补 `triggered/skipped` 成对日志 | `a28bd7e` / `9224aa7` |
| **TR-081** | 9.9/9.11 用例期望与实现语义偏差；**连带发现急停恢复链文档与实现矛盾** | 自动回安全位用 **`lastHomed_` 上升沿**判定，而 `axisHomed_` 仅在「启动初始化 / 回零被中断」时清除——手动移走、**急停**、断使能都不清 → 已回零状态下重回零无上升沿。故急停恢复链原描述的"回零→自动回安全位"**不成立** | 决策**方案 A**（接受现状、零行为逻辑改动）：恢复链改为「使能→回零→**手动**回安全位→切自动」；急停红字①文案同步（唯一代码改动）；同步 `global_safe_position.md` §五、`architecture.md`、`auto_run_loop_production.md`。方案 B（急停清全轴 homed 使上升沿成立）**否决**（语义扩张影响面大） | `be63b27` / `1297c02` |
| **TR-082** | `safePos.enabled` 实为**总开关**（同时管自动触发与手动按钮），但 UI 名只写「回零后自动回安全位」 | `RunSafePos` 第一行即查 `enabled`，手动 `OnGoSafePos` 与自动触发**共用该函数**；且 `enabled` 兼作「已示教」标记 | 决策**方案 A**：语义统一为总开关，**逻辑不变仅改文案**——复选框改「**启用安全位**」+ tooltip 注明双入口；示教/拒绝提示同步。方案 B（分离控制、新增 `configured` 字段）否决 | `690d225` |
| **TR-083** | 工艺页单步停在 `Paused(Step)` → 一键回零 → 点「回安全位」误报「有任务执行中」 | 一键回零只走 `HardwareManager::HomeAxis`，**完全不触碰 SequenceWorker**；单步会话阻塞在 `WaitForStep`（只认 `stepGo`/`cancel`）→ 回零后 worker 仍 Paused | 新增 `PrepareHomingSession()`：**Paused → `Stop()` 终止后放行回零**；**Fault → 不动锁存仅预告**；**Running → 拒绝回零**（`Stop()` 不下发硬件停止属假安全）。`Stop()` 置于 `HomeAxis` 之**前**且不等待（回零耗时秒级 vs worker ≤20ms 收敛）；工艺页 UI 靠既有 `interrupted → ResetStepSession` 自动复位，**无跨页耦合** | `09a9705` |
| **TR-084** | 关闭软件时若存在未完成会话 → 崩溃 `0xc0000005` | **结构性**：worker 由 `connect(workerThread_, &QThread::finished, ..., deleteLater)` 交事件循环销毁，而 4 个观察者持**裸指针**——判空只防 null 不防悬垂；退出时 `ShutdownWorker:229` 的 `processEvents` 派发**已 post** 的 `interrupted`（Qt 不因 sender 析构回撤）→ 槽访问已释放内存 | 决策**方案 C**：4 处观察者指针改 `QPointer`（析构自动置空），**不动退出时序**（processEvents 是防 `BlockingQueuedConnection` 挂死的刻意设计）。方案 D（去 deleteLater / 重排析构）否决 | `d1d5346` |

## 二、关键认知沉淀（本阶段新增）

1. **自动回安全位 = 软件启动后的第一次一键回零**。`axisHomed_` 只在启动初始化 / 回零被中断时清除，其后 `lastHomed_` 恒 true → 无上升沿。9.9（全轴重回零）、9.11（Paused 重回零）、9.12（单轴重回零）三条实测一致。**防误触发是刻意设计**（否则单轴回零会突然走安全位）。
2. **`safePos.enabled` 是总开关**，不是「自动开关」——命名必须覆盖其**全部**控制入口。
3. **不经过状态机的硬件操作（回零/点动/使能）必须回头确认是否要清理状态机残留**。异步挂起点的清理代码在执行循环**之后**，"操作完成即自动收敛"的假设不成立。
4. **跨线程/长生命周期 QObject 的观察者指针一律用 `QPointer`**，禁用裸指针长期持有——已沉淀至 `doc/architecture.md` §5 关键不变量。
5. **判空只防 null、不防悬垂**；且**已 post 的跨线程 Queued 信号在 sender 析构后仍会派发**。
6. 安全论证要基于事实：回零是**重建**坐标基准（非使其失效）；残留会话的真正危害是**位姿不连续**（从机械原点直冲旧方案下一绝对目标，四轴独立 MoveAbs 无「先抬 Z」前提）+ 异步竞争致 Fault 锁存。

## 三、验证与提交

- **验证**：每次改动均 Debug + Release 双编译 `EXITCODE=0` + Sim 冒烟 `SMOKE_ALIVE=True` / `SMOKE_INIT_COMPLETE=True`。**Sim 覆盖不到**舵机回零、安全位走位、退出时序等路径（SimServo 不走 XRS 协议），相关结论以真机为准。
- **用例文档**：`doc/test/real_machine_plan_phase3.md` 阶段 9 新增 9.15（残留处置）/9.16（Running 拒绝），原 9.15 顺延 **9.17**（编号去重 `9103f6d`）；测试记录表阶段 9 填 🟢；工时表用例数 15 → 17。
- **提交链**（2026-09-09 ~ 09-10，共 10 个 commit，**均未 push**）：`14c9455` `20341b3`（TR-079）→ `a28bd7e` `9224aa7`（TR-080）→ `be63b27` `1297c02`（TR-081）→ `690d225`（TR-082）→ `09a9705`（TR-083）→ `9103f6d` `f55a7a6` `0e15297`（阶段 9 收口 + 真机调参）→ `d1d5346`（TR-084）。
- **`log/crash.txt`**：TR-084 定位后已清空（崩溃产物；崩溃栈记录保留于 TR-084 条目）。

## 四、待办

- **阶段 10 真机验证（TR-075 UI 互锁 + 急停恢复全链路，10.1–10.23）**——注意 10.14/10.16/10.17 已按 TR-081 修正为「手动回安全位」；10.23 恢复链描述同步。
- **TR-084 退出复现真机验证**：让工艺页单步停在 Paused（或方案连续执行中）→ 关闭软件 → 确认不弹崩溃、`log/crash.txt` 不新增。
- **TR-080 相关**：9.16（Running 时一键回零被拒）为行为变更，真机确认。
- 未 push（需用户授权）。

## 五、过程说明

- worklog 缺 2026-09-08 / 09-09 两篇（阶段 8 收口与阶段 9 前半程记录在 `TEST_RECORD.md` TR-076~TR-080）；本日志起于阶段 9 修复链的完整闭环。
- 本次多次遵循「先查证再定方案」：每项问题均先由只读探查拿到文件:行号级证据，再经 Plan 设计、用户裁决（TR-081/082/083/084 均给方案对比后由用户选定）。
