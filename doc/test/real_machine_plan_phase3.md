# 真机联调计划（阶段 3：自动运行 5 按钮 / 全局安全位 / UI 权限互锁）— 2026-09-08 起

> 状态：**待执行**（代码 TR-073 / TR-074 / TR-075 已于 2026-09-07 完成并提交 `2be4083`；Debug+Release 双编译 EXITCODE=0、Sim 冒烟 `SMOKE_INIT_COMPLETE=True` 通过）
> 配套：`doc/test/real_machine_plan_phase2.md`（阶段 0-7 已全 🟢，本轮沿用其阶段 0/1 前置与真机环境知识）
> 　`doc/auto_run_button_refactor.md`（TR-073）、`doc/global_safe_position.md`（TR-074）、`doc/ui_interlock_plan.md`（TR-075，含"实施定稿"节）
> 　台账：`TEST_RECORD.md` TR-073 / TR-074 / TR-075 / TR-076（当前最大号 TR-076，执行后新记录从 TR-077 起）
> 编号规则：阶段 8 = TR-073、阶段 9 = TR-074、阶段 10 = TR-075（与 phase2 的 0-7 零撞号，跨文件引用不歧义）

## ⚠ §0 代码走查发现的三处待确认事实（用例中已标注，非阻塞，但需记录）

| # | 事实 | 依据 | 处理 |
|---|---|---|---|
| A | 自动页提示写 **「全部使能」**，而手动页实际按钮名是 **「全局轴使能」/「全局断使能」**——文案与按钮名不一致（历史遗留，非本轮引入） | `AutoRunPage.cpp:367/454/632` vs `ManualControlPage.cpp:140/145` | 用例 8.10 / 8.11 记录；如需修文案另开 TR |
| B | 「回安全位」提示说"可在**自动运行页**暂停/停止"，但该路径实际**不可达**：手动模式下自动页五钮因 `gate=false` 全灰，且安全位会话 Running 期间切【自动】会被门禁弹窗拒绝 | `AutoRunPage.cpp:566/575`、`MainWindow.cpp:667` | 用例 9.8 记录；**安全位走位有危险时只能按顶栏【急停】** |
| C | 回零完成触发的**自动**回安全位由 MainWindow 直调 `RunSafePos()`，手动页 `SetHint` 不参与 → 回零后轴突然继续走位，手动页**无任何文字提示** | `MainWindow.cpp:171-183` | 用例 9.9 记录 UX 缺口（如需补提示另开 TR） |

---

## 一、术语与状态速查（执行前必读）

本节供真机执行时快速对照，避免靠"感觉"判断状态。

### 1.1 `SequenceWorker::WorkerState` 四态速查

| 状态 | 含义 | 触发方式 | 自动页五钮表现（gate=true 时） | 典型日志 |
|---|---|---|---|---|
| **Idle** | **没有会话在执行**（引擎空闲） | 上电默认 / 跑完 / 停止 / 清报警后 | 启动/继续=按 homed 决定、**暂停灰、停止灰、清报警可点、初始化可点** | `state -> 0 reason 0` |
| **Running** | 正在执行动作 | 点「▶ 启动/继续」、`RunSafePos()` | 启动灰、**暂停可点、停止可点**、清报警灰、初始化灰 | `state -> 1 reason 0` |
| **Paused** | 挂起且保留上下文 | 点「⏸ 暂停」、单步等下一步 | **启动/继续可点（继续）**、暂停灰、停止可点、清报警灰、初始化灰 | `state -> 2 reason 0/1`（0=用户暂停，1=单步等待） |
| **Fault** | 上次失败且未清除（**软件锁存**） | 动作失败（如 IK 不可达） | 启动灰、暂停灰、停止灰、**清报警可点**、初始化可点 | `state -> 3 reason 0` |

- `IsRunning()` 定义为 `state != Idle` → **Idle = 引擎没持有任何会话**。
- 让引擎离开 Idle 的只有三条路：自动流程 `RunSequence`、单动作 `RunSingleAction`、**安全位 `RunSafePos()`**（回零后自动回安全位期间也是非 Idle）。

### 1.2 `Idle` **不等于**什么（真机最易混淆）

- Idle **≠ 已使能** —— 使能是 `HardwareManager::axisEnabled_`，另一套状态
- Idle **≠ 已回零** —— `homed` 也是另一套（所以 Idle 时「▶ 启动/继续」仍可能因未回零而灰）
- Idle **≠ 无故障** —— 故障是独立的 `Fault` 态；Idle 只表示"当前没在跑"
- Idle **≠ 轴静止** —— 手动页点动不经过 worker，轴在动而引擎仍可以是 Idle

### 1.3 真机上如何确认"当前是 Idle"（按可靠度排序）

1. **日志**最近一条是 `[SequenceWorker] state -> 0 reason 0`（0=Idle、reason 0=None）——最准
2. **自动页五钮矩阵**：暂停灰、停止灰、清报警可点、初始化可点
3. ⚠ **不要用状态标签判断**：`UpdateStatusByState` 的 Idle 分支**故意不覆盖标签**（`AutoRunPage.cpp:622-624`），为保留「✅ 完成」「⏸ 中断」等终态。所以 Idle 时标签显示的仍是上一次的终态，**不是**"空闲"

### 1.4 上升沿 / 电平语义（安全位自动触发的关键，详见 `doc/global_safe_position.md` 三节）

- `homeStateChanged(bool homed)` 是**电平语义**：只要"全轴 homed"成立就会 emit true，**单轴重回零（其余轴 homed 保持）会重复 emit true**。
- MainWindow 用 `lastHomed_` 造**上升沿**（`rising = homed && !lastHomed_`），只有"从 false 跳到 true"那一次才回安全位。
- **若未做上升沿**：操作员单独回零 J1 时也会触发一次安全位走位 → 危险。用例 9.12 专测此回归。
- **急停锁存同理**：不能用 `IsSystemHomed()` 判定急停是否恢复（TR-062：`AbortHoming` 只复位"回零中"的轴）→ 用 `estopPending_`（TR-075）。

## 二、目标

在真机上验证 2026-09-07 落地的三项功能（Sim 冒烟已过，本轮**只补 GUI 交互与真机走位**）：

1. **TR-073** 自动运行页 5 按钮 + SequenceWorker 显式状态机（点级暂停原地续走、StopImmediate、Fault 锁存、初始化分级引导、单步零回归）。
2. **TR-074** 全局安全位（示教落盘、先抬 Z 再水平、回零上升沿自动触发、两个跳过条件、不污染工艺页列表）。
3. **TR-075** UI 权限与全局互锁（启动默认手动、导航白名单、视觉页只读、模式切换门禁、单步会话强制清理、急停恢复全链路闭环）。

本轮**不改代码**（纯验证）。若真机暴露缺陷需改码，回归 `AGENTS.md` 硬规约：Debug+Release 双编译 + Sim 冒烟判定日志含 `Initialize complete`。

## 三、硬件与配置现状

### 3.1 机械与轴
沿用 `real_machine_plan_phase2.md` §二：J1/J2/R 舵机 + Z/夹爪/挤出卡轴；本轮**无硬件变更**。软限位 J1 `[−102, 8]`、Z 按标定值、夹爪 `[−5, 0]`。

### 3.2 网络（**开工前必做**）
- **必须关闭 wifi**：BoPai SDK `MC_Open` 在 wifi 开启时会失败（code=-6）（`AGENTS.md:104`）。
- `ipconfig` 确认 pcIp 为**真实网卡 IP** 且与卡 `192.168.0.1` 同网段 → `ping 192.168.0.1` 通。

### 3.3 config 现状（本轮新增，关键）
- `config/config.json` **当前无 `kinematics.safePos` 节点**（grep 计数 0）→ 阶段 9 的 9.1 可复现"未配置/未启用"分支。
- **示教落盘路径 = 输出目录的 `config.json`**（Debug：`out/build/x64-Debug/config.json`），**不是**源码 `config/config.json`；**重建（POST_BUILD 复制）会覆盖示教结果** → 示教成功后立即备份，并在阶段 9 开始与阶段 10 之前各确认一次 `enabled` 仍为 true。
- `safePos.z` 为**电机坐标**（回零后 0 最高、向下为负），与自动页坐标面板 TCP Z（回零后 165）差固定偏移 **165**；两者不可混用（见 9.5）。
- safePos 每次 `RunSafePos` **现读 config**，ConfigPage 编辑**不 emit paramsChanged**（不触发 Kinematics 重建）。

### 3.4 历史背景
阶段 5（5.1-5.15）已全 🟢；5.9 真实相机未接走降级路径；**TR-066 跨页互斥仍未收口**（手动点动与 worker 并存风险，见 §六 风险第 7 条）。

### 3.5 **前置（每次上电必做）**
1. 关 wifi → `ipconfig` → `ping 192.168.0.1` 通。
2. 启动 **Debug** 构建 → 手动页「全局轴使能」→「一键回零」。
3. 工艺流程页确认有 ≥1 个可用方案（≥2 个 Move 点、speedPercent=50%、点位在工作空间内且远离机械干涉）。
4. 准备一个"不可达点"方案用于制造 Fault（某点 X 手填 600，见 8.7）。
5. 清空运动范围障碍物；**手放顶栏急停按钮旁**。

---

## 四、执行顺序（每组验收不通过即停）

**总体顺序：阶段 8 → 阶段 9 → 阶段 10。** 硬依赖：
- 阶段 8 先做：五钮矩阵与状态机是 9/10 两组的**观测工具**（10.x 全靠按钮灰/亮判定 gate）；且 8.7 的"安全制造 Fault"技法是 10.8 的前置。
- 阶段 9 次做：**9.4（示教安全位）必须早于 10.16**（急停恢复链路要看到"回零后自动走安全位"）；9.1 又必须早于 9.4（依赖 config 无 safePos 的初始态）。
- 阶段 10 最后：急停会断使能、舵机垂落、坐标不可信，放最后避免污染前两组初态；**10.13→10.17 是一条链，中间不可断开**。

标记约定：`[TR-07x]`=验证项；`⚠前置`=特殊前置；`🔗`=必须与上一条连做。

### 阶段 8：自动运行页 5 按钮 + 显式状态机（TR-073）

**前置**：手动模式、已使能 + 已回零、自动运行页下拉已选中有 ≥2 个 Move 点的方案。

| # | 动作 | 期望 |
|---|---|---|
| 8.1 🟢 | 点顶栏开关切【自动】，观察五钮与下拉 | 状态「⏸ 待机」；**启动/继续=可点、暂停=灰、停止=灰、清报警=可点、初始化=可点**；方案下拉可点；日志 `[MainWindow] 模式切换为 自动` `[TR-073][TR-075]` |
| 8.2 🟢 | 点「▶ 启动/继续」启动，运动开始瞬间观察 | 状态变 **「▶ {动作名}」**（绿 #7ed67e）；日志 `state -> 1 reason 0`；五钮变为 启动灰/暂停可点/停止可点/清报警灰/初始化灰；**方案下拉变灰** `[TR-073]` |
| 8.3 🟢 | 运行中（选 J1 长距离段）记录 J1 读数 A → 点「⏸ 暂停」 | 提示 **「暂停请求已下发（减速停止后挂起，点「▶ 启动/继续」恢复）」**；日志 `pause requested (cooperative, takes effect at next gate)` + `state -> 2 reason 1`；轴减速停（**允许继续走一段**，记录减速距离 \|B−A\|，J1 高速约 9° 属设计非缺陷）；停稳后状态「⏸ 已暂停」；按钮 启动可点/暂停灰/停止可点/清报警灰/初始化灰 ★核心 🔗8.4 |
| 8.4 🟢 | 🔗记录四轴读数 (J1,J2,Z,R) → 点「▶ 启动/继续」 | 日志 `resume requested` + `state -> 1 reason 0`；**从半路直奔当前点绝对目标**：J1 单调趋近目标、**无反向回摆、不回到动作起点**；到位后继续后续动作 ★核心 |
| 8.5 🟢 | 运行中（水平段）点「■ 停止」 | 状态**立即**变 **「⏹ 停止中…」**（黄 #f7c948）；提示「正在安全停止…」；日志 `immediate stop requested (decel stop, keep enabled)`；全轴减速停；随后状态「⏸ 中断」、日志框追加「中断: 用户停止」（**文案固定"用户停止"**）；**使能保持** ★核心 🔗8.6 |
| 8.6 🟢 | 🔗不重新使能、不回零，直接点「▶ 启动/继续」 | 能正常启动（证明停止保持使能且未触发回零互锁）；无 `RunSequence rejected` 日志 |
| 8.7 🟢 | ⚠前置：工艺流程页把某 Move 点坐标手填到工作空间外（如 X=600）保存 → 自动页选中该方案 → 点「▶ 启动/继续」 | **IK 在运动前失败，无实际运动**；日志 `IK failed for point (600.0, …)`；状态「⛔ 错误」（红）；提示「执行出错：目标点不可达…（清除报警后可重新启动）」；按钮 启动灰/暂停灰/停止灰/**清报警可点**/初始化可点 🔗8.8 |
| 8.8 🟢 | 🔗点「↺ 清报警」 | 日志 `fault latch cleared (hardware enable NOT touched)` + `state -> 0 reason 0`；提示 **「故障锁存已清除。若驱动器仍报警，请到【手动控制】页断使能后重新使能，再执行一键回零」**；日志框追加「清除报警：{动作名}」；按钮回 Idle 矩阵；**硬件使能状态不变** ★ |
| 8.9 🟢 | Idle 态点「↺ 清报警」（承接 8.8 清完报警即回 Idle，或阶段 8 任意 Idle 时刻） | 提示「当前无故障锁存」；状态不变（不产生状态迁移日志） |
| 8.10 🟢 | ⚠前置：手动页点「全局断使能」（homed 仍 true）→ 切自动 → 点「▶ 启动/继续」 | 按钮**可点**（homed=true）；点击后提示 **「请先使能所有轴（手动页点击「全部使能」）」**（黄）；无运动。⚠**记录 §0-A 文案与按钮名不符** |
| 8.11a 🟢 | ⚠前置：新进程（未使能）→ 切自动 → 点「⟳ 初始化」 | 日志框追加「初始化完成」；提示 **「硬件已连接。请到【手动控制】页「全部使能」→「一键回零」后方可启动」**（蓝）；**点完初始化后手动页仍为未使能**（程序不自动使能，安全原则）★ |
| 8.11b 🟢 | ⚠前置：已使能但**未**回零 → 切自动 → 点「⟳ 初始化」 | 提示 **「已使能但未回零。请到【手动控制】页执行「一键回零」」** |
| 8.11c 🟢 | ⚠前置：已使能 + 已回零 → 切自动 → 点「⟳ 初始化」 | 提示 **「初始化完成，可以启动」** |
| 8.12 🟢 | 运行中点「⟳ 初始化」 | 按钮**灰**不可点（!busy） |
| 8.13 🟢 | ⚠前置：切【手动】→ 工艺流程页 → 点「单步执行」 | 仅执行第 1 个动作后挂起；工艺页状态 **「● 单步暂停」**（#e0a520）；日志 `Step mode: pausing after action 0`；不自动跑后续 🔗8.14 |
| 8.14 🟢 | 🔗再点「单步执行」（=NextStep） | 日志 `Next step released`；执行下一动作后再次挂起；**N 个动作需 N+1 次点击**（末个动作跑完仍挂起）；终态「✅ 完成」、会话复位 |
| 8.15 🟢 | 单步暂停中切到自动运行页观察五钮 | 五钮**全灰**（手动模式 gate=false）；点「▶ 启动/继续」无响应；不会误放行单步 `[TR-075]` |
| 8.16 🟢 | 单步暂停中在自动页点「■ 停止」（跨页，TR-066 已知开口） | 记录实际行为：预期打断单步会话、工艺页变「■ 已停止（保持使能）」。**本条仅记录，不做通过/失败判定** |
| 8.17 🟢 | 正常方案完整跑完 | 状态「✅ 完成」、启动钮恢复可点、无残余 Paused |

> **实测记录（2026-09-08/09）**：
> - 8.1–8.17 全部 ✅（含 8.11a/b/c 三段初始化分级引导；8.13 修复后复测通过）
> - 8.3/8.5 ⚠️ 暂停与停止均有减速滑行距离（开环步进 `MC_Stop` 斜坡，设计预期，操作须预留安全距离）
> - 8.4 ⚠️ 引出新需求：用户确认需要**循环生产** → 方案已出 `doc/auto_run_loop_production.md`（待审核）
> - 8.6/8.8 ⚠️ 停止/清报警后再启动从第一个动作开始 —— **确认为设计意图**（废弃上下文，工业标准语义；断点续跑走「⏸ 暂停/继续」）
> - 8.10 ⚠️ 记录 §0-A：提示文案「全部使能」与按钮名「全局轴使能」不一致（待修文案）
> - 8.11c 附加说明：初始化 = 建连接 + 读轴配置 + 启 50ms 轮询 + 状态归零，**不含使能/回零**（安全原则）
> - 8.13 ⚠️ 首测不通过 → 发现并修复 **TR-076**（stepMode 清除点在 RunSequence 入口抹掉单步标志）→ 复测通过
> - 8.13 复测中发现 **TR-077**（执行选中动作在调用前清单步会话标志，致死锁）→ 修复并复测通过
> - 8.16 ✅ 操作员确认"单步暂停中切自动页五钮全灰"符合逻辑预期
> - 附加问题确认：单步暂停切【自动】强制清理会话 = 既定设计（Q12 原意，2026-09-08 用户确认），日志行为符合预期

### 阶段 9：全局安全位（TR-074）

**前置**：手动模式、已使能 + 已回零；**确认输出目录 config.json 尚无 safePos 节点**（9.1 依赖此状态）。

| # | 动作 | 期望 |
|---|---|---|
| 9.1 🟢 | 手动页点「回安全位」（未配置/未启用） | 提示 **「回安全位未执行：未配置/未启用（设备配置页或示教安全位设定）」**（#e0a520）；**无任何运动**；日志 `RunSafePos skipped: not enabled` |
| 9.2 🟢 | ⚠前置：未回零状态 → 点「示教安全位」 | 提示 **「请先一键回零，再示教安全位」**；config 不写入 |
| 9.3 🟢 | ⚠前置：已回零但某轴运动中 → 点「示教安全位」 | 提示 **「轴运动中，请待停止后再示教」**；config 不写入 |
| 9.4 🟢 | ★已回零，手动走到预期安全姿态（建议 J1=0°、J2=20°、Z=−50、R=0）停稳 → 点「示教安全位」 | 提示 **「已记录安全位：J1=0.00 J2=20.00 Z=-50.00 R=0.00（已启用回零后自动回安全位）」**；**两位小数且与手动页四轴读数一致（±0.01）**；输出目录 config.json 出现 `kinematics.safePos.{j1,j2,z,r,enabled=true}`；设备配置页 4 个「安全位 J1 (°)/J2 (°)/Z (mm)/R (°)」显示同值、「回零后自动回安全位」**已勾选** ★核心 |
| 9.5 | ★Z 语义校验：比对提示中的 Z、手动页 Z 读数、自动页坐标面板 TCP Z | 提示 Z = 手动页 Z 读数（**电机坐标**），与坐标面板 TCP Z 差 **165**；三者关系一致（混用会撞顶）`[TR-071]` |
| 9.6 🟢 | ★手动走到远离安全位的低位姿态（J1=−60、J2=40、Z=−150）停稳并记录四轴 → 点「回安全位」 | 提示 **「正在回安全位（先抬 Z 再水平移动，可在自动运行页暂停/停止）」**；**第 1 段只有 Z 动**（J1/J2/R 不变，容差 ±0.1°），Z 到位后第 2 段才动 J1/J2/R；两段先后明确；速度约最大 50% ★核心 **实测 2026-09-09：首次误报「有任务执行中」（Fault 锁存误报，见 TR-080）；修复后复测通过——二次回零后自动回安全位正常、无 Fault；三次回零后不再触发（上升沿正常）；手动回安全位成功。『回零需多次执行（首轮回零轴1未到位）』定性 feature 暂不修** |
| 9.7 🟢 | 🔗走位途中（水平段）点顶栏开关切【自动】 | 弹窗标题「禁止切换模式」，正文 **「正在返回安全位，请稍候，完成后即可切换模式。」**（**不含**"请先点击停止"）；日志 `[MainWindow] 模式切换被拒：执行中 autoMode=1 state=1`；开关回弹保持【手动】★ |
| 9.8 | 走位途中切到自动运行页看五钮；再切到工艺流程页看列表选中行 | 五钮**全灰**（提示中的"可在自动运行页暂停/停止"实际不可达 → §0-B）；工艺流程页**选中行不跳到第 0/1 行**、状态文本不被改成「回安全位」（`IsSafePosSession` 守卫生效）★ |
| 9.9 🟢 | ★先手动走到别处（远离安全位）→ 手动页点「一键回零」 | 【期望修正 TR-081】已回零状态重回零：`axisHomed_` 全程 true → `homeStateChanged(true)` 无上升沿 → **不自动发起**安全位走位（与 9.12 单轴重回零同语义，设计如此——防"只想回零却突然走安全位"）；手动点「回安全位」正常执行两段走位。**实测 2026-09-09 与修正后期望一致**（原文本"自动发起"与上升沿设计矛盾，系用例笔误；重启后首次回零的自动触发已由 9.6 复测覆盖）★核心 |
| 9.10 | 跳过条件 A：设备配置页**取消勾选**「回零后自动回安全位」→ 回手动页点「一键回零」 | 回零后**无**安全位走位；轴停在回零位（J1=−102、J2≈−28、R≈0）；无 RunSafePos 相关日志；复选框仍为未勾选（现读现用，不 emit paramsChanged，不触发 Kinematics 重建） |
| 9.11 | 跳过条件 B：重新勾选 enabled → ⚠先让工艺流程页单步停在 Paused（worker 非 Idle）→ 点「一键回零」 | 日志 **`[MainWindow] auto safe-pos skipped: worker not idle`**；无安全位走位。**可选**（构造困难，若不可达标注"未复现，按代码路径确认"） |
| 9.12 | 电平语义回归：enabled=true 且已 homed → 手动页对 J1 单轴点「回零」 | `homeStateChanged(true)` 会再次 emit，但 `lastHomed_` 上升沿判定 → **不重复触发**安全位走位 |
| 9.13 | 现读现用：设备配置页把「安全位 J1」改成另一合法值（如 −20）保存 → 手动页点「回安全位」 | 走到**新** J1 值（−20）；**不触发 Kinematics 重建**（无运动学重载日志/无 paramsChanged 副作用） |
| 9.14 | 幂等：已在安全位时点「回安全位」 | 仍执行两段（位移≈0），完成后 Idle，无报错无卡死 |
| 9.15 | 有任务执行中：工艺流程页单步到 Paused（worker 非 Idle）→ 手动页点「回安全位」 | 提示 **「有任务执行中，无法回安全位」**（#e0a520）；无运动 |

> **实测记录（　　　　）**：

### 阶段 10：UI 权限与全局互锁 + 急停恢复全链路（TR-075）

**前置**：阶段 8/9 已完成；**safePos 已示教且 enabled=true**（10.16 依赖）；本组最后 5 条（10.13-10.17）**必须一口气连做**。

| # | 动作 | 期望 |
|---|---|---|
| 10.1 | 启动程序，不做任何操作 | 开关在【手动】侧；**「手动」标签绿 #7ed67e /「自动」灰 #8da3bb**；导航 5 项（🌟自动运行/🛠️手动控制/📝工艺流程/🎥视觉检测/⚙️设备配置）**全部可点**；自动页五钮**全灰**；视觉页 9 参数控件**可编辑**；无模式切换日志 ★ |
| 10.2 | 先切到「🛠️ 手动控制」→ 点开关切【自动】 | 日志 `模式切换为 自动`；**自动跳到「🌟 自动运行」**（导航选中 + 内容区同步）；「🛠️ 手动控制」「📝 工艺流程」「⚙️ 设备配置」**置灰且点击无响应**；「🌟 自动运行」「🎥 视觉检测」可点；标签高亮互换 ★ |
| 10.3 | 自动模式切到「🎥 视觉检测」页 | 9 控件**全部置灰不可编辑**：相机型号/序列号/分辨率/帧率/算法型号/置信度阈值/深度 Z min/深度 Z max/曝光；而「打开相机」「关闭相机」「开始采集」「停止采集」「单次检测」「连续检测」「加载图片」「视图切换」「截图」等**操作类仍可点** ★ |
| 10.4 | 切回【手动】→ 再到视觉检测页 | 9 控件**恢复可编辑**；操作类不受影响 |
| 10.5 | 自动模式点「打开相机」（无真实相机） | 不崩溃；按 5.9 降级路径处理。⚠误点「关闭相机」致检测走 Fault → 记**已知边界**，回自动页「↺ 清报警」恢复 |
| 10.6 | ★自动模式运行中 → 点开关试图切【手动】 | 弹窗标题 **「禁止切换模式」**，正文 **「当前有流程正在执行，禁止切换【手动 / 自动】模式。\n请先点击「停止」结束当前执行，再切换模式。」**；确定后开关**回弹保持【自动】**；「自动」标签**仍绿**（不残留手动色）；日志 `模式切换被拒：执行中 autoMode=0 state=1`；**连点两次各只弹一次**（QSignalBlocker 防递归）★核心 |
| 10.7 | 运行中点「⏸ 暂停」→ Paused → 点开关切【手动】 | 同上弹窗（**同一普通文案**）；日志 `… state=2`；回弹保持自动；【停止】按钮始终可点 |
| 10.8 | ★Fault 放行：制造 Fault（同 8.7 不可达点）→ 状态「⛔ 错误」→ 点开关切【手动】 | **无弹窗、放行**；日志 `模式切换为 手动`；导航全开、视觉页可编辑、自动页五钮全灰；可到手动页点动脱困 ★核心 🔗10.9 |
| 10.9 | 🔗再切回【自动】→ 点「↺ 清报警」 | 切回自动同样**放行**（Fault 不在拦截枚举内）；清报警后启动钮恢复可点 |
| 10.10 | ★单步会话切【自动】强制清理（Q12）：【手动】→ 工艺流程页 → 点「单步执行」停在「● 单步暂停」→ 点开关切【自动】 | 日志 **`[MainWindow] 切自动：强制清理单步会话残留`** → `Stop requested` → **`[ProcessPage] 模式切换强制清理单步会话`**；工艺页状态变 **「■ 已停止（模式切换清理）」**；跳到自动运行页；**无残留 Paused(Step)** ★核心 🔗10.11 |
| 10.11 | 🔗切回【手动】→ 工艺流程页 → 点「单步执行」 | 应**启动新会话**（日志 `Step mode ON`），而**不是** `Next step released` → 证明清理彻底、无脏上下文 ★ |
| 10.12 | 单步等待态（Paused+Step）点「▶ 启动/继续」 | 提示「继续运行…」并**放行下一步**（`resume requested` + stepGo）；「⏸ 暂停」在该态**灰**（`st==Running` 才亮，防误放行）`[TR-073]` |
| 10.13 | ★急停：自动模式运行中 → 点顶栏【急停】 | 日志 `[MainWindow] 全局急停 triggered`；**全轴断使能**（J2/R 舵机失力可手拨动）；自动页状态「⛔ 急停」（红）；提示 **「急停已触发，系统已锁定。请切换至【手动控制】模式，依次执行【全局轴使能】→【一键回零】，回零完成后急停锁定自动解除，方可恢复生产」**；**五钮全灰**；硬刹车（与减速停不同）★核心 🔗10.14 |
| 10.14 | 🔗点开关切【手动】 | **放行无弹窗**（worker 非 Running/Paused）；导航全开；手动页红字 **①「急停已触发，所有轴已断使能。请依次执行：【全局轴使能】→【一键回零】→【回安全位】；回零完成即解除急停锁定（不会自动回安全位，须手动点【回安全位】）」**（#ff5e6b，TR-081 文案修正）🔗10.15 |
| 10.15 | 🔗点「全局轴使能」 | 提示 **②「全局轴使能完成。请执行【一键回零】以解除急停锁定」**（#f7c948）；**切到自动运行页确认五钮仍全灰**（`estopPending_` 未清，锁存不被使能绕过）★ 🔗10.16 |
| 10.16 | 🔗点「一键回零」（enabled=true） | 回零完成 → 手动页绿字 **③「急停锁定已解除（全轴回零完成），可切回【自动】恢复生产」**（#7ed67e）；**不自动发起安全位走位**（急停不清已回零轴 homed → 无上升沿，TR-081 定性，原文本"自动发起"修正）；回零后停在回零位 → **手动点「回安全位」**（先抬 Z 再水平）★全链路 🔗10.17 |
| 10.17 | 🔗安全位走位完成（Idle）→ 切【自动】 | 放行；五钮恢复（启动可点）、状态「⏸ 待机」；点「▶ 启动/继续」能正常跑方案 —— **急停恢复闭环完成**（恢复链 = 使能→回零→**手动**回安全位→切自动，TR-081）★ |
| 10.18 | 已知边界 A：制造 Fault → 按急停 → 切手动 → 使能 → 一键回零 | 绿字③ 出现（锁存解除）；但 `RunSequence` 被 Fault 门禁拒 → **无安全位走位**；需回自动页「↺ 清报警」后再手动「回安全位」。**符合设计即通过，非缺陷** |
| 10.19 | 已知边界 B：配置页取消勾选 enabled → 制造急停 → 切手动 → 使能 → 一键回零 | 绿字③ 出现；**无**安全位走位（与 9.10 一致） |
| 10.20 | 回归：手动模式全流程（点动 / Go / 单轴回零 / 全局断使能） | 与 phase2 阶段 2/4 行为一致，无新增回归 |

> **实测记录（　　　　）**：

---

## 五、验收标准

1. **五钮矩阵**：Idle / Running / Paused / Fault / 非自动模式 / 急停锁存 六种情形下与 `UpdateControlsEnabled` 表完全一致，无"该亮不亮/该灰不灰"。
2. **暂停续走为原地续走**：续走瞬间四轴单调趋近当前点绝对目标，不回退、不重跑整个动作；停止为全轴减速停且**保持使能**（不重新使能即可再启动）。
3. **安全位**：示教数值落盘且两位小数与轴读数一致；手动/自动触发均**先动 Z 到安全位 Z、再动 J1/J2/R**；`enabled=false` 与 `worker not idle` 两个跳过条件均生效；安全位会话不污染工艺页列表选中行。
4. **互锁**：启动默认手动；自动模式白名单 {自动运行, 视觉检测} + 强制跳页 + 视觉页 9 控件只读、操作类仍可用；Running/Paused 弹窗 + 回弹无递归；Fault 放行；单步会话切自动强制清理且无残留。
5. **急停全链路闭环**：急停 → 红字① → 切手动 → 使能 → 黄字② → 回零 → 绿字③ + 自动回安全位 → 切自动 → 可启动；期间五钮始终全灰（锁存不被使能有效绕过）。

## 六、风险与注意事项（真机安全）

1. **关 wifi**（最高优先级）：`MC_Open` 在 wifi 开启时失败（code=-6）；pcIp 须真实网卡 IP 与 192.168.0.1 同网段。
2. **减速停距离不是缺陷**：暂停与「■ 停止」都是 `MC_Stop` 斜坡减速，**J1 高速约 9°**；点位与障碍物须预留该距离。不可接受时后续走 `MC_SetStopDec` 调档，**不要改断脉冲**（开环会惯性滑行丢步）。
3. **续走路径变更**：Resume 重发**当前点绝对目标 MoveAbs**，不经过原轨迹 → 续走前目视确认中途无障碍。
4. **安全位第一段不一定是"抬 Z"**：点 1 = `{当前 J1, 当前 J2, safePos.z, 当前 R}`；若当前 Z 高于安全位 Z（电机坐标 0 最高、向下为负），第一段实为**下降** → **示教安全位时 Z 必须取行程上部的值**，否则从高位触发会撞下方物体。
5. **急停后坐标不可信**：断使能 → J2/R 可能垂落；开环无编码器。**禁止急停后续走/启动**，必须走完整恢复链。
6. 急停期间手远离机构（J2/R 失力可被拨动）。
7. **单一运动源**：安全位会话与自动流程共用 worker（running 门禁互斥），但**手动点动与 worker 并存风险未收口（TR-066）** → 任何 worker 非 Idle 期间禁止在手动页点动/Go/单轴回零。
8. **config 落盘路径陷阱**（§3.3）：示教写输出目录 config.json，重建会覆盖 → 示教后立即备份，阶段 10 前复查 `enabled`。
9. **安全位会话无法软中断**（§0-B）：危险时**只能按顶栏【急停】**，测试时手必须在急停按钮旁。
10. **制造 Fault 只准用"不可达点"**（IK 在运动前失败，无实际运动）；**严禁撞限位/堵转**。
11. 无真实相机：10.3/10.4 只验证控件可编辑性，不验证相机功能。
12. 软限位与夹具/线缆干涉（§3.1）。
13. **日志取证**：判定以 `log/creampuff.log` 为准，UI 提示为辅助；每条用例前记录时间戳，便于定位 `state -> N reason N` 序列。
14. 每阶段验收不通过即停；定位前先读 `TEST_RECORD.md` 防回归（`AGENTS.md` 硬规约）。

## 七、测试记录（执行时填写）

| 阶段 | 测试项 | 结果 | 备注 |
|---|---|---|---|
| 8 | 自动运行页 5 按钮 + 显式状态机（TR-073） | 🟢 | 8.1–8.17 全通过（2026-09-08/09）；过程中发现并修复 TR-076/077 两个回归；8.4 引出循环生产新需求（方案待审核） |
| 9 | 全局安全位（TR-074） |  |  |
| 10 | UI 权限与全局互锁 + 急停恢复全链路（TR-075） |  |  |

## 八、工时估算

| 阶段 | 内容 | 估算 |
|---|---|---|
| 8 | 5 按钮 + 状态机（17 条，含暂停续走与 Fault 制造） | 1 天 |
| 9 | 全局安全位（15 条，含示教与 Z 语义校验） | 0.5 天 |
| 10 | 互锁 + 急停恢复全链路（20 条，含 5 连做） | 1 天 |
| — | 合计 | 2.5 天 |

## 九、执行后回填方式

1. **本文档**：通过用例在编号后标 🟢（`| 8.4 🟢 |`），偏差写 ⚠️、失败写 ❌；每组表格下方 `> **实测记录（2026-09-XX）**：` 逐条写**具体数值/文案/日志串**（禁止"正常""通过"这类不可复核的词）；第七节表填三行。
2. **`TEST_RECORD.md`**：**每组执行完当天即追加**（不要等三组做完，防并行会话抢号）。阶段 8 已记账 **TR-078**（阶段 9/10 执行时取"当前最大号 +1"，当前 TR-078）；禁止重排/复用；结果列写 `🟢 已通过（真机验证，2026-09-0X，详见 doc/test/real_machine_plan_phase3.md 阶段 8）`。
3. **发现的缺陷各开独立 TR**（如 §0-A 文案不符、§0-B 提示不可达、§0-C 无 UI 提示 → **TR-079 起（当前最大 TR-078）**），不要塞进验证记录。
4. **`AGENTS.md` 收口**：三条 TR 追加后，在「2026-09-07 三项落地」三条的 🟢 状态里补「真机验证通过，阶段 8 见 TR-078，其余执行后追加」。
5. 全文引用一律 `TR-###`，**不得用行号**。
