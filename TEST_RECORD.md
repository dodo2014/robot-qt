# CreamPuffRobot 测试追踪台账 (Continuous QA)

| 模块 | 测试场景 / 动作 | 期望结果 | 测试状态 | 踩坑记录 / 备注 |
| :--- | :--- | :--- | :--- | :--- |
| *示例* | *在自动模式下点击手动点动按钮* | *UI必须置灰拦截，且底层拒绝下发指令* | *🟢 已通过* | *防止权限穿透导致撞机* |
| 软限位（HardwareManager/PollTick） | 夹爪点动夹紧惯性冲到 0.2（>limitMax=0）后持续按住「松开」 | 连续反向运动回界内，不再"只能一点点按" | 🟢 已通过 | 根因：PollTick 只按"越界+running"拦截、不看运动方向，反向离开被每 50ms StopJog 打断；修复按 delta 判断方向仅拦截"仍朝越界方向运动"。J1 同源一并受益。真机 2026-08-14 验证通过 |
| 软限位（HardwareManager/PollTick） | 位置 0.2（超上界）时 Go 到 -3（界内目标） | MoveAbs 正常执行，不被立即 StopJog 打断 | 🟢 已通过 | 与上同根因，方向判断修复后放行"朝边界内运动"。真机 2026-08-14 验证通过 |
| 回零（BoPai/HomeAxis） | 夹爪（轴5，axis4/home4）手动页点「回零」 | 找原点后停在零位、坐标归零 | 🟢 已通过 | 配置 `homeDir=1`/`homeSns=0`（低有效）生效，与 J1（homeDir=0/homeSns=1 高有效）方向相反；回零动作到位、坐标归零。真机 2026-08-14 验证通过。Z 轴（axis2）未测 |
| 运动学（Kinematics 重构） | K01 FK/IK 往返还原 2000 组随机（J1∈[-80,80]） | 同构型误差 <1e-6 度/mm | 🟢 已通过 | 独立测试程序 `test_kinematics` 19/19 通过（2026-08-19）。J1 全范围随机会触发 atan2 主值域 ±180° 卷绕解超限被正常拒绝（非数学错误），故采样收窄至实际工作区 |
| 运动学（Kinematics） | K02 超臂展 / K03 原点奇点 / K04 双解+就近 / K05 R 透传 / K06 TCP 偏移 / K07 超限位拒绝 | 拒绝返回 false 不崩溃；双解 Forward 还原；InverseSmart 就近选解；TCP 剥离/叠加互逆 | 🟢 已通过 | 全部断言通过（2026-08-19） |
| 坐标变换（CoordTransform） | C01 单位阵退化 / C02 平移矩阵 / C03 PixelToRobot 内参链路 | 输出=输入；输出=输入+平移(12.3,-5.7,38.1)；像素+深度两步转换正确 | 🟢 已通过 | 手眼矩阵 4×4 row-major；未设内参返回零位（2026-08-19） |
| Home Offset（HardwareManager） | H01 J1 回零 → 逻辑 -102°；J2 回零 → -28°；H02 Go 逻辑 5 → 机械 -107（inverted）→ 回读 5；H03 J2 舵机 Go 5 → 机械 33 → 回读 5；H08 限位区间核对 | 逻辑=机械-offset 全链路一致；限位恒用逻辑坐标 | 🟢 已通过 | 驱动测试 24/24 通过（2026-08-19）。Servo HomeAxis 归机械 0 → 逻辑自动为 -offset |
| Home Offset（软限位回归） | H04 点动撞界自动停、Go 越界拒绝、反向放行 | 撞界停在 8 并 StopJog；Go 20 拒绝；Go -50 放行 | 🟢 已通过 | 两个坑：① **SimCard::SetAxisConfig rotation 分支漏 gearRatio**（ppu 错 100 倍，软限位脉冲值错→点动撞界失效），已修 `denom=360*gearRatio`；② **MoveJog 启动边界容差 1e-6 太紧**（撞界停止后位置 7.99996875 浮点舍入），改 0.01 |
| Home Offset 真机（阶段1，2026-08-21） | 1.1 连接+使能；1.3 J1 回零 | 状态点绿、无 Connect failed；J1 回零显示 -102° | 🟢 已通过 | 连接/使能/J1 回零 Home Offset 生效 |
| Home Offset 真机（1.2 位置显示） | 未回零位置显示 | 卡轴 J1 逻辑=机械规划位(0)−102=-102°；J2/R 舵机=遥测真实机械角−offset（显示上电实际角） | 🟢 已通过 | **原测试预期"显示 0"写错已修正**。卡轴未回零=规划位 0→显示 -offset；舵机绝对编码器无回零概念 |
| Home Offset 真机（1.4 J2 回零精度） | J2 回零多次 | 显示 -27.4/-27.2/-27.3（机械停 0.6~0.7°，期望 -28） | 🟡 待复测 | 舵机重复精度+机械负载；金属套松动可能放大，**先紧固复测**。已确认暂不软件补偿（0.6°≈末端 1.7mm 可接受） |
| Home Offset 真机（1.5 R 回零） | R 回零多次 | 显示 0.2°（重复性极好=系统性偏差） | 🟡 待复测 | 可能需紧固后评估加 homeOffset 补偿；暂不处理 |
| SequenceWorker（T5 引擎层） | S01 完整方案 [Move(3点,50%)→Gripper→Vision→Delay→Extrude] 自动运行 | 逐步 actionStarted/Finished 日志、IK 正确、schemeFinished 发出、方案结束仍保持使能 | 🟢 已通过 | 临时驱动测试 17/17 通过（2026-08-20）。Move 逐点 MoveAbs 走 HardwareManager 门面（物理单位+Home Offset 已内化）；Extrude 挤出/回抽按绝对目标位置；Gripper 打开/闭合取限位 Max/Min |
| SequenceWorker（S03 Vision 闭环） | Vision 动作走 SimCamera 采帧 + SimAlgo 检测 + CoordTransform 手眼换算 | 输出基座坐标（含手眼平移 12.3,-5.7,38.1）与置信度，日志打印 | 🟢 已通过 | 临时驱动测试验证：采帧→检测→CameraToRobot 链路完整（2026-08-20） |
| SequenceWorker（S04 单步） | SetStepMode(true) 后运行，每动作完成后挂起，NextStep 唤醒 | 执行完首动作后 IsPaused=true；5 次 NextStep 后全部执行完毕 | 🟢 已通过 | 5 个动作需 5 次 NextStep 释放（2026-08-20） |
| SequenceWorker（S05/S06 中断） | 运行中 Stop() / EmergencyStop() | Stop 安全停止当前动作后 interrupted（保持使能）；EmergencyStop 中断+断使能（IsGlobalEnabled=false） | 🟢 已通过 | Worker 移入独立 QThread（moveToThread），运行中主线程事件循环不卡（S08） |
| SequenceWorker（S07 使能门禁） | 未使能时 RunSequence | 拒绝执行 + errorOccurred 提示，不发运动指令 | 🟢 已通过 | 与手动页共用 HardwareManager::IsGlobalEnabled 单点门禁（2026-08-20） |
| PickCycleController（T6 重构） | 删除 SetHardware 旧接口与硬编码卡轴号 0/2/3，全走 HardwareManager 门面 | 编译通过；MoveJointsSequential 按逻辑轴 J1/J2/Z/R 移动并等待到位；保留状态机/使能门禁/视觉抓取闭环模板 | 🟢 已通过 | 引擎层改造完成，作为视觉抓取单周期模板保留供 SequenceWorker 的 Vision 动作委托复用（2026-08-20） |
| UI 接线（T7 SequenceWorker 线程） | MainWindow 创建 QThread+SequenceWorker，moveToThread，aboutToQuit/析构 quit+wait | 启动存活、退出无挂起；GetSequenceWorker 供页面注入 | 🟢 已通过 | Debug 编译+仿真冒烟通过（2026-08-20）。线程 finished→deleteLater 自动回收 |
| UI 接线（T8 AutoRunPage） | 方案下拉 + 5 按钮（启动=RunSequence/复位=HomeAll/停止=Stop/初始化=Initialize/急停=EmergencyStop）+ 坐标 FK 面板 + 日志框 + 双相机 QLabel | 未使能启动被拒并提示；坐标面板随 stateUpdated 实时 FK 刷新；logMessage/errorOccurred 追加日志框；frameReady 渲染双画面 | 🟢 已通过 | 编译+冒烟通过（2026-08-20）；完整 GUI 联测待做。DrawOverlays 为原地修改返回 void（曾误当返回值） |
| UI 接线（T9 示教读取） | ProcessPage「读取当前坐标(示教)」：GetPosition→Kinematics::Forward→表格新增行 | 未使能弹窗拒绝；使能后新增行 X/Y/Z=FK 结果、R=当前角，自动命名 点_XXX 并选中 | 🟢 已通过 | 编译通过（2026-08-20）；GUI 实测待做 |
| UI 接线（T10 参数喂入） | ConfigPage 运动学(L1/L2/Z0/h1)/TCP(XYZ) 编辑 → paramsChanged 信号 → ReloadFromConfig | 编辑保存后 SequenceWorker 内部 Kinematics/CoordTransform 同步刷新，下次 RunSequence 用新参数 | 🟢 已通过 | makeCoordWidget 外层 lambda 必须捕获 this（内层 lambda 捕 this 报 C3493）（2026-08-20） |
| UI 接线评审修复（2026-08-21，代码评审 D1-D6） | ① 方案下拉时序：ProcessManager::load() 提前到 MainWindow SetupUI 前 + AutoRunPage showEvent 刷新下拉；② 方案失败后启动按钮恢复（OnError setEnabled+状态标签）；③ ReloadFromConfig 加 running 门禁（运行中跳过，防跨线程数据竞争 UB）；④ 退出路径 ShutdownWorker（先 Stop 置 cancel + quit + wait(20)/processEvents 循环 + terminate 兜底）；⑤ RefreshCoordPanel 改用 stateUpdated/servoStateUpdated 缓存关节位（避免 50ms 实时读舵机串口）+ Kinematics 成员缓存（参数变化才重建，消除 60 条/秒日志刷屏） | ① 下拉不再永远"（无方案）"，增删方案切页自动同步；② errorOccurred 后按钮/状态恢复；③ 运行中改参数跳过重载不发运动；④ 退出不挂死（进程 10s 内退出、crash.txt 为空）；⑤ 真机不再 40 次/秒阻塞串口 | 🟢 已通过 | Debug+Release 编译通过；GUI 完整联测待真机验证（2026-08-21）。评审发现的 errorOccurred 置灰、下拉时序 bug 已代码核实属实 |
| UI 接线评审修复（D7-D17，2026-08-21） | D7 OnFrameReady 空结果不再深拷贝+空跑 DrawOverlays（双标签共享同一 QPixmap）；D8 OnTeachRead push 进 action.points+save（对齐 OnAddPoint）+ 校验动作类型为 Move；D9 方案增删后下拉同步（showEvent 刷新已覆盖）；D10 HomeAll/Initialize 返回值检查+失败提示；D11 急停无条件先 HardwareManager::EmergencyStop（不依赖 worker）；D12 ConfigPage Tab5 limitMin/Max emit paramsChanged；D13 日志框 document maximumBlockCount(1000)；D14 删 GetSequenceWorker 死代码；D15 删 QTimer 前向声明/QDebug include；D16 抽 KinematicsHelper::FromConfig/ReadConfigParams（消除 AutoRunPage+ProcessPage 两处重复，统一读 toolOffsetY）；D17 wait 魔数/重复 quit 由 D4 ShutdownWorker 覆盖 | Debug+Release 编译通过无警告；Sim 模式冒烟 7s 存活无崩溃 | 🟢 已通过 | 真机联测待做（2026-08-21）。KinematicsHelper 放 UI 层（Core 不依赖 Config，遵守分层）；SequenceWorker::ReloadFromConfig 为超集（含手眼矩阵+限位）保持独立 |
| 手动页回零状态提示（UI 层，2026-08-24） | 单轴回零（舵机 J2/R 等）：点击回零→显示"轴X 回零中..."，超时(5s)后显示"回零完成"；一键回零全部完成后显示"回零完成"；回零中点停止→显示"回零已停止"；状态点回零中显示紫色"回零中" | 提示不再卡死"回零中"；jog/Go 拦截仍由 HardwareManager homingActive_(5s 超时) 负责，超时后自动解阻、无需点停止 | 🟢 已通过 | 根因：原 homingPending_(bool) 仅一键回零置位、且 OnStateUpdated 只检查卡轴 homeDone，单轴回零与舵机轴提示永不更新。改为每轴 homingAxes_ 向量，由 axisMoveFinished 信号（卡轴/舵机轴超时统一发出）清除并显示"回零完成"，OnStopAxis 中止时清除。保留 5s 超时策略、不改用约等于判定（轴误差暂保留）。Debug+Release 编译通过，ManualControlPage.cpp 无新警告 |