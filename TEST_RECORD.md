# CreamPuffRobot 测试追踪台账 (Continuous QA)

> **编号规则（`TR-###`，2026-09-03 起）**：首列为唯一编号，按追加顺序递增（当前 TR-001 ~ TR-096）。
> - 追加新记录时取「当前最大号 + 1」，**禁止重排、禁止复用**已删除记录的编号。
> - 台账内部与跨文件（AGENTS.md / doc/ / 代码注释）引用**一律用 `TR-###`，不得用行号**——行号随记录增删漂移。
> - 编号与记录终身绑定：记录内容可调整、状态可更新，编号不变。

| 编号 | 模块 | 测试场景 / 动作 | 期望结果 | 测试状态 | 踩坑记录 / 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| — | *示例*（模板行，勿编号） | *在自动模式下点击手动点动按钮* | *UI必须置灰拦截，且底层拒绝下发指令* | *🟢 已通过* | *防止权限穿透导致撞机* |
| TR-001 | 软限位（HardwareManager/PollTick） | 夹爪点动夹紧惯性冲到 0.2（>limitMax=0）后持续按住「松开」 | 连续反向运动回界内，不再"只能一点点按" | 🟢 已通过 | 根因：PollTick 只按"越界+running"拦截、不看运动方向，反向离开被每 50ms StopJog 打断；修复按 delta 判断方向仅拦截"仍朝越界方向运动"。J1 同源一并受益。真机 2026-08-14 验证通过 |
| TR-002 | 软限位（HardwareManager/PollTick） | 位置 0.2（超上界）时 Go 到 -3（界内目标） | MoveAbs 正常执行，不被立即 StopJog 打断 | 🟢 已通过 | 与上同根因，方向判断修复后放行"朝边界内运动"。真机 2026-08-14 验证通过 |
| TR-003 | 回零（BoPai/HomeAxis） | 夹爪（轴5，axis4/home4）手动页点「回零」 | 找原点后停在零位、坐标归零 | 🟢 已通过 | 配置 `homeDir=1`/`homeSns=0`（低有效）生效，与 J1（homeDir=0/homeSns=1 高有效）方向相反；回零动作到位、坐标归零。真机 2026-08-14 验证通过。Z 轴（axis2）未测 |
| TR-004 | 运动学（Kinematics 重构） | K01 FK/IK 往返还原 2000 组随机（J1∈[-80,80]） | 同构型误差 <1e-6 度/mm | 🟢 已通过 | 独立测试程序 `test_kinematics` 19/19 通过（2026-08-19）。J1 全范围随机会触发 atan2 主值域 ±180° 卷绕解超限被正常拒绝（非数学错误），故采样收窄至实际工作区 |
| TR-005 | 运动学（Kinematics） | K02 超臂展 / K03 原点奇点 / K04 双解+就近 / K05 R 透传 / K06 TCP 偏移 / K07 超限位拒绝 | 拒绝返回 false 不崩溃；双解 Forward 还原；InverseSmart 就近选解；TCP 剥离/叠加互逆 | 🟢 已通过 | 全部断言通过（2026-08-19） |
| TR-006 | 坐标变换（CoordTransform） | C01 单位阵退化 / C02 平移矩阵 / C03 PixelToRobot 内参链路 | 输出=输入；输出=输入+平移(12.3,-5.7,38.1)；像素+深度两步转换正确 | 🟢 已通过 | 手眼矩阵 4×4 row-major；未设内参返回零位（2026-08-19） |
| TR-007 | Home Offset（HardwareManager） | H01 J1 回零 → 逻辑 -102°；J2 回零 → -28°；H02 Go 逻辑 5 → 机械 -107（inverted）→ 回读 5；H03 J2 舵机 Go 5 → 机械 33 → 回读 5；H08 限位区间核对 | 逻辑=机械-offset 全链路一致；限位恒用逻辑坐标 | 🟢 已通过 | 驱动测试 24/24 通过（2026-08-19）。Servo HomeAxis 归机械 0 → 逻辑自动为 -offset |
| TR-008 | Home Offset（软限位回归） | H04 点动撞界自动停、Go 越界拒绝、反向放行 | 撞界停在 8 并 StopJog；Go 20 拒绝；Go -50 放行 | 🟢 已通过 | 两个坑：① **SimCard::SetAxisConfig rotation 分支漏 gearRatio**（ppu 错 100 倍，软限位脉冲值错→点动撞界失效），已修 `denom=360*gearRatio`；② **MoveJog 启动边界容差 1e-6 太紧**（撞界停止后位置 7.99996875 浮点舍入），改 0.01 |
| TR-009 | Home Offset 真机（阶段1，2026-08-21） | 1.1 连接+使能；1.3 J1 回零 | 状态点绿、无 Connect failed；J1 回零显示 -102° | 🟢 已通过 | 连接/使能/J1 回零 Home Offset 生效 |
| TR-010 | Home Offset 真机（1.2 位置显示） | 未回零位置显示 | 卡轴 J1 逻辑=机械规划位(0)−102=-102°；J2/R 舵机=遥测真实机械角−offset（显示上电实际角） | 🟢 已通过 | **原测试预期"显示 0"写错已修正**。卡轴未回零=规划位 0→显示 -offset；舵机绝对编码器无回零概念 |
| TR-011 | Home Offset 真机（1.4 J2 回零精度） | J2 回零多次 | 显示 -27.4/-27.2/-27.3（机械停 0.6~0.7°，期望 -28） | 🟡 待复测 | 舵机重复精度+机械负载；金属套松动可能放大，**先紧固复测**。已确认暂不软件补偿（0.6°≈末端 1.7mm 可接受） |
| TR-012 | Home Offset 真机（1.5 R 回零） | R 回零多次 | 显示 0.2°（重复性极好=系统性偏差） | 🟡 待复测 | 可能需紧固后评估加 homeOffset 补偿；暂不处理 |
| TR-013 | SequenceWorker（T5 引擎层） | S01 完整方案 [Move(3点,50%)→Gripper→Vision→Delay→Extrude] 自动运行 | 逐步 actionStarted/Finished 日志、IK 正确、schemeFinished 发出、方案结束仍保持使能 | 🟢 已通过 | 临时驱动测试 17/17 通过（2026-08-20）。Move 逐点 MoveAbs 走 HardwareManager 门面（物理单位+Home Offset 已内化）；Extrude 挤出/回抽按绝对目标位置；Gripper 打开/闭合取限位 Max/Min |
| TR-014 | SequenceWorker（S03 Vision 闭环） | Vision 动作走 SimCamera 采帧 + SimAlgo 检测 + CoordTransform 手眼换算 | 输出基座坐标（含手眼平移 12.3,-5.7,38.1）与置信度，日志打印 | 🟢 已通过 | 临时驱动测试验证：采帧→检测→CameraToRobot 链路完整（2026-08-20） |
| TR-015 | SequenceWorker（S04 单步） | SetStepMode(true) 后运行，每动作完成后挂起，NextStep 唤醒 | 执行完首动作后 IsPaused=true；5 次 NextStep 后全部执行完毕 | 🟢 已通过 | 5 个动作需 5 次 NextStep 释放（2026-08-20） |
| TR-016 | SequenceWorker（S05/S06 中断） | 运行中 Stop() / EmergencyStop() | Stop 安全停止当前动作后 interrupted（保持使能）；EmergencyStop 中断+断使能（IsGlobalEnabled=false） | 🟢 已通过 | Worker 移入独立 QThread（moveToThread），运行中主线程事件循环不卡（S08） |
| TR-017 | SequenceWorker（S07 使能门禁） | 未使能时 RunSequence | 拒绝执行 + errorOccurred 提示，不发运动指令 | 🟢 已通过 | 与手动页共用 HardwareManager::IsGlobalEnabled 单点门禁（2026-08-20） |
| TR-018 | PickCycleController（T6 重构） | 删除 SetHardware 旧接口与硬编码卡轴号 0/2/3，全走 HardwareManager 门面 | 编译通过；MoveJointsSequential 按逻辑轴 J1/J2/Z/R 移动并等待到位；保留状态机/使能门禁/视觉抓取闭环模板 | 🟢 已通过 | 引擎层改造完成，作为视觉抓取单周期模板保留供 SequenceWorker 的 Vision 动作委托复用（2026-08-20） |
| TR-019 | UI 接线（T7 SequenceWorker 线程） | MainWindow 创建 QThread+SequenceWorker，moveToThread，aboutToQuit/析构 quit+wait | 启动存活、退出无挂起；GetSequenceWorker 供页面注入 | 🟢 已通过 | Debug 编译+仿真冒烟通过（2026-08-20）。线程 finished→deleteLater 自动回收 |
| TR-020 | UI 接线（T8 AutoRunPage） | 方案下拉 + 5 按钮（启动=RunSequence/复位=HomeAll/停止=Stop/初始化=Initialize/急停=EmergencyStop）+ 坐标 FK 面板 + 日志框 + 双相机 QLabel | 未使能启动被拒并提示；坐标面板随 stateUpdated 实时 FK 刷新；logMessage/errorOccurred 追加日志框；frameReady 渲染双画面 | 🟢 已通过 | 编译+冒烟通过（2026-08-20）；完整 GUI 联测待做。DrawOverlays 为原地修改返回 void（曾误当返回值） |
| TR-021 | UI 接线（T9 示教读取） | ProcessPage「读取当前坐标(示教)」：GetPosition→Kinematics::Forward→表格新增行 | 未使能弹窗拒绝；使能后新增行 X/Y/Z=FK 结果、R=当前角，自动命名 点_XXX 并选中 | 🟢 已通过 | 编译通过（2026-08-20）；GUI 实测待做 |
| TR-022 | UI 接线（T10 参数喂入） | ConfigPage 运动学(L1/L2/Z0/h1)/TCP(XYZ) 编辑 → paramsChanged 信号 → ReloadFromConfig | 编辑保存后 SequenceWorker 内部 Kinematics/CoordTransform 同步刷新，下次 RunSequence 用新参数 | 🟢 已通过 | makeCoordWidget 外层 lambda 必须捕获 this（内层 lambda 捕 this 报 C3493）（2026-08-20） |
| TR-023 | UI 接线评审修复（2026-08-21，代码评审 D1-D6） | ① 方案下拉时序：ProcessManager::load() 提前到 MainWindow SetupUI 前 + AutoRunPage showEvent 刷新下拉；② 方案失败后启动按钮恢复（OnError setEnabled+状态标签）；③ ReloadFromConfig 加 running 门禁（运行中跳过，防跨线程数据竞争 UB）；④ 退出路径 ShutdownWorker（先 Stop 置 cancel + quit + wait(20)/processEvents 循环 + terminate 兜底）；⑤ RefreshCoordPanel 改用 stateUpdated/servoStateUpdated 缓存关节位（避免 50ms 实时读舵机串口）+ Kinematics 成员缓存（参数变化才重建，消除 60 条/秒日志刷屏） | ① 下拉不再永远"（无方案）"，增删方案切页自动同步；② errorOccurred 后按钮/状态恢复；③ 运行中改参数跳过重载不发运动；④ 退出不挂死（进程 10s 内退出、crash.txt 为空）；⑤ 真机不再 40 次/秒阻塞串口 | 🟢 已通过 | Debug+Release 编译通过；GUI 完整联测待真机验证（2026-08-21）。评审发现的 errorOccurred 置灰、下拉时序 bug 已代码核实属实 |
| TR-024 | UI 接线评审修复（D7-D17，2026-08-21） | D7 OnFrameReady 空结果不再深拷贝+空跑 DrawOverlays（双标签共享同一 QPixmap）；D8 OnTeachRead push 进 action.points+save（对齐 OnAddPoint）+ 校验动作类型为 Move；D9 方案增删后下拉同步（showEvent 刷新已覆盖）；D10 HomeAll/Initialize 返回值检查+失败提示；D11 急停无条件先 HardwareManager::EmergencyStop（不依赖 worker）；D12 ConfigPage Tab5 limitMin/Max emit paramsChanged；D13 日志框 document maximumBlockCount(1000)；D14 删 GetSequenceWorker 死代码；D15 删 QTimer 前向声明/QDebug include；D16 抽 KinematicsHelper::FromConfig/ReadConfigParams（消除 AutoRunPage+ProcessPage 两处重复，统一读 toolOffsetY）；D17 wait 魔数/重复 quit 由 D4 ShutdownWorker 覆盖 | Debug+Release 编译通过无警告；Sim 模式冒烟 7s 存活无崩溃 | 🟢 已通过 | 真机联测待做（2026-08-21）。KinematicsHelper 放 UI 层（Core 不依赖 Config，遵守分层）；SequenceWorker::ReloadFromConfig 为超集（含手眼矩阵+限位）保持独立 |
| TR-025 | 手动页回零状态提示（UI 层，2026-08-24） | 单轴回零（舵机 J2/R 等）：点击回零→显示"轴X 回零中..."，超时(5s)后显示"回零完成"；一键回零全部完成后显示"回零完成"；回零中点停止→显示"回零已停止"；状态点回零中显示紫色"回零中" | 提示不再卡死"回零中"；jog/Go 拦截仍由 HardwareManager homingActive_(5s 超时) 负责，超时后自动解阻、无需点停止 | 🟢 已通过 | 根因：原 homingPending_(bool) 仅一键回零置位、且 OnStateUpdated 只检查卡轴 homeDone，单轴回零与舵机轴提示永不更新。改为每轴 homingAxes_ 向量，由 axisMoveFinished 信号（卡轴/舵机轴超时统一发出）清除并显示"回零完成"，OnStopAxis 中止时清除。保留 5s 超时策略、不改用约等于判定（轴误差暂保留）。Debug+Release 编译通过，ManualControlPage.cpp 无新警告 |
| TR-026 | 连接状态分段着色（2026-08-25） | 手动控制页急停旁「运动卡/舵机」连接状态：未连接改黄色 #e0a520、已连接绿色 #7ed67e | 任一未连接时对应段独立显示黄色，不再整条误显绿色；分隔符与基础色中性浅蓝 | 🟢 已通过 | 根因：原实现 (cardOk \|\| servoOk) 整条统一着色。改 QLabel 富文本 span 分段着色 + setTextFormat(RichText)。Debug+Release 编译通过、冒烟存活；**真机复测通过（2026-08-25）：未连接段显示黄色** |
| TR-027 | 全局使能连接门禁（2026-08-25） | 运动卡/舵机 COM 任一未连接时点「全局轴使能」 | EnableAll 入口拒绝（HardwareManager.cpp 连接门禁），axisEnabled_ 保持全 false → 状态灯均显示未使能；提示「未连接硬件，命令可能无效」（警示黄） | 🟢 已通过 | 根因：原 EnableAll 只判指针非空（if (motionCard_)），未连接时底层 EnableAxis 仍返回 true → 状态灯误绿；UI 判据 card \|\| servo 改 AND。DisableAll 不设门禁（断使能属安全操作）。SimCard/SimServo IsConnected 为 Connect 后置位，仿真链路不受影响。Debug+Release 编译通过、冒烟存活。注：本行与上两行曾因并发写回丢失，已补录。**真机复测通过（2026-08-25）：断开网线/舵机 COM 点「全局轴使能」→ 状态灯保持未使能 + 黄色提示；恢复连接后使能流程正常** |
| TR-028 | 舵机软件掉线防误判 + 重连引导（2026-08-25）| 真机设备运行中舵机"掉线"→1s 全量重连→重连后点动被门禁拦（需断使能+再使能恢复） | 三层加固：① Transaction 响应滑动对齐（脏字节逐字丢弃）+ 校验失败不放弃继续等帧；② ReadTelemetry 失败自动重试 1 次、超时 40→60ms；③ 离线阈值 1s→2s 且重连前 Ping 门卫确认，Ping 通仅清计数不重连。重连后 UI 边沿提示「舵机已重连（扭矩已释放），请重新执行全局轴使能」 | 🟢 已通过 | 根因：单次查询失败即判 online=false（40ms 超时/坏帧即失败），EMI/USB 抖动被放大成离线；且响应解析遇脏字节永不丢弃必超时。重连后无法点动属门禁正确行为（扭矩丢失须人工重新使能），不做自动使能（违背先使能再运动设计）。IAxisServo 新增 Ping() 接口（SimServo 返回 connected_）。真机复测关注：运行中不再频繁出现 reconnect 日志 |
| TR-029 | 真机联调阶段 2（2026-08-25）| 2.1 J1 点动撞界/反向放行；2.2 J1 Go 逻辑 5 度；2.3 越界拒绝；2.4 J2 舵机点动；2.5 J2 Go 逻辑 5 度；2.6 R 点动/Go；2.7 夹爪点动 | 撞 +8 自动停+橙色提示、Go 5 下发机械 107 回读 5（H02）、9 拒绝（H08）、J2/R/夹爪方向与逻辑角全部正确 | 🟢 已通过 | 遗留：① Z 轴待硬件调试完成后测（calibrationPending=true 未标定）；② J2 点动顿挫依然存在（用户决定暂不处理，疑节流阈值/匀速模式与负载匹配）；③ J2 位置显示小误差（可接受）。Home Offset 逻辑坐标全链路真机验证完成 |
| TR-030 | 舵机重连指数退避 + 日志降噪（2026-08-26）| 真机 COM3 不可用（USB 转串口 Open port failed）时无退避疯狂重连：80 分钟 1646 次、26320 条"串口未打开"日志 | 重连改指数退避 2s→4s→…→30s cap（成功归零）；ReconnectServos 失败打原因（GetLastError）不误导；ReadTelemetry 仅 online→offline 边沿打 WARN（onlineWarned_ 去重） | 🟢 已通过 | 根因：程序启动时 COM3 就打不开（CreateFileA 失败，USB 设备不可用/被占用/驱动异常），非代码逻辑 bug；此前无退避导致刷爆日志。修复后不可用期间约 30s 重试一次。真正解决需现场查 USB 转串口线/供电/占用。Debug+Release 编译通过、冒烟存活（2026-08-26）|
| TR-031 | 真机联调阶段 3 Z 标定（2026-08-26）| 3.1 低速点动标定；3.2 行程实测；3.3 软限位；3.4 标定收尾 + Gemini 五步验证（参数/方向回零/精度实测/软限位探底/3D 坐标系验收） | 全部通过：Go -100 实测精确 100mm；回零 Z 显示 165.0mm、Go -165 尖端触桌面显示 0.0mm（Z0=470/h1=175/tcpDown=130） | 🟢 已通过 | Z0 基准、h1 大臂落差标定完成，calibrationPending 可置 false。运动学全链路真机闭环验证通过 |
| TR-032 | 手动页坐标面板 FK 接线（2026-08-26）| 手动控制页全局使能下方 X/Y/Z/R 面板随轴运动实时刷新 | stateUpdated/servoStateUpdated 驱动关节缓存 → KinematicsHelper::FromConfig FK → 富文本更新；参数变化才重建 Kinematics（对齐 AutoRunPage 模式） | 🟢 已通过 | 曾为硬编码假数据（145.23/87.46/32.81/12.50）从不更新。冒烟注意：Start-Process 继承控制台被 shell 回收触发 CTRL_CLOSE 主动退出（exitcode 0xC0000374 伪影非崩溃），须用 -WindowStyle Hidden 独立窗口验证 |
| TR-033 | 回零完成检测保护期（2026-08-26）| 一键回零/单轴回零：J1 未回零压界 -102==limitMin，回零搜索刚启动即报「轴1到达软限位」运动停止、未碰原点开关（昨天 J1/Z 单轴回零偶发同源） | PollTick 回零完成检测加 1s 最短保护期（homeStartedMs_ 发起时刻基准）：发起 1s 内 running=false 不再误判"完成"，homingActive_ 保护不被提前清 → 压界起步的回零搜索不被软限位 StopJog 误杀 | 🟢 已通过 | 根因链：① MC_HomeStart 到卡端 running 置位有启动间隙（含 Start=1 的 HomeStop+150ms 重试窗口）；② line961 单拍判定 running=false 即清 homingActive_；③ inverted 轴回零搜索方向恰朝越界侧（delta<0）→ 拦截条件全满足。挤出轴6 同源（位置恒 0 压 limitMin=0）。Debug+Release 编译通过、独立窗口冒烟存活；待真机复测一键回零 |
| TR-034 | 回零中点动误中断门禁（jogInProgress_，2026-08-28 重做）| 回零进行中按同轴点动键再松开 | StopJog 仅 `jogInProgress_ && jogAxis_==axis` 才执行停止+清保护；回零中松点动键不再打断回零（MoveJog 被回零门禁拒后 OnJogStop 不再误停） | 🟢 已通过 | 2026-08-26 晚 reset 丢弃的已验证修复，已按 worklog 重做。置位：MoveJog 卡轴/舵机成功分支；复位：StopJog 放行/JogTick 撞限/PollTick 撞界/HomeAxis/StopAxis/DisableAll/EmergencyStop/ReconnectServos/MoveAbs/CheckAxisBusy 超时。曾根因：回零中松键 OnJogStop→StopJog 无条件 servo->Stop()+清 homingActive_（日志 16:41:47 J2 实证）。**真机复测通过（2026-08-28）** |
| TR-035 | 急停状态残留清除（ResetAxisStates，2026-08-28 重做）| 轴停在软限位边界 → 按急停 → 观察状态点 | 急停后状态点显示灰色"未使能"（不再残留橙色"限位"） | 🟢 已通过 | 根因：RefreshStatusDot 优先级 alarm→限位→回零→运行→使能→未使能，"限位"高于"未使能"；急停后使能复位但 UI limitState_/softLimitDir_ 残留（曾复现轴1/3 误显示"限位"）。ResetAxisStates 全清状态数组并刷新，急停 lambda 调用。**真机复测通过（2026-08-28）** |
| TR-036 | 回零 post-start 竞态 + Z reject（BoPaiCard，2026-08-28 重做）| 一键回零多轴顺序发起；Z homeMaxDis=0 假完成 | post-start 等待 running 置位（≤200ms/50ms 步进），running 起来→正常；始终不置位且无 homeDone/homeSwitch → reject（MC_HomeStop+MC_Stop 收尾 HOME 状态机）返回 false | 🟢 已通过 | 曾 Bug1：立即单拍 reject 误杀所有轴（一键回零后 J1/Z/夹爪/挤出没反应）→ 现先等待 running 再判；b9c7ed3 原为"只 WARN 不 reject"（缺 Z 假完成拦截与 UI 提示）。Z home 参数（maxDis=2500000）已在 b9c7ed3 config 固化。**真机复测通过（2026-08-28）** |
| TR-037 | 一键回零未启动轴提示（UI，2026-08-28 重做）| 一键回零部分轴被拒 | 提示「回零中...（轴X 未启动）」逐轴列出被拒轴（报警/忙/卡端 reject） | 🟢 已通过 | OnGlobalHome 收集 HomeAxis 返回 false 的轴拼入提示；全部启动仍为"回零中..."、全被拒仍为"回零被拒（请先使能或检查轴状态）"。**真机复测通过（2026-08-28）** |
| TR-038 | 舵机重连抖动循环治理（成功冷却+离线降频，2026-08-28）| 设备"半死"（时通时断）时：重连成功归零 → 1-2s 后又 offline → 立即再重连。日志实证 10:02-10:03 一分钟 8+ 次全量重连（52 次 Connected to COM3）；离线时遥测 250ms×2 次尝试×60ms 超时占满 UI 线程，急停/全局使能点击排队秒级无响应 | ① 重连成功也冷却 30s（不再归零 0），重连硬上限 2 次/分钟；② 离线时遥测降频 250ms→1s；③ 已离线 ReadTelemetry 单次尝试（不重试）；④ Ping ok skip 退避 2s→4s→8s→15s；⑤ 删 XRServo 重连纯噪声 INFO 2 条（Reusing shared handle/Servo id set to） | 🟡 暂不专项验证（2026-08-28 决定后续观察） | 指数退避本身未失效，只覆盖"连续失败"；"反复成功-失败"抖动靠成功冷却钳制（重连的 CloseHandle→CreateFileA→DTR 翻转本身扰动总线，越连越抖）。在线后遥测查询成功自动恢复 online 不依赖重连；仅句柄失效（拔 USB）需重连，冷却最多延迟恢复 30s。Debug+Release 编译通过、Sim 冒烟存活 6s。**后续硬件定性（2026-08-28）：官方 FashionStar 测试软件也连不上 J2（id=0）、断电恢复、通电 24h+ → 根因在舵机侧状态类故障，本软件修复（冷却+降频）在死机期依然正确必要** |
| TR-039 | PollTick 函数级拆分 P1（2026-08-28）| HardwareManager::PollTick 186 行拆为 PollCardAxis()（卡轴：状态映射/软限位方向拦截/回零完成保护期/报警边沿/异常签名）+ PollServoTelemetry()（舵机：离线降频遥测+热重连退避），PollTick 缩为 3 行骨架 | 纯重排零行为变化：成员/执行顺序/日志/信号完全不变；Debug+Release 编译通过、Sim 冒烟存活 6s | 🟢 已通过 | 架构梳理结论落盘 doc/architecture.md：门面（门禁/Home Offset 单点收口）不可再拆；P2 ServoSupervisor、P3 JogController 待阶段 5 真机验证通过后执行。教训：函数头替换后原 if 闭括号会多一层，编译器兜住（C2059）即改 |
| TR-040 | 全局急停升舱 + 业务停止就近（UI 重构，2026-08-28）| MainWindow 顶栏新增圆形红色全局急停（HardwareManager::EmergencyStop + SequenceWorker::EmergencyStop 判空调用）；AutoRunPage/ManualControlPage 删除各自急停按钮；HardwareManager 新增 emergencyStopTriggered 信号（EmergencyStop 内 emit）驱动各页状态清理；ProcessPage「单步执行」右侧「停止」按钮已接 SequenceWorker::Stop（业务停止就近） | 全局急停语义与原 AutoRunPage 急停一致：硬件断使能 + worker 中断 + 各页状态复位（手动页状态点清空+提示、自动页恢复启动按钮+状态标签）；急停行为单点化（唯一触发点 MainWindow 顶栏）；ProcessPage 停止为业务停止（保持使能） | 🟢 已通过（5.4 + 5.12 两项均已真机验证） | 与全局急停语义区分（停止=业务、急停=安全）。5.4/5.12 操作路径改为顶栏全局急停；AutoRunPage 5 按钮变 4、手动页顶部 4 按钮变 3。Debug+Release 编译通过、Sim 冒烟存活 6s。**真机复测：5.4 ✅（2026-09-03 用户确认）**——单步执行/单步暂停中点顶栏急停：断使能 + worker 中断 + 单步会话经 emergencyStopTriggered 复位 + 状态标签「⏹ 已急停（需重新使能）」；急停只清正在回零中轴、IsSystemHomed 仍 true → 单步按钮不置灰，但点击被 IsGlobalEnabled 门禁静默拦（只写日志不弹窗）。**5.12 ✅ 已测（2026-09-04 真机，5.12 用例）**：自动运行中顶栏急停——EmergencyStop + 断使能 + worker 中断 + 自动页经 emergencyStopTriggered 复位（恢复启动按钮、状态标签「⏹ 已急停需重新使能」），需重新手动使能。与 5.4 单步急停同路径、同语义，单点化触发点一致 |
| TR-041 | ProcessPage 单步执行 UI 接线（SetStepMode+NextStep，2026-08-28）| MainWindow 注入 worker 到 ProcessPage（SetSequenceWorker，镜像 AutoRunPage T7）；OnStepExecute 首点 SetStepMode(true)+RunSequence（跑 action[0] 后 WaitForStep 暂停），后续点 NextStep 释放下一动作；停止按钮→Stop 且重置单步会话；schemeFinished/interrupted/errorOccurred + emergencyStopTriggered 均复位 m_stepActive | 单步语义与引擎层 S04 一致：每动作跑完暂停、逐点释放；未使能/未选方案拒绝；停止/急停/完成四路径单步会话复位，下次重新 RunSequence | 🟢 已通过（真机 2026-09-03 验证通过，2026-09-04 用户确认：5.1-5.5 用例已覆盖） | 引擎逻辑 S04 已验（17/17）；UI 此前是 stub（OnStepExecute/停止仅日志），本次补齐接线。ResetStepSession 经 PMF connect 到私有成员函数（Qt5 支持）。Debug+Release 编译通过、Sim 冒烟存活（点击逐步逻辑需 GUI 手动验证）。**真机确认（2026-09-03，用户 09-04 确认）**：首点 SetStepMode+RunSequence 跑 action[0] 后 WaitForStep 挂起、状态「单步暂停」；后续点 NextStep 释放下一动作、逐点前进；停止/急停/完成四路径单步会话均复位（m_stepActive 清、下次重新 RunSequence），与原 TR-065 单步按钮禁用/恢复路径无退化 |
| TR-042 | SequenceWorker/ProcessPage 选中动作单独执行（RunSingleAction，2026-08-31）| ProcessPage 动作列表按钮行「下移」后新增绿色运行图标「执行选中动作」（仅图标+tooltip）：选中动作列表某行 → 该动作（含全部点位）独立执行一次 | 动作独立跑完：日志 actionStarted/actionFinished/singleActionFinished；按钮执行中禁用、结束/中断/出错三路径恢复；未选动作弹窗「请先选择一个动作」；未使能拒绝 + errorOccurred「轴未使能，请先手动使能」；与单步会话互斥（启动前 ResetStepSession + stepMode.store(false)）；不破坏 S01/S04/S07/D3 既有用例 | 🟢 已通过 | 实施前已评估原计划（quantum-vortex-newton.md）：8 处修正全部属实，补 1 处硬伤（ProcessPage.h 缺 `class QPushButton;` 前置声明，已加）。RunSingleAction 镜像 RunSequence 模式（running 门禁 + InMainThread 使能门禁 + QueuedConnection 派发）；StartSingleExecution 镜像 ExecuteActions（cancel 区分 interrupted/errorOccurred，失败双发 errorOccurred 为既有兜底语义）。Debug+Release 编译通过无警告、Sim 冒烟存活 8s（SimCard/SimServo 全家桶初始化正常）。GUI 点击交互逻辑待真机/手动验证 |
| TR-043 | 运动学 FK 真机验证（Kinematics，2026-08-31）| 真机摆位轴角 J1=0°/J2=0.5°/Z=0/R=179.9（逻辑坐标），界面坐标显示 x=358.68/y=1.92/z=165/r=179.9 | 按 Forward 公式（l2_eff=166.86+53 TCP 前伸=219.86，z=z+z0−h1−tcpDown）手算比对 | 🟢 已通过 | 四轴全对应：x 计算 358.682（差+0.002mm）、y 1.919（差−0.001）、z 165.000（0）、r 179.900（0）——误差均在 0.01mm 显示精度内。物理闭环自洽：y=1.92 恰为等效小臂 219.86×sin(0.5°)（J2 的 0.5° 偏转横向分量）；x=138.83+219.86×cos(0.5°)（大臂沿 +X 伸直 + 小臂近共线 + TCP 前伸 53）；z=0+470−175−130=165（Z 回零位）；R 透传。IK 反解实测坐标得 J1=−0.069°/J2=+0.612°，与实测差 0.07°/0.11° 属坐标显示舍入在近臂展极限处的正常放大（正向为准）。L1=138.83/L2=166.86/TCPf=53 标定在该位姿真机验证通过 |
| TR-044 | 运动学验证程序固化 + 第二组真机摆位（tests/test_kinematics_check，2026-08-31）| ① 第二组真机摆位：J1=−90/J2=−0.1/Z=0/R=180.1 → 界面 x=−0.38/y=−358.69/z=165/r=180.1；② 固化验证程序 `tests/test_kinematics_check.cpp`（root CMake 挂 tests 子目录，独立 target 不进主程序依赖链）| ① FK 与实测四轴全对应（x 差 −0.004mm，y/z/r 精确）；② 程序内置真机摆位回归 + 限位内随机 20 组 FK↔IK 往返自检，Debug+Release 双版本 22/22 ALL PASS | 🟢 已通过 | 用法：无参跑内置用例（退出码 0/1 可脚本调用）；`fk <j1> <j2> <z> <r>` 正解查询、`ik <x> <y> <z> <r> [curJ2]` 逆解查询。参数自动读 config（kinematics.links/tcpCalibration/axes 软限位），改 config 无需改程序。编译：`$env:TARGET='test_kinematics_check'` 后跑 `out\smoke\build_debug.bat` / `build_release.bat`（两者已支持 TARGET 环境变量选 target，默认 CreamPuffRobot 不变）。PowerShell→bat 传参用环境变量（`%~1` 在该链路曾失真）。踩坑：spdlog 无 `levels.h`（set_level 在 spdlog.h）。第二组物理自洽：x=219.86×cos(−90.1°)≈−0.38（J2 −0.1° 引起），y=−(138.83+219.86×sin(90.1°)≈358.69) |
| TR-045 | 运动学 FK 真机验证第三组（Kinematics，2026-08-31）| 真机摆位 J1=0/J2=20.2/Z=0/R=180.1 → 界面 x=345.17/y=75.92/z=165/r=180.1 | FK 计算比对 | 🟢 已通过 | 计算 x=345.167（差−0.003mm）、y=75.917（差−0.003）、z=165（0）、r=180.1（0）——完全对应。至此 0°/−90°/20.2° 三个位姿（伸直/正交/弯折）全部通过，运动学标定验证完备 |
| TR-046 | 工作区边界容差误拒示教点（Kinematics，2026-08-31 修复）| 方案_1 动作[0]「移动到：初始位置」点_001 (358.69, 1.15, 165, 179.8) 示教保存后执行选中动作 → 日志 `IK: target out of workspace, dist=358.69 > L1+L2_eff=358.69` + `IK failed for point` | 示教可达点执行不被误拒 | 🟢 已通过 | 根因：点_001 极径 √(358.69²+1.15²)=358.6918mm，仅超理论最大半径 L1+L2_eff=138.83+219.86=358.69mm 达 0.0018mm（示教坐标显示精度 0.01mm 的舍入+浮点微差），而 IK 边界容差仅 **0.001mm**（注释写"允许 1mm 误差"但代码实际 0.001，注释与代码不符）→ 完全伸直位示教点被误拒。修复：`Kinematics::ikSolve` 内外甜甜圈边界容差 0.001→**0.1mm**（覆盖示教舍入+标定微差，超界解仍受 ValidateJoints 关节限位兜底）。test_kinematics_check 新增固定边界回归用例（358.69,1.15 IK 必须成功），**23/23 ALL PASS**（新用例：IK 得 J(0.184,0.000)，还原误差 1.84e-3mm）。Debug+Release 编译通过、Sim 冒烟存活。真机待复测：重新执行方案_1 动作[0] 应正常走完 3 个点 |
| TR-047 | 示教点 IK 无解第二层原因：R 限位域与角度表示不符（2026-08-31）| 点_001 修复后点_002/点_003 (r=−179.9) 仍报 `IK(Smart): no valid solution` | 双解在限位内（elbow_up 明明合法 J1=0.004/J2=20.19）却无解 | 🟢 已通过 | 根因：示教保存 r=−179.9（cmd 10 ±180 回绕表示），config R 限位为 **[0,180]** 正值域 → `ValidateJoints` 判 r<0 双解全拒。elbow_up 解经 python 精确验证合法（J1=0.004°/J2=20.19°，都在限位内），与姿态无关。用户重新示教点位（R 保存正值）后正常。**教训：R 角度域（±180 回绕）与 R 限位域（[0,180]）须一致**；已建测试用例 ik 模式复现验证。附：用户正确指出示教点带 posture（elbow 确认），MoveToPoint 应尊重示教姿态而非一律 InverseSmart——当前硬编码示教 posture=elbow_up 且执行用 InverseSmart，后续改进项（本次非直接原因，未改） |
| TR-048 | J4 使能 180° 边界翻转一整圈（XRServo TorqueOn，2026-08-31 修复）| 真机 J4 停在 180° 附近，点「全局轴使能」→ J4 沿 180→90→0→−90→−180 翻转一整圈（日志 14:52:32 `SendSetAngle id=1 angle=-179.8 interval=1000ms`）| 使能只锁定不位移 | 🟢 已通过 | 根因：TorqueOn 用 `SET_ANGLE(查询角, 1000ms)` 锁位——cmd 10 角度为 ±180 回绕表示，180° 边界处查询返回 −179.8，舵机从 +180 到 −179.8 计算行程 359.8° → 走一整圈。修复：TorqueOn 改为 `CMD 24 (SendControlModeStop mode=1 停止后保持锁力)`（与 Stop 同款已真机验证的锁定方式），只锁定不位移，杜绝边界回绕。**真机复测通过（2026-08-31 15:3x）：180° 附近使能 J4 不再翻转**。备注：MoveToAngle/MoveAtSpeed 目标本就钳制 ±180，180.1 会被截为 180.0（显示 180.1 与运动目标 180.0 存在 0.1° 显示差，低优先级） |
| TR-049 | 动作运行速度滑条改后不生效（ProcessPage，2026-08-31 修复）| 动作[0] 拖动「动作运行速度」滑条后直接点执行/单步，目测速度无变化 | 拖动速度即生效 | 🟢 已通过 | 根因：速度只在「保存动作」(OnSaveAction :1120-1121) 才写 `action.speedPercent`，拖动滑条只改 UI 控件（spin/slider 双向同步），直接执行读内存旧值。修复：滑条/输入框 `valueChanged` 时经 `ApplySpeedPercentToCurrentAction(v)` **实时写回当前编辑动作的 speedPercent**（改即生效，正式落盘仍由保存按钮负责，两处写同一字段无冲突）。ExecuteMove 的 `speedScale = qBound(0.01, speedPercent/100, 1.0)` 链路本就正确。**真机复测通过（2026-08-31 15:3x）：拖动速度滑条执行，速度随动生效** |
| TR-050 | 示教读取 R 角与手动页显示不一致（XRServo，2026-08-31 修复）| 真机 J4 停在 180.1°：手动页坐标面板显示 180.1，示教读取显示 −179.8（差 360°） | 两处角度来源一致 | 🟢 已通过（真机 2026-09-03 用户确认）| 根因：**cmd 22 (MONITOR) 与 cmd 10 (QUERY_ANGLE) 角度表示不同**——MONITOR 原始角度为 0~360 宽范围（int32，180.1 原样返回），QUERY_ANGLE 为 ±180 有符号回绕（int16，180.1° 被固件表示为 −179.9°）。手动页坐标面板用遥测（cmd 22）、示教读取经 GetPosition→ReadAngle（cmd 10）→ 相差 360。修复：XRServo::QueryMonitor 角度统一 wrap 到 [−180,180]（`>180 → −=360`、`<−180 → +=360`），与 cmd 10 及 R 软限位 [−180,180] 表示一致。修复后同一位置手动页与示教均显示 ≈−179.9（差 0.1° 为读取时刻物理微差）。Debug+Release 编译通过、Sim 冒烟存活（SimServo 不走 XRServo 协议，须真机复测 180° 边界处显示一致）。顺带修复：build_debug.bat/build_release.bat 中文注释致 cmd 乱码（bat 必须纯 ASCII，已改英文注释）。**真机复测通过（2026-09-03 用户确认）：180° 边界处手动页坐标面板与示教读取显示一致（≈±179.9/180，不再相差 360）** |
| TR-051 | 动作点间停顿 7 秒不连贯（XRServo MoveToAngle interval，2026-08-31 修复）| 方案_1 动作[0] 4 个点执行，点间间隔约 7 秒（日志 15:32：点_001 后 7.4s 到点_002、点_003 后 7.1s 到点_004），动作不连贯 | 点间无长时间停顿（仅到位检测 ~几十 ms） | 🟢 已通过 | 根因：`MoveToAngle` 计算行程时目标 179.9 与查询角 −179.9（cmd 10 ±180 回绕，物理实为 180.1°）**表示差 360°** → `dAngle=359.8°` → interval=359.8/50×1000≈**7196ms** → `MarkAxisBusy(7.2s)` → `WaitForAxes` 干等 7 秒（舵机实际 0.2° 早到位）。日志实证 `SendSetAngle id=1 interval=7194/7196ms`（点_001/003/004，每次 J4 目标 179.8~179.9 而 cached/查询 −179.9 时）。修复：XRServo 新增 `Impl::AlignNear(ref, target)`（±360 归一化到目标附近 ±180），`MoveToAngle`（查询角）与 `MoveAtSpeed`（缓存角）行程计算前对齐——interval 从 7196ms → 50ms 级。这是 cmd 10/22 角度表示问题（显示/使能/行程三坑）的第三处。Debug+Release 编译通过、Sim 冒烟存活。**真机复测通过（2026-08-31 15:5x）：4 点连续执行点间不再停顿，动作连贯** |
| TR-052 | 强制开机回零互锁（Homing Interlock，2026-08-31 新增）| 程序启动（未回零）：AutoRun 启动按钮禁用、ProcessPage 单步/执行选中禁用、手动页 Go 被拒（MoveAbs 回零门禁 + 提示「系统未回零，请先执行一键回零」）、JOG 点动放行（脱困）、使能/回零放行；一键回零成功后：homeStateChanged(true) → 全部绝对运动入口激活 | 未回零禁止绝对定位/自动运行，回零后放行；JOG/使能/回零不受限 | 🟢 已通过（真机 2026-09-03 用户确认）| 背景：J1/Z 开环步进断电丢坐标，未回零发绝对坐标可能撞机（急停是事后补救，回零是事前保障；Gemini 建议分析采纳）。实现：`HardwareManager` 新增 `IsSystemHomed()/IsAxisHomed()/homeStateChanged` + `axisHomed_`（无绑定轴恒 true）+ `MarkAxisHomed`（HomeAxis 卡轴/舵机成功分支置位，全轴完成后 emit）；`MoveAbs` 入口加回零门禁（使能之后，JOG/回零不走 MoveAbs 不受限）；`RunSequence/RunSingleAction` 加同门禁（errorOccurred「系统未回零，请先一键回零」）；AutoRunPage 启动按钮初始禁用 + homeStateChanged 联动；ProcessPage 单步（m_stepBtn 提为成员）/执行选中按钮联动 + 两槽内双保险弹窗；ManualControlPage Go 失败提示「系统未回零」。状态语义：程序实例内全轴回零成功后保持 true（断使能/急停不丢坐标不清位）。Debug+Release 编译通过、Sim 冒烟存活。**真机验证通过（2026-09-03 用户确认）：程序启动未回零时 AutoRun 启动/ProcessPage 单步/执行选中全禁、手动页 Go 被拒、JOG 放行，一键回零成功后全部激活** |
| TR-053 | 回零中途急停后系统误判已回零（Homing Interlock 漏洞，2026-08-31 修复）| 真机：启动→全局轴使能→一键回零（中途急停，未完成全部轴回零）→重新使能→手动 Go 成功运动（**错误**：应被「系统未回零」拦截）；预期同时被禁：AutoRun 启动、单步执行、执行选中 | 回零被打断 → 系统未回零 → 绝对定位/自动运行全部拒绝，直至重新完整回零 | 🟢 已通过（真机 2026-09-03 用户确认）| 根因：`HomeAxis` 卡轴/舵机分支在**成功下发回零指令后立即 `MarkAxisHomed`**——回零是异步的（指令即返回 true，实际运动在后台），HomeAll 瞬间把全部绑定轴标记「已回零」；中途急停只清 homingActive_/使能/忙态、**不清 axisHomed_** → `IsSystemHomed()` 恒 true → Go 放行。修复（把「回零完成」从指令下发挪到轴实际到位观测点）：① `HomeAxis` 卡轴/舵机分支**不再置 homed**，只置 homingActive_ + homeStartedMs_；② 卡轴回零完成 → `PollCardAxis` 既有检测点（homingActive_ && !st.running && 过 1s 保护期）置位 `MarkAxisHomed`；③ 舵机回零完成 → `PollServoTelemetry` 新增检测（homingActive_ && 遥测逻辑角 \|angle\|≤1°）置位；④ 新增 `AbortHoming(ai)`：仅复位「正在回零中」的绑定轴 homed（部分完成不算完成，须重新回零），已回零完成的轴（机械未动）保持——接入 EmergencyStop/DisableAll/StopAxis/StopJog 四处打断点；无绑定轴恒 true 不变（轴6 按用户要求暂不涉回零测试）。Debug+Release 编译通过、Sim 冒烟存活。**真机复测通过（2026-09-03 用户确认）：重复用户场景——回零中途急停后重新使能 → Go 被拒「系统未回零」、AutoRun 启动/单步/执行选中全禁；重新完整回零后全部恢复。另本次验证同源确认「强制开机回零互锁」整链路（见 TR-052）** |
| TR-054 | 轴6（挤出）未接电机 → config enabled 豁免（Homing Interlock 配套，2026-08-31）| 轴6 只接运动卡（portId=2）未接电机，回零无意义；若参与 `IsSystemHomed` 判定，真机回零行为不确定（running 复位与否取决于卡）→ 最坏系统锁死（Go/自动运行/单步全禁） | 未接硬件轴不参与回零互锁/使能/回零/运动判定 | 🟢 已通过（编译+Sim 冒烟；真机随回零互锁复测） | 实现：`AxisConfig` 加 `enabled`（默认 true）；`LoadAxisConfigsFromConfig` 读 `axes.Axis_X.enabled`，**enabled=false → `SetBinding(None)`**（显式覆盖，防 AxisMap 默认表兜底回 Card）；`HomeAll` 跳过 None 绑定轴；`IsGlobalEnabled` 既有逻辑本就跳过 None（空轴不参与判定）。config `Axis_Extruder.enabled=false`。后续接电机后改回 `enabled=true` 即恢复参与。**注意：`axisHomed_` 的 None 轴同步评估由 Initialize 第 5 步统一完成（SetBinding 之后 fill+循环），禁止在 LoadAxisConfigsFromConfig 内直接写 axisHomed_[i]**（曾因该处访问未 resize 的 axisHomed_ 启动即崩 QList 越界，见下条）。Debug+Release 编译通过、Sim 冒烟存活 |
| TR-055 | 启动崩溃 QList 越界（enabled 豁免引入，2026-08-31 修复）| 真机启动（Bopai/XRServo）：Initialize 中 `SimCamera Opened` 后断言崩溃 `ASSERT failure in QList::operator[]: index out of range`（qlist.h:518），程序启动即退出 | 启动正常跑完 Initialize | 🟢 已通过（Sim 冒烟日志完整 `Initialize complete`） | 根因：enabled 豁免实现时在 `LoadAxisConfigsFromConfig`（Initialize 第 4 步）绑定循环内加了 `axisHomed_[i]=true`，但 `axisHomed_` 的 resize/fill 发生在**第 5 步**（第 4 步执行时仍是空 QVector<bool>）→ `operator[]` 越界 → Qt 断言崩溃。且该行本就冗余：第 5 步的 `if (Get(i).type != None) axisHomed_[i]=false` 在 SetBinding(None) 之后执行，轴6 自动保持 true。修复：删除该行，由第 5 步统一初始化。教训：**成员容器在 Initialize 分步初始化时，前序步骤禁止访问后续步骤才 resize 的容器**；Sim 冒烟 SMOKE_ALIVE=True 曾因断言框挂起进程误报通过（日志停在相机打开），验证须查日志含 `Initialize complete` |
| TR-056 | 回零完成判定用逻辑角误判 J2 未回零（Homing Interlock 漏洞②，2026-09-01 修复）| 真机：一键回零完成、机械臂已停止运动后，手动页 Go（含 J1 等任意轴）仍提示「系统未回零，禁止绝对定位！请先执行一键回零（点动可脱困）」；日志 `MoveAbs rejected: system not homed (axis 0)`（axis 0 为 Go 目标轴，根因在 J2） | 一键回零完成后 Go 放行并运动 | 🟢 已通过（Debug+Release 编译+Sim 冒烟；真机随回零互锁复测） | 根因：舵机回零完成检测（`PollServoTelemetry::checkServoHomeDone`）用的是**逻辑角**（tJ2/tR 经 `toLogicalAngle` 减 homeOffset 的值），而 J2 的 `homeOffset=28` → 回零到位机械角≈0°、逻辑角≈**−28°** → `\|angle\|≤1°` 永不满足 → J2（axis1）永不 `MarkAxisHomed` → `IsSystemHomed()` 恒 false → 全部绝对定位/自动运行/单步被拒。**与轴6 无关**（axis5 是 None 绑定，`axisHomed_` 恒 true）。日志实证（2026-09-01 10:06–10:09）：卡轴 0/2/4「home done (running cleared)」、舵机 3（R, offset=0）「home done (angle=0.10)」均出现，唯独**轴 1（J2）全程无 home done**。修复：`checkServoHomeDone` 改用**机械角**（tJ2/tR 取 `ReadTelemetry()` 原始值，未减 homeOffset），与 `HomeAxis` 下发 `MoveToAngle(0.0)`（机械角 0）语义一致；UI 仍推逻辑角副本（`toLogicalAngle` 包一层再 push 进 servos）。R 轴 offset=0 两种判定等价不受影响。Debug+Release 编译通过（LNK1168 因 exe 运行，结束进程后重编成功）、Sim 冒烟存活（日志 `Initialize complete`，SimServo 不走 XRServo 协议，回零判定须真机复测）。**真机复测路径：一键回零完成后 J2 显示 ≈−28°、Go 应放行且能运动；回零中途急停打断 → Go 仍被拒（接 2026-08-31 互锁漏洞①）** |
| TR-057 | Tooltip 黑色背景与深色 UI 混淆看不清（UI 样式，2026-09-01 修复）| 工艺流程页动作列表下方按钮（新建/编辑/删除动作/上移/下移，及全局所有 tooltip）鼠标悬停 tip 为黑色背景、与深色 UI 几乎同色，内容看不清 | tooltip 白底深字、清晰可读 | 🟢 已通过（Debug+Release 编译+Sim 冒烟+**沙箱 tipcheck 像素级 + 目视验证通过**；真机待用户目视确认）。**2026-09-01 13:48 三修**：用户用 gemini 调整后仍暗色——gemini 把 `main.cpp` 的 `QToolTip::setPalette()` 整个注释掉、只留无效的 `QToolTip { }` QSS 规则导致回退；已恢复双保险（main.cpp setPalette + QSS `QToolTip, QTipLabel { }`），按用户要求只编译未跑界面校验（exe 13:47 重编）。**2026-09-01 16:23 四修（决定性）**：用户再用 gemini/gpt 修复仍失败——gpt 版只设三组 color group 的 `QToolTip::setPalette()`（无效机制，被 application stylesheet 绕过）且把 QSS 里唯一有效的 `QTipLabel {}` 规则删空。终极修复（三层独立保险，任何一层生效即白底）：① QSS `QToolTip, QTipLabel { }` 规则恢复（MainWindow.cpp:221）；② gpt 的 setPalette 保留（无害兜底）；③ **新增 TipStyleFilter 全局事件过滤器**（MainWindow.cpp:40-64）——tooltip 窗口首次 Polish/Show 时对其【实例】直接 setStyleSheet，实例级样式表优先级高于 application 级，架构上不可能被任何层叠规则覆盖。双编译通过（exe 16:22/16:23），按用户要求未做后台校验 | **三层根因（沙箱 tipcheck v2 四模式像素对比证实，out/smoke/tipcheck/）**——① 应用级 stylesheet（`MainWindow.cpp` `qApp->setStyleSheet`）的 `QWidget { color:#b8cce3 }` 宽泛规则**会**匹配 QTipLabel，把文字染浅蓝；② QSS 的 `QToolTip { ... }` 选择器**不匹配**实际弹出的 `QTipLabel`（私有类名是 QTipLabel/QFrame/QWidget），规则不生效；③ `QToolTip::setPalette()` 在 application stylesheet 环境下**被 QStyleSheetStyle 完全绕过**——tipcheck 验证：nopal 模式 white=26,636、palette 模式（setPalette 生效 ToolTipBase=#ffffff）white 仍=26,636，深色像素 ~96 万 → **setPalette 单独无效**。**唯一真生效机制**是 QSS `QTipLabel { ... }` 规则（QSS 按类名字符串匹配，私有类也命中）——tipcheck qtiplabel 模式 white=639,973（差 24 倍）。**最终修复（双保险）**：`MainWindow.cpp` QSS 增 `QToolTip, QTipLabel { background-color:#ffffff; color:#1a1a1a; border:1px solid #cfd8e0; border-radius:4px; padding:4px 8px; font-size:13px; }` + `main.cpp` `QToolTip::setPalette(白底深字)` 作辅——tipcheck current 模式 white=780,678（全模式最高）。**全模式目视验证**：tipcheck_current.png 显示按钮"hover me (test)"上方**清晰可见白底矩形 tooltip「测试 tooltip：白底深字」**。Sim 冒烟 Release exe 存活、日志含 `Initialize complete`、无回归。改动为纯 QSS 文本 + 6 行 C++（setPalette），风险极低；编译命令已落盘 `doc/compile_guide.md`，沙箱手工方案对 cl.exe 关键 flag（`/Zc:__cplusplus` `/permissive-`）有记录 |
| TR-058 | 夹爪动作行程输入框（工艺流程页，2026-09-02 新增）| 编辑夹爪动作：下拉（张开/闭合）后新增行程输入框（mm），值为轴5 绝对目标坐标（0=夹紧、负=松开，与手动页轴5 Go 同语义）；速度滑条实时映射 `GetMaxSpeed(Gripper)×percent` | ① 输入值存入 `process.json` 新字段 `gripperTarget`（旧文件缺失时按方向补默认：张开 -3.0/闭合 0.0）；② 执行时越界（超出轴5 软限位 -5~0）被 `IsWithinSoftLimits` 拒绝并 `errorOccurred`，绝不静默改写目标；③ 低速时到位超时按 `GetAxisBusyMs` 动态兜底不再误报 | 🟢 已通过（Debug+Release 编译 + Sim 冒烟 SMOKE_INIT_COMPLETE=True + **真机验证通过，2026-09-02 用户确认**） | **行为翻转（重要）**：旧实现 `isGripperOpen ? GetLimitMax(0) : GetLimitMin(-5)` 与物理事实相反（0=夹紧），"打开夹爪"此前实际是夹紧；改后"打开夹爪"= 松开 3mm（正确）。**process.json 里「关闭夹爪」(156 行) 的 isGripperOpen 本就是 true，迁移后默认拿 -3.00（松开），须在界面人工改成 0.00**。坑：QDoubleSpinBox QSS 禁写 font-size（polish 崩溃）改用 setFont；单位用框外 QLabel 禁 setSuffix；回显 combo 须 blockSignals 防新接的 currentIndexChanged 回填默认值覆盖数据；`RefreshActionDetail`/`OnSaveAction` 双时机刷新 spin 范围（实时读 config） |
| TR-059 | R 轴 ±180° 角度环绕：示教 R=-179.9 执行报"目标点不可达"（2026-09-02 修复）| 夹爪垂直向下（R 物理位置在 180° 软限位边界）时，舵机遥测读数在 {179.9, 180, -179.9} 跳动（舵机角度域 [-180,180]，±0.1° 噪声跨 ±180 表示边界）；示教存入 -179.9 后执行选中动作：Kinematics::ValidateJoints 判 r<rMin=0 → "目标点不可达"（MoveAbs 软限位 [0,180] 也会拒）| ① R 读数/显示/示教全链路归一化到限位域内（-179.9→180.1），180 边界处不再跳负值；② 旧方案含 -179.9 的点位不重标直接可执行；③ 无负向软限位误告警 | 🟢 已通过（Debug+Release 编译 + Sim 冒烟 + **真机验证通过，2026-09-02 用户确认**：边界显示稳定、旧数据直接执行） | 修复：HardwareManager 新增 `NormalizeRotationAngle(axis, angle)`（以 [limitMin,limitMax] 中心 wrap ±180 窗口），应用于 PollServoTelemetry::toLogicalAngle、GetPosition Servo 分支、MoveAbs 入口（仅 Servo 轴）、SequenceWorker::MoveToPoint 的 target.r（**必须在 IK 前**——ValidateJoints 先于 MoveAbs 拦截）；下发不额外 wrap（XRServo::MoveToAngle 已有 ±180 硬 clamp，与官方软件一致，180.1→180 物理差 0.1°）；config Axis_R.limitMax 用户已改 180→185（仅软件校验域，舵机物理上限 180 无机械越程）。坑：① checkServoHomeDone 回零判定必须用原始机械角，归一化绝不能掺入该路径；② J2 域 [-90,152] 中心 31 窗口内读数恒等不受影响；③ 卡轴不做归一化（脉冲域连续）；④ axisConfigs_ 快照不热更新但 GetLimitMin/Max 实时读 config，归一化窗口自动跟随 |
| TR-060 | 连接状态标签分段着色 + 自动运行页连接日志（2026-09-02）| 手动控制页顶部「运动卡/舵机」连接状态：旧版 (cardOk||servoOk) 整条同色，任一在线全绿无法区分单侧掉线；且 connectionChanged 仅 Initialize 发一次，舵机离线/热重连后标签卡在旧颜色 | ① 运动卡/舵机各自独立着色（未连接黄 #e0a520 / 已连接绿 #7ed67e），前缀字样与状态同色不暗；② 自动运行页日志框每次连接状态变化（Initialize/舵机离线/重连）刷出「硬件连接状态变更：运动卡: X \| 舵机: X」 \| 🟢 已通过（Debug+Release 编译 + Sim 冒烟 + **真机验证通过，2026-09-02 用户确认**：着色分段清晰、前缀同色、自动运行页日志框正常刷出连接状态变更） \| 修复①：ManualControlPage::OnConnectionChanged 改富文本，整段「运动卡: X」/「舵机: X」各自 span 着色（初版 span 只包状态词导致前缀用默认 palette 发暗，已整段包裹）。修复②根因：AutoRunPage 的 connect 原在 SetSequenceWorker 内（MainWindow 在 HardwareManager::Initialize 之后才调用）→ 错过 Initialize 那次 connectionChanged emit → 启动连接日志丢失；把 stateUpdated/servoStateUpdated/frameReady/connectionChanged 四个 HardwareManager 连接移到 AutoRunPage 构造函数（SetupUI 后），SetSequenceWorker 只留 worker 信号。另加 HardwareManager::PollTick 连接状态边沿检测（connStateInited_/lastCard/lastServo），Initialize 同步初值防首次 tick 重复广播 |
| TR-061 | ProcessPage 单步执行状态可观测性（stateChanged 接线 + 顶栏两行，2026-09-03）| 顶栏由单行改两行：第一行「当前方案: [名称框] [方案下拉] 新增方案 切换方案 删除方案」，第二行「方案调试 单步执行 停止 [状态标签]」；stateChanged/schemeFinished/interrupted/errorOccurred 接到状态标签 | 单步执行有界面状态反馈：● 空闲 / ● 运行中 / ● 单步暂停 / ● 执行选中动作 / ✅ 完成 / ■ 已停止（保持使能）/ ⏹ 已急停（需重新使能）/ ✖ 出错 | 🟢 已通过（Debug+Release 编译 + Sim 冒烟 + 真机 2026-09-03 用户确认 5.1-5.5 全部以此为判定依据）| 根因：**stateChanged 全项目 6 处 emit、0 处 connect**（声明 SequenceWorker.h:71）→ ProcessPage 此前无任何执行状态显示，5.1/5.2 期望的「单步暂停」「✅ 完成」在界面上根本不存在，不改就无法判定。两个实现要点：① **终态保留**——StartExecution 在 emit schemeFinished/interrupted 后还会补发 stateChanged("空闲")（SequenceWorker.cpp:245-250），直接显示会把终态冲掉，故用 m_finalStatus/m_finalColor 保留终态，新会话启动时清空；② **急停与业务停止都发 interrupted**，靠 HardwareManager::IsGlobalEnabled() 区分文案（急停断使能、停止保持使能）。按钮文案「确认切换」→「切换方案」，三个方案按钮移到下拉框之后，第二行行首加「方案调试」文本。槽函数绑定未动，OnConfirmSwitch 逻辑不变 |
| TR-062 | ProcessPage 单步执行真机验证（阶段 5 用例 5.1/5.2/5.4/5.5，2026-09-03）| 真机：5.1「单步执行」首点、5.2 连续点至全部动作结束、5.4 单步中按顶栏全局急停、5.5 未使能时单步执行 | 5.1 只跑 action[0] 后挂起不自动继续；5.2 每点跑一个动作并暂停，结束后 schemeFinished + 会话复位；5.4 断使能 + worker 中断 + 会话经 emergencyStopTriggered 复位；5.5 拒绝执行 | 🟢 已通过（**真机 2026-09-03 用户确认**）| **点击次数是 N+1（1 次启动 + N 次释放）**：WaitForStep 在 for 循环体内（SequenceWorker.cpp:306），**最后一个动作跑完也会挂起**，须再点一次才退出循环发 schemeFinished。原 real_machine_plan_phase2.md:116 写的「共 N 次点完 N 动作」有误，已修正为 N+1。5.2 语义真机确认：**点一次 = 立即执行下一个动作 + 该动作执行完立刻挂起，不会自动连跑**。5.4 确认：急停只清「正在回零中」的轴（AbortHoming，HardwareManager.cpp:717-727），已完成回零的轴 axisHomed_ 不变 → IsSystemHomed() 仍 true → 「单步执行」按钮不置灰；点击被 IsGlobalEnabled 门禁静默拦在 ProcessPage.cpp:1000，**只写日志不弹窗**（按钮看着可点、点了没反应，属待改进项）。5.5 确认：使能门禁无 QMessageBox，只有 SPDLOG_WARN，**「提示」= 日志而非弹窗**；可达路径为 使能→回零→断使能→点单步（未回零时按钮直接 disabled，点击发不出去，测不到门禁逻辑）|
| TR-063 | ProcessPage 单步执行「停止」语义真机确认（5.3，2026-09-03）| 真机：单步执行中点 ProcessPage「停止」，观察机械臂是否立即刹车；停止后再次点「单步执行」 | 业务停止（保持使能）、单步会话复位、可重新单步（从第一个动作开始） | 🟢 已通过（**真机 2026-09-03 用户确认**）| **实测结论：Stop 不是立即刹车，会执行完当前动作才停**。ProcessPage「停止」只调 SequenceWorker::Stop()（ProcessPage.cpp:178），而 Stop 仅置 cancel、**不下发任何停轴命令**（SequenceWorker.cpp:210-215），轴走完当前 MoveAbs 才停。**TR-040 描述的「业务停止就近（就近刹车原则）」与代码语义不符，以本条实测为准**，后续勿再按「就近刹车」预期（该行历史记录保留不动，以此条为准）。停止后再次单步 → m_stepActive 已复位 → 走 OnStepExecute 启动分支 → 从第一个动作重新开始（真机确认）。另注：Stop() 首行 `if (!running) return;`（:212），非运行态点击完全无效且无任何日志 |
| TR-064 | ProcessPage 单步执行边界用例真机验证（补 1/3/4/5，2026-09-03）| ① 单步暂停中连点「单步执行」3 次；② 单步暂停中点「执行选中动作」；③ 单步暂停中拖速度滑条再点下一步；④ 单步暂停中切换方案 | ① 连点只推进 1 步；② 被拒且有提示；③ 边界明确；④ 中途不允许切换方案并弹窗提示 | 🟢 已通过（② ③ ④ 与 **① D2 修复后均真机验证通过，2026-09-03 用户确认**）| ② 🟢 真机通过：RunSingleAction 被 running 门禁拒（SequenceWorker.cpp:172），**日志有拒绝提示**，UI 无弹窗（用户确认可接受）。③ 🟢 真机通过：impl_->scheme 是启动时**值拷贝**（SequenceWorker.cpp:157），改 ProcessManager 内存不影响正在跑的会话 → **单步会话中途改速度滑条不生效**；与 TR-049「拖动即生效」不冲突（那是启动前改）。④ 🟢 真机通过并已加固：原行为是切方案后继续点会按**旧拷贝**继续跑（与界面显示不一致），按 2026-09-03 要求新增门禁——OnConfirmSwitch 在 `IsExecutionActive()`（m_stepActive \|\| m_worker->IsRunning()）时弹窗拒绝「单步执行尚未结束，请先点击「停止」结束当前调试，再切换方案」。**① 🟢 已通过（D2，2026-09-03 修复：单步按钮改执行期同步禁用，见下一条记录）**。D2 根因知识保留：stepGo 是 bool **非计数、不排队**（SequenceWorker.cpp:234），同一时刻最多只能有 1 个「待生效放行」。原模型（修复前，设暂停在 A0 之后、点击间隔 ~150ms、Move 动作耗时数秒）：点在**暂停期**→放行下一个动作；点在**执行期**→预置放行，下一个暂停点被立即跳过（两个动作连着跑）；已有待生效放行时再点→**静默丢失**——执行期内无论点几下，最多只多跑 1 个动作；若点击间隔 > 动作耗时（慢慢点）则 1:1 正常，5.2 即属此情况。修复后按钮执行期灰置，点击只能落在暂停期，上述竞态不复存在。 |
| TR-065 | D2 修复：单步按钮执行期禁用 + 删除方案门禁（ProcessPage，2026-09-03）| ① 单步执行动作执行期间禁用「单步执行」按钮，仅单步暂停期可用；② OnDeleteScheme 加与 OnConfirmSwitch 同类的执行期门禁 | ① 每次点击必然落在暂停期，语义收敛为「走一步 → 停 → 确认 → 再走一步」，不再出现跳过暂停点连跑两个动作；② 单步执行中删除方案被弹窗拒绝 | 🟢 已通过（Debug+Release 编译 + Sim 冒烟 SMOKE_INIT_COMPLETE=True + **真机验证通过，2026-09-03 用户确认**：5.1/5.2/删除方案门禁、补 1 连点只跑 1 个动作且日志仅 1 条「释放下一步」、停止/急停后恢复路径无退化）| ① 实现（不改 SequenceWorker，纯用既有信号驱动，无引擎侧回归风险）：`actionStarted` 与 `stateChanged("运行中"/"执行选中动作")` → `setEnabled(false)`；`stateChanged("单步暂停")` → `setEnabled(true)`；`stateChanged("空闲")` → 交回 homed 联动。信号顺序依赖 ExecuteActions 的 emit 次序（actionStarted :292 → actionFinished :303 → 单步暂停 :307 → WaitForStep → 运行中 :313），Qt 同一 sender→receiver 的 queued 调用按 emit 顺序投递，顺序有保证。**不改 stepGo 为计数器的原因**：真机上狂点会连跑多个动作，反而更危险；禁用按钮既消除隐患又提供更清晰的交互（按钮亮起 = 轮到操作者点）。② 提取 `IsExecutionActive()`（m_stepActive \|\| m_worker->IsRunning()）供切换/删除两处共用，避免条件漂移。**副作用（正面）**：AutoRunPage 连续运行时 ProcessPage 单步按钮同步禁用（走 actionStarted 路径），意外补齐了部分跨页互斥。**补 1 竞态根因与二次修复（重要）**：初版只有**信号驱动禁用（异步）**——`stateChanged("运行中")` 由 worker 线程跨线程 queued 回主线程，而连点的第 2 个 QMouseEvent 已在主线程事件队列里、排在信号之前 → 处理点击时按钮尚未灰 → NextStep 被再次接受（日志出现第 2 条「单步执行：释放下一步」）→ stepGo 提前置位 → A1 跑完后 WaitForStep 立即放行连跑 A2。**修复：在 OnStepExecute 内点击即同步 setEnabled(false)**（启动分支 RunSequence 成功 / NextStep 分支成功后），再靠 `stateChanged("单步暂停")` 重新点亮；Qt 对 disabled 按钮不派发 clicked，第 2 下点击被拦截。顺带修 D1：启动分支现在检查 `RunSequence` 返回值，被拒则 return 且**不置 m_stepActive**（原实现无条件置 true → 会话永久卡死）。**同步禁用不破坏异步执行逻辑（用户提问，已答复）**：改动仅在 UI 层（SequenceWorker 零改动），按钮 enabled 只作输入门控、与 worker 状态机正交；握手自洽 enable→点击→同步 disable→异步 enable，不存在反序覆盖。**回归要点（2026-09-03 真机全部验证通过）**：补 1 连点 3 次 → 日志只 1 条「释放下一步」+ 只跑 1 个动作；5.1/5.2/停止/急停后再点单步的恢复路径不受影响 |
| TR-066 | 手动控制页 / 工艺流程页 / 自动运行页 操作互斥（2026-09-03 提出）| 单步暂停中切到自动运行页点启动、或切到手动控制页点动/Go | 三页运动操作互斥（同一时刻只允许一个运动源），跨页操作被明确拒绝 | 🟡 待补充（功能未实现）| 2026-09-03 用户明确：**三界面操作互斥逻辑暂未实现，后续补充**。关联的 D1 缺陷（ProcessPage.cpp `RunSequence` 返回值未检查、`m_stepActive` 无条件置 true → 被拒后单步会话卡死）**已随 2026-09-03 D2 修复顺带解决**（启动分支检查返回值、被拒不置 m_stepActive，见 TR-065）；互斥补齐后该路径本也不可达。互斥实现时 UI 联动参考：AutoRunPage 启动按钮/ProcessPage 单步按钮当前仅被 homed 状态联动，无跨页 worker 占用互斥 |
| TR-067 | QMessageBox 弹窗深底黑字（tooltip 修复回归，2026-09-03 修复）| 所有 QMessageBox 弹窗提示文字显示为暗色/黑色（深底黑字看不清）；tooltip 白底正常 | 弹窗消息文本恢复浅色（#b8cce3 系），与深色 UI 一致；tooltip 保持白底深字 | 🟢 已通过（Debug+Release 编译 + Sim 冒烟 SMOKE_INIT_COMPLETE=True + **真机目视确认，2026-09-03 用户确认**：弹窗文字恢复浅色）| 根因（回归来源）：48b8d74（tip 白底深字定稿，2026-09-01）把全局 QSS 宽泛基底 `QWidget { color:#b8cce3 }`（QSS 类型选择器匹配所有 QWidget 子类，含 QMessageBox 内文本 QLabel）**收窄为 `QMainWindow, QDialog { color:#b8cce3 }`** 以避开 QTipLabel 污染 → 此后 QMessageBox 内部消息 QLabel 无任何规则匹配，全局 stylesheet 生效后文字回退默认 palette WindowText = **黑**。修复：MainWindow.cpp `ApplyGlobalStyle()` 在 `QToolTip, QTipLabel` 规则**之前**补 `QLabel { color:#b8cce3 }`（同 specificity 后出现者胜 → tooltip 应用级深字规则优先级不受影响；TipStyleFilter 实例级兜底亦未动）。**顺序约束（勿调换）**：QLabel 规则必须位于 QToolTip/QTipLabel 之前，否则 tip 应用级文字被后出现的浅字规则覆盖（虽 TipStyleFilter 实例级仍兜底，但勿削弱三层中的 QSS 层）。注：tipcheck 已同日裁定废弃（独立复刻与实际按钮环境不一致，无效果），本项闭合以目视为准 |
| TR-068 | ProcessPage 单步执行自动选中当前动作（2026-09-03 新增）| 工艺流程页单步/自动执行时，动作列表自动高亮当前正在执行的动作；右侧详情与「当前动作:」标签同步跟随 | 点「单步执行」释放后列表选中随执行逐行前进，暂停时停在刚完成动作；右侧详情显示执行动作参数 | 🟢 已通过（Debug+Release 编译 + Sim 冒烟 SMOKE_INIT_COMPLETE=True + **真机验证通过，2026-09-03 用户确认**）| 实现：`actionStarted` lambda 加 `setCurrentRow(i)`（ProcessPage.cpp:896-900）——列表行与 `scheme.actions[i]` 严格 1:1（RefreshActionList :633-637），setCurrentRow 经 currentRowChanged（:248）同步 m_currentActionIdx + RefreshActionDetail。顺带解决既有遗留「执行时 m_currentActionLabel 不动」（原来只随列表选中变化）。越界防御 i < count()（setCurrentRow 越界本为 no-op）。跨线程安全：actionStarted 由 worker 线程 emit，connect Auto → queued 主线程执行 UI 操作。单步暂停期选中停留在刚完成动作，用户可自由点选其它动作查看 |
| TR-069 | 阶段 5 自动运行真机验证（5.6-5.15，2026-09-04）| AutoRunPage 启动整跑工艺流程方案，覆盖 Move/Gripper/Delay/Vision/schemeFinished/运行中停止/运行中急停/门禁/运行中改参/运行中关窗 共 10 个用例 | 逐点 InverseSmart→MoveAbs 无大甩臂；动作按序执行；异常与边界被安全处理；进程不挂死 | 🟢 已通过（真机 2026-09-04 用户确认）| 逐项（5.6-5.15）：① 5.6 Move 整跑 ✅——逐点 InverseSmart→MoveAbs(J2→R→J1→Z) 无大甩臂；② 5.7 Gripper 开/合按 GetLimitMax/Min 到位 ✅；③ 5.8 Delay 1s 不卡 UI ✅；④ 5.9 Vision ✅ **但走降级路径**——真实相机未接，走无相机模拟延时（验证不崩溃），**接相机后需复测**；⑤ 5.10 完成后 schemeFinished ✅（状态「✅ 完成」、启动按钮恢复）；⑥ 5.11 AutoRunPage「停止」✅（自动流程中测：当前动作安全停止 + interrupted + 保持使能）；**跨页互斥仍待完善**——工艺流程单步调试中不允许操作 AutoRunPage 相关按钮，逻辑后续补充（关联 TR-066）；⑦ 5.12 运行中顶栏全局急停 ✅（断使能 + worker 中断 + 自动页复位，需重新使能，见 TR-040）；⑧ 5.13 未使能启动拒绝 ✅（门禁）；⑨ 5.14 运行中编辑运动学参数 ✅（ReloadFromConfig 被 running 门禁跳过，下次启动生效）；⑩ 5.15 运行中关闭窗口 ✅（进程不挂死、10s 内退出，ShutdownWorker）。注：5.9 相机降级路径、5.11 跨页互斥待完善为已知开口，不阻阶段 5 收口 |
| TR-070 | 舵机连接状态语义 + 探测 id 时序（HardwareManager/XRServo，2026-09-04 修复）| 舵机仅插 USB（串口转接板）未上电启动 → UI 误显「舵机: 已连接」；附带发现：上电启动日志两次 `WriteUserData id=1`/`Servo id 1 online`，id=0 从未被探测（J2 在线标志错探成默认 id=1，addr=44 accel 参数写错对象） | ① 未上电 UI 显示「舵机: 未连接」（黄）；② 软件开着时上电 ~2s 内自动恢复「已连接」（无需重启）；③ 未上电时「全局使能」被拒（`EnableAll rejected ... servo=false`）；④ 上电启动日志 `Servo id 0 online` 与 `Servo id 1 online` 各一次、`WriteUserData id=0`/`id=1` 各一次 | 🟢 已通过（Debug+Release 编译 + Sim 冒烟 SMOKE_INIT_COMPLETE=True + **真机验证通过，2026-09-04 用户确认**） | 根因三层：① `XRServo::Connect` 串口打开成功即返回 true（Ping 失败仅 WARN）；② `IsConnected()` 语义=串口句柄有效（真实在线在 `IsOnline()`，未接入任何"已连接"判定）；③ `HardwareManager::IsServoConnected()` 只查串口级 → UI 标签/EnableAll 门禁/PollTick 边沿检测全部基于假"已连接"。修复=HardwareManager 单文件三处：`IsServoConnected` 收紧为「串口打开 且 所有已创建舵机 IsOnline()」（SimServo 的 IsOnline==connected_，仿真不受影响）+ `Initialize`/`ReconnectServos` 先 `SetServoId`/`SetSpeed` 再 `Connect`（Connect 内 Ping/WriteUserData 面向配置 id）。上电自动恢复走既有「Ping ok skip reconnect」+ PollTick 边沿广播 `connectionChanged` 链路，无需新增机制。已知开口：软件开着时上电、经 skip-reconnect 路径恢复的不重写 addr=44 accel 参数（舵机用户数据预期掉电保持；真机点动平滑即无需处理，若顿挫回归再在恢复边沿补写） |
| TR-071 | 示教记录关节角 + 确定性执行（ProcessPage/SequenceWorker/ProcessManager，2026-09-04 新增）| 工艺流程页示教读取后，process.json 的 points 旧版 save 硬编码写 `joints{j1..j4}=0.0` 占位（真实关节角从未落盘）；执行一律 InverseSmart 就近选解，示教姿态不被复现 | ① 示教读取时把 J1/J2/Z/R 关节角记入该点 `joints` 字段（j1/j2=逻辑关节角、j3=Z 电机坐标（回零后=0，与 coord.z 末端高度差固定偏移 Z0−h1−tcpDown=165）、j4=R 角）；② 执行时点位带有效 joints → 直接按关节角运动（日志 `Teach joints →`，无 IK 双解歧义）；③ 手改坐标保存 → joints 作废（json 不再写该字段，非置 0）；④ FK 一致性校验（±0.5mm/0.5°）不匹配 → 回退 IK（旧版硬编码全 0 残留数据靠此拦截，fill_start 等模板点不会误走到 J(0,0,0)） | 🟢 已通过（Debug+Release 编译通过 + Sim 冒烟 SMOKE_INIT_COMPLETE=True + **真机验证通过，2026-09-04 用户确认**） | 根因：save 硬编码占位 + OnTeachRead 读关节角只做 FK 后丢弃。实现：PointData 加 hasJoints/j1~j4（缺失字段=无记录，不置 0——0 是合法关节角无法区分作废）；load 纯读取不做语义校验（保持 Config 纯数据层，FK 校验放 Logic 层）；OnSaveAction 表格重建按容差 0.006（覆盖 2 位小数显示舍入）比对坐标一致才继承 joints；MoveToPoint 三分支（teach 校验通过/回退 IK/无 joints 走 IK）共用下发段。posture 字段语义不动（独立待改进项）。真机验收确认：示教新点 json 出现 joints 且 j3=Z 电机坐标、带 joints 点执行日志 `Teach joints →` 姿态复现、手改坐标保存后字段消失执行 `IK →`、旧全 0 模板点 `mismatch, fallback to IK` 位置正确 |
| TR-072 | AutoRunPage 坐标面板窄宽度 R 折行（2026-09-04 排查，暂不处理）| 窗口/右侧分屏缩小后坐标面板 (X,Y,Z,R) 显示不全；调整字号与自动换行后，R 角度折行到第二行 | 坐标始终单行完整显示（不折行、不裁切） | 🟡 暂不处理（2026-09-04 用户裁定：不影响功能，仅样式）| 根因——`m_coordPanel` 为单 `QLabel` + `setWordWrap(true)`（现行「修改1/2/3」版本，AutoRunPage.cpp），面板宽度不足时 Qt 在空格处折行，`R: 0.00°` 被挤到第二行；关换行则裁切（原始「显示不全」），单标签两难。已定修复方案（代码曾实现、用户手动回滚）：拆 X/Y/Z/R 四个独立 `QLabel`（各 `setWordWrap(false)`）入容器 `QHBoxLayout`，单值原子化、布局给各标签自然最小宽度，整体不折行不裁切；`MainWindow setMinimumSize(1200,700)`，折行多发于分屏拖窄场景。后续如需修复按此方案实施 |
| TR-073 | 自动运行页 5 按钮 + SequenceWorker 显式状态机（架构指令 1/2，2026-09-07 重构）| 自动运行页 4 按钮改 5 按钮：▶启动/继续、⏸暂停、■停止、↺清报警、⟳初始化；引擎引入 `WorkerState{Idle,Running,Paused,Fault}`+`PauseReason{None,User,Step}` 显式状态机，`stateChanged` 改 enum 签名 | ① 暂停=减速停止当前轴（MC_Stop 斜坡/舵机锁位）→ 挂起保持上下文 → 继续时**只重发当前那条绝对目标 MoveAbs** 原地续走（Delay 按剩余时间续算、回抽阶段不反向补料、Vision 不重采图）；② 停止=`StopImmediate()` cancel+全轴减速停（保持使能），废弃上下文回 Idle；③ 动作失败且非 cancel→Fault 锁存，清报警（仅清引擎，不自动使能）后可重启；④ 单步语义零回归（按钮可点权威条件=Paused∧Step） | 🟢 已通过（Debug+Release 双编译 EXITCODE=0 + Sim 冒烟 SMOKE_INIT_COMPLETE=True；暂停/继续/停止/清报警的 GUI 交互待真机验证）| 关键设计决策（与用户/Gemini 评审定稿，4 项）：① **点级暂停+原地续走**而非"重试整个动作"——`ExecuteExtrude` 分挤出→回抽两段，回抽阶段暂停后重试整个动作会把挤出轴**反向补回满量程**（多挤料）；② 回零入口归手动控制页（原"复位"实为 HomeAll），初始化只 Initialize+按 未连接/未使能/未回零/就绪 分级引导——"程序不自动使能"是既有安全原则；③ 仅自动页立即停（`StopImmediate`），`Stop()` 保持 cancel 就近——单步调试语义与 `ShutdownWorker` 零回归；④ 安全位/初始位与模式互锁放后续。**底层事实**：所有"停止"本就走 `MC_Stop` 减速斜坡（BoPaiCard.cpp:327，非断脉冲），开环步进"立即断脉冲"会惯性滑行丢步——用户"继续运动一点角度"的观察正是减速距离（J1 高速约 9°），**不是缺陷也不能改断脉冲**；`AbortHoming` 仅复位"正在回零中"的轴（HardwareManager.cpp:721）→ 运行中减速停不清 homed，恢复后 MoveAbs 不被回零互锁拒。**实现要点**：全部挂起点收敛于 3 个等待函数+2 个循环头（WaitForAxes/PauseGate/点边界/动作边界/Delay 剩余时间）；`UpdateControlsEnabled()` 为按钮启用唯一出口（删 8 处散点 setEnabled）；按钮按下标分派（原 text.contains 匹配改文案会静默错配）；新增 `QPushButton:disabled` 样式（既有置灰无视觉反馈）；`stateChanged` 唯一消费者 ProcessPage 同步改 enum，单步可点条件 `Paused∧reason==Step` 比字符串更强（守 D2）；`MoveAbs` 为绝对坐标 → 续走不累积误差。已知开口：暂停在 Vision 同步采图段不可中断（百毫秒级）；真机需验证减速停距离是否可接受（可后续用 `MC_SetStopDec` 调平滑减速档缩短）；TR-066 跨页互斥仍未实现（点动与自动流程并存风险，靠模式互锁下一步收口） |
| TR-074 | 全局安全位（Global Safe Position，2026-09-07 新增）| 回零完成后（含急停恢复链路的重新回零）自动走到预配置安全位：**先原地抬 Z 再水平走**（防从低位直奔方案第一步横扫桌面）；手动控制页提供「回安全位」「示教安全位」按钮 | ① 回零完成上升沿自动回安全位（enabled=true 时）；② 未启用/未配置时不动作；③ 未回零/任务执行中按钮拒绝并提示；④ 与自动运行互斥（同一 worker running 门禁）；⑤ 单步/连续运行零回归 | 🟢 已通过（Debug+Release 双编译 EXITCODE=0 + Sim 冒烟 SMOKE_INIT_COMPLETE=True；走位动作与急停恢复全链路待真机验证）| 实现决策（与用户/Gemini 评审定稿 4 项）：① 配置形态=**关节角 j1/j2/z/r**（与示教 joints 同坐标系，MoveAbs 直发，无 IK 双解歧义；Z 为电机坐标）；② 触发=回零后自动+手动按钮；③ 执行=**复用 SequenceWorker**——构造临时方案（1 个 Move 动作 2 个 hasJoints 点：点1={当前J1/J2/R+safeZ}、点2=安全位）走 RunSequence→ExecuteMove，**零新运动代码**，白得暂停/停止/门禁/互斥；④ 配置 UI=设备配置页运动学区 4 输入框+enabled checkbox + 手动页示教钮。**关键实现约束**：① hasJoints 点必须用 `impl_->kin.Forward(joints)` 同源填 x/y/z/r——MoveToPoint 有 ±0.5 FK 一致性校验，填 0 会静默回退 IK（可能跳逆解分支）；② `homeStateChanged` 是**电平语义**（手动页单轴重回零、其余轴 homed 保持会再次 emit true）→ MainWindow 用 `lastHomed_` 做**上升沿**判定；③ `safePos.enabled` 默认 **false**（出厂 config 全 0 不应乱走），示教安全位自动置 true；④ safePos 每次 RunSafePos 现读 config → ConfigPage 编辑**不 emit paramsChanged**（那只会触发无谓的 Kinematics 重建）。**顺带修复隐患**：`RunSequence` 原先不清 `stepMode`——单步会话残留 stepMode=true 时，回零触发的安全位会话 1 个动作跑完会挂起 Paused(Step) 等 NextStep 卡死（RunSingleAction:216 本就有清，此次对齐）；ProcessPage `actionStarted` 加 `IsSafePosSession()` 守卫，防临时方案把工艺流程页动作列表误选到第 0/1 行。**急停恢复链路**：急停（断使能，已回零轴 homed 保持）→ 仅重新使能**不**触发 homeStateChanged → 必须一键回零 → emit true → 自动回安全位——正是期望行为（急停后舵机 TorqueOff 可能垂落，旧坐标不可信）；边界：急停前已进 Fault → 回零后 RunSequence 被 Fault 门禁拒，自动回安全位跳过（先清报警，合理）。零新增运动代码/线程/信号；已知边界：Vision 同步采图段暂停不可中断（百毫秒级，沿用 TR-073）；安全位走位速度固定 50%（可后续参数化） |
| TR-075 | UI 权限与全局互锁（架构指令 2/2，2026-09-07 新增）| 基于顶栏【手动/自动】开关 + 急停锁存实现全局互锁：模式切换门禁、自动模式导航白名单、视觉页参数只读、急停待恢复锁存 | ① 运行/暂停（含安全位会话）禁止切模式（弹窗+开关回弹，QSignalBlocker 防递归）；② 单步会话切自动强制 Stop+清理（Q12）、切手动放行；③ Fault 态放行切模式（无运动在执行，切手动脱困合法）；④ 自动模式导航仅放行【自动运行】【视觉检测】，视觉页 9 个参数控件只读；⑤ 急停锁存 `estopPending_` 在 HardwareManager（全局硬件级状态，**禁止页面自持副本**——Gemini Q6），置位于 enableStateChanged 之前、急停后全轴回零完成时清除（先清后发 estopCleared）；⑥ 手动页急停后显示分步恢复指导（先 ResetAxisStates 再 SetHint 防被 RefreshSoftLimitHint 覆盖），estopCleared/使能成功时阶段化提示 | 🟢 已通过（Debug+Release 双编译 EXITCODE=0 + Sim 冒烟 SMOKE_INIT_COMPLETE=True；手测 7 条——模式切换门禁/白名单/视觉只读/急停恢复链路/单步清理——待真机验证阶段统一执行）| 关键实现要点：① `ApplyModeState` 为模式态唯一出口——**标签高亮从 toggled lambda 迁入**（拒绝回弹经 QSignalBlocker 屏蔽 toggled，高亮留在 lambda 会残留被拒模式色，修正原 ui_interlock_plan 5.5 的"无需补偿"结论）；② 强制跳页双写 `setChecked(true)+setCurrentIndex(0)`（程序化置位不发 idClicked）；③ `m_schemeCombo->setEnabled(!busy)` 脱离 gate——手动/急停期保留方案预选；④ `estopPending_=true` 必须置于 `emit enableStateChanged()` 之前（AutoRunPage 该信号槽刷按钮，晚置位一帧错误解锁）；⑤ 标志清除先于 `homeStateChanged(true)` emit，专用信号 `estopCleared` 供手动页区分"急停恢复回零"；⑥ ProcessPage 新增 public `IsStepSessionActive/AbortStepSession`，`AbortStepSession` 照搬停止按钮模板（Stop+ResetStepSession+状态文本）；⑦ 已知边界：急停前 worker 已 Fault → 回零后 RunSequence 被 Fault 门禁拒、安全位跳过（先清报警，合理）；自动模式视觉页"关闭相机"仍可点（范围外，误关走 Fault→清报警恢复）。**实施过程异常记录**：发现并行会话同时实施互锁（已在 HardwareManager.h 写入 `IsEStopPending/estopPending_` 声明），经用户裁决由本会话续用其声明完成（避免撞车）；本会话计划模式下曾误实施并已全部精确撤销后重做（grep+git diff 核实零残留） |
| TR-076 | 单步执行失效（回归缺陷，2026-09-08 真机 8.13 发现并修复）| 工艺流程页点「单步执行」：应执行 1 个动作后挂起在「● 单步暂停」，再点执行下一个（N 个动作 N+1 次点击）。实测：点一次后**执行完所有动作**，状态直接从「运行中」→「✅ 完成」，全程无单步暂停、单步按钮不可点 | ① 点单步执行 → 只跑第 1 个动作 → 状态「● 单步暂停」+ 日志 `Step mode: pausing after action 0`；② 再点 → `Next step released` → 跑第 2 个动作再挂起；③ 末动作跑完仍挂起，再点一次才发 schemeFinished（N+1 次点击）；④ 自动页启动（不设 stepMode）仍一次性跑完，不受影响 | 🟢 已修复并真机验证通过（2026-09-08/09：8.13 复测——单步只跑第 1 个动作停在「● 单步暂停」、N+1 次点击至「✅ 完成」均正常；Debug+Release 双编译 EXITCODE=0 + Sim 冒烟 SMOKE_INIT_COMPLETE=True）| **根因**：TR-073 中我为修"单步残留 stepMode=true 使后续会话卡死 Paused(Step)"（TR-074 隐患 1），在 `SequenceWorker::RunSequence` 开头加了 `impl_->stepMode.store(false)`——但单步的调用顺序是 `ProcessPage::OnStepExecute` 先 `SetStepMode(true)`（ProcessPage.cpp:1062）再 `RunSequence`（:1063），**刚设的 true 被 RunSequence 自己清掉** → 动作间不再挂起（ExecuteActions:475 `if (stepMode)` 不成立）→ 一次跑完。**修复**：清除点从 RunSequence 开头移到**会话结束时**（StartExecution 末尾，与 safeSession 清除同处），单步标志在会话内生效、结束即清；另在 `RunSafePos` 里显式清一次作双保险（安全位会话绝不用单步）。**教训**：修改共享引擎状态时，必须按"调用方设置→引擎使用→会话结束清除"的生命周期定位，不能在引擎入口无条件清调用方标志；本次缺陷源于把"防残留"的清除点放在了最前而非最后。**注**：phase3 文档原预分配 TR-076/077/078 = 阶段 8/9/10，因本缺陷先占用 076，预分配顺延为 TR-077/078/079，缺陷顺延 TR-080 起 |
| TR-077 | 执行选中动作破坏单步会话（缺陷，2026-09-09 真机发现并修复）| 单步执行跑完第 1 个动作停在「● 单步暂停」→ 选中其它动作点「执行选中动作」→ 被引擎拒绝（RunSingleAction rejected: already running）；**之后再点「单步执行」也被拒**（Step mode ON → RunSequence rejected: already running → 提示「会话未激活」）→ 单步会话无法继续，形成死锁 | ① 单步/自动会话期间「执行选中动作」按钮**置灰**（UI 防呆），即便点到也门禁拒绝；② 拒绝时**不破坏单步会话状态**——之后可继续点「单步执行」释放下一步，直至跑完 | 🟢 已修复并真机验证通过（2026-09-09 复测：单步期间「执行选中动作」置灰、防死锁，单步可正常继续释放；Debug+Release 双编译 EXITCODE=0 + Sim 冒烟 SMOKE_INIT_COMPLETE=True）| **根因**：`ProcessPage::OnRunSelectedAction` 在调用 `RunSingleAction` **之前**执行 `if (m_stepActive) ResetStepSession();`，而 RunSingleAction 随后被引擎拒绝（worker 仍在单步会话）→ **标志已丢、会话还在** → 再点单步走"新会话启动"分支被 running 门禁拒 → 死锁。**修复三点**：① 删除"调用前清状态"，改为先判门禁 `IsExecutionActive()`，拒绝时**不动任何状态**（弹窗提示"请先停止结束当前会话"）；② stateChanged 消费者新增 `m_runSelectedBtn` 联动（Running/Paused 置灰，Idle/Fault 恢复）；③ `OnStepExecute` 加防御——`m_stepActive` 与引擎态不同步（Paused + reason==Step + IsStepMode）时按"释放下一步"处理而非启动新会话。**教训**：UI 会话标志的清理必须放在"操作成功确认之后"，不能放在"调用之前"——先清后调，一旦被调用方拒绝就产生"标志已丢、会话还在"的不一致状态；这与 TR-076（引擎入口清调用方标志）是同一类"清除点位置错误"问题 |
| TR-078 | 自动运行页 5 按钮 + 显式状态机真机验证汇总（阶段 8，用例 8.1–8.17，2026-09-08/09）| 验证 5 钮启用矩阵（六态）、点级暂停原地续走、停止保持使能、清报警、初始化分级引导、单步回归、方案下拉运行期禁用 | 8.1–8.17 全部 🟢（详见 `doc/test/real_machine_plan_phase3.md` 阶段 8 实测记录）；① 暂停/停止有减速滑行距离（开环步进设计预期，预留安全距离）；② 停止/清报警后再启动从第一动作开始 = 设计意图（废弃上下文）；③ 初始化不含使能/回零（安全原则）确认 | 🟢 已通过（2026-09-08/09 真机；过程中发现并修复 TR-076/TR-077 两个回归缺陷）| **验证收获**：① 8.4 引出新需求「循环生产」——现状仅单轮，方案已出 `doc/auto_run_loop_production.md`（待审核）；② 8.16 操作员确认"单步暂停中切自动页五钮全灰"符合逻辑预期；③ 单步暂停切【自动】强制清理会话 = 既定设计（Q12 原意），日志行为符合预期；④ §0-A 文案不一致（「全部使能」vs「全局轴使能」）待修。**状态机与 5 钮语义收口 🟢** |
| TR-079 | 示教安全位后设备配置页不同步（缺陷，2026-09-09 真机 9.4 发现并修复）| 手动页示教安全位后切到设备配置页，运动学区 4 个「安全位 J1/J2/Z/R」输入框仍显示构造时的 0、复选框未勾选；**重启软件后才显示实际值** | ① 示教后**不重启**切到配置页，4 个输入框立即显示实际值（两位小数）且复选框已勾选；② 配置页手改保存后手动页「回安全位」走到新值（9.13 行为不变） | 🟢 已修复（Debug+Release 双编译 EXITCODE=0 + Sim 冒烟 SMOKE_INIT_COMPLETE=True；真机复测待执行）| **根因**：`ConfigPage` 的 safePos 控件只在**构造时**读一次 config（`ConfigPage.cpp:389/404`），该类**无 showEvent**；而示教由 `ManualControlPage::OnTeachSafePos` 直接 `ConfigManager::set` 写入（按设计不 emit paramsChanged）→ 配置页无从感知。**修复**：新增 `ConfigPage::RefreshSafePosFields()` + `showEvent` 重写（每次切到配置页重新读 config 回填；复选框用 `QSignalBlocker` 阻断 toggled 防反向写回；`setText` 不触发 editingFinished 故不会误写）。**附带优化**：示教写入前量化到两位小数（`std::round(v*100)/100`，并消除 -0）— 原直接落盘全精度 double 会产生科学计数法 `j1: -4.6875000009549694e-05`，与提示文案的 -0.00 对不上；该值 = -0.0000469°（J1 换算不足 1 个脉冲）、j2 尾数也远小于舵机分辨率与死区，**对运动零影响**，仅为存档/显示一致。**教训**：config 现读现用 + 不 emit 信号的设计下，所有展示该配置的页面都必须在**页面显示时**（showEvent）重新读取，只靠构造初值必然产生"重启才对"的不同步 |
| TR-080 | 回安全位误报「有任务执行中」（缺陷，2026-09-09 真机 9.6 发现并修复）| 重启 → 使能 → 一键回零 → 点「回安全位」，提示「有任务执行中，无法回安全位」，实际无任务在跑；日志三次复现（14:34/15:20/16:00）`MoveAbs rejected: axis N homing in progress` → `state -> 3 (Fault)` | ① 重启 → 使能 → 一键回零 → **重复一键回零**：自动回安全位正常执行、无 Fault（舵机 homingActive_ 应 ≤250ms 内清除而非等 5s，日志见 `servo axis X home done`）；② 制造 Fault 后手动页点「回安全位」应提示故障锁存 + 清障指引（红色文案）而非「有任务执行中」 | 🟢 已修复 + 真机复测通过（2026-09-09：Debug+Release 双编译 EXITCODE=0 + Sim 冒烟 SMOKE_INIT_COMPLETE=True + 真机复测；Sim 覆盖不到舵机回零路径）| **根因（三层叠加）**：① 舵机重复回零竞态——`checkServoHomeDone`（`HardwareManager.cpp:1249`）清除 `homingActive_` 的分支有 `!axisHomed_[idx]` 条件，轴已回零过时重复回零的 homingActive_ 走不到清除分支，只能等 5s busy 超时兜底；期间全轴 homed 上升沿（`MarkAxisHomed` → `MainWindow.cpp:182`）自动触发回安全位，首条 MoveAbs(J2/R) 撞 `MoveAbs` 门禁（:399）被拒。② `ExecuteActions` 失败置 **Fault 锁存**（`SequenceWorker.cpp:466`，StartExecution 见 Fault 刻意不回 Idle）——设计行为，但正常操作序列必然产出一次 Fault。③ 手动页 `OnGoSafePos`（`ManualControlPage.cpp:538`）`GetState() != Idle` 把 Fault 一律报「有任务执行中」；errorOccurred 只连工艺页、ClearFault 唯一入口在自动页 → 手动页对 Fault 完全不可见。**修复**：① 删掉 `!axisHomed_[idx]` 条件与卡轴路径（:1144）对齐（MarkAxisHomed 幂等；homeStateChanged 电平语义三消费者——MainWindow lastHomed_ 上升沿 / AutoRunPage / ProcessPage——均幂等，重复 emit 安全）。② OnGoSafePos 区分 Fault：红色提示「故障锁存，请到自动运行页点↺清报警」；Running/Paused 保持原文案。③ 补日志：OnGoSafePos 入口/拒绝分支、MainWindow 自动触发 triggered（与 skipped 成对）。**不做**：手动页清障按钮（文案指引已够，与现有跨页指引习惯一致）；回零按钮 UI 禁用（HardwareManager :587 门禁已兜底）。**教训**：`axisHomed_`（电平）与 `homingActive_`（过程）是两个独立状态数组，"全轴 homed" ≠ "回零过程全结束"——跨数组联动的上升沿触发必须考虑过程标志的滞留窗口。**复测记录（同日）**：重启→使能→回零（轴1未到位）→再回零成功→自动回安全位正常执行、无 Fault（根因修复生效）；第三次回零后不再触发（lastHomed_ 上升沿判定正常、无 Fault 残留）；手动「回安全位」成功。**遗留 feature（用户定性暂不修）**：首轮回零轴1偶发未到位需再点一次回零，后续看情况决定是否修复 |
| TR-081 | 9.9 已回零状态重回零不自动回安全位（用例期望与实现语义偏差，2026-09-09 真机发现，定性非缺陷）| 9.9 场景（手动移走→一键回零）回零完成后**未**自动发起安全位走位；手动「回安全位」正常 | 修正用例期望后实测一致：已回零状态重回零不自动触发（上升沿设计）+ 手动回安全位正常 | 🟢 用例期望修正 + 急停恢复链改手动（无行为逻辑代码改动）| **语义澄清**：自动回安全位仅「未回零 → 已回零」**上升沿**触发（`MainWindow.cpp:173` lastHomed_）；`axisHomed_` 仅重启初始化 / AbortHoming（回零中轴）清除，手动移走、急停、断使能均不清 → 已回零重回零无上升沿。设计文档 `global_safe_position.md:24` 本就明确此为防误触发设计（9.12 单轴重回零同语义），9.9 原文本"自动发起"系用例笔误。**连带发现**：急停恢复链（10.14-10.17）原描述"回零→自动回安全位"不成立——急停不清已回零轴 homed，恢复回零无上升沿；此前文档未察觉，阶段 10 实测 10.16 时必然踩到。**决策（用户选方案 A）**：接受现状，恢复链改为 使能→回零→**手动**回安全位→切自动；急停红字①文案同步修正（`ManualControlPage.cpp:176`，唯一代码改动，纯字符串）。**方案 B（急停清全轴 homed 使恢复回零产生真上升沿、恢复链自动闭环）被否决**：急停语义扩张影响面大，手动一步可接受——留档备查。同步文档：phase3 9.9/10.14/10.16/10.17、`global_safe_position.md` §五+概述、`architecture.md:93`、`auto_run_loop_production.md` 10.23。**2026-09-09 后续真机确认（用户口径一句话）**：「自动回安全位只有软件启动之后第一次一键回零后触发，其它一键回零操作都不会触发」——9.9（全轴重回零）、9.11（Paused 重回零）、9.12（单轴重回零）三条实测一致。**修正我此前在 TR-083 中的一处表述偏差**：曾写"Paused 回零经 Stop() 后回零完成时 worker 已 Idle → 自动回安全位**会**执行"——**错**。`Stop()` 只决定 worker 是否 Idle，**不影响上升沿**；9.11 场景因 `lastHomed_` 已 true 无上升沿，故不触发（两条原因叠加，现象均为"无自动回安全位"）。结论不变、修复不变，仅原因表述更正；用户确认不再深究 |
| TR-082 | 安全位开关语义澄清：`safePos.enabled` 实为**总开关**但 UI 名只写「回零后自动回安全位」（2026-09-09 真机 9.10 发现）| 9.10 取消勾选后回零确实不自动回安全位（符合预期）；但**手动点「回安全位」也报「未配置/未启用」**——名称只描述了自动场景，误导 | 取消勾选后自动与手动**均**不执行（同一开关两个入口）；UI 名改称「启用安全位」后术语一致 | 🟢 已修复（纯文案+文档；Debug+Release 双编译 EXITCODE=0 + Sim 冒烟 SMOKE_INIT_COMPLETE=True）| **事实**：`RunSafePos`（`SequenceWorker.cpp:242`）第一行即查 `kinematics.safePos.enabled`，手动 `OnGoSafePos` 与自动（MainWindow 回零上升沿）**共用该函数** → 同一开关管两个入口；且 enabled 兼作「已示教过」标记（示教后置 true，`ManualControlPage.cpp:598`）。**决策（方案 A，用户选定）**：语义统一为总开关，**逻辑不变**，仅改文案——ConfigPage 复选框「回零后自动回安全位」→「启用安全位」+ tooltip 注明同时控制①②；示教成功提示「已启用安全位」；手动拒绝提示「安全位未启用…」。同步 `config.md`、`global_safe_position.md`、phase3 9.4/9.10。**方案 B（分离控制：enabled 只管自动，手动改判新增 configured 字段）被否决**：需新增状态字段与旧 config 兼容判断，概念变多、收益有限。**教训**：配置项命名必须覆盖其**全部**控制的入口——一个开关管两个入口时，只按其中一个入口命名必然产生误导；新增门禁时应回头核对所有调用方与 UI 名称的一致性 |
| TR-083 | 单步会话残留导致「回安全位」误报「有任务执行中」（2026-09-09 真机阶段 9 发现并修复）| 工艺页单步执行停在 Paused(Step) → 手动页一键回零（成功）→ 点「回安全位」提示「有任务执行中，无法回安全位」，实际**无轴在动**，只是单步会话仍挂起 | ① 单步挂起时点一键回零：日志 `[ManualControl] 一键回零：终止残留会话 state=Paused reason=…` + `state -> 0`，回零**照常执行**，提示黄字「已终止未完成的单步会话；回零中...」；② 回零完成后点「回安全位」正常走位；③ 回工艺页状态「■ 已停止（保持使能）」且可重新单步；④ Running（轴真在动）时点一键回零被拒（9.16） | 🟢 已修复 + 真机复测通过（2026-09-09：9.15 残留处置、9.16 Running 拒绝、9.17 Paused 手动回安全位文案、9.11 复测均通过；Debug+Release 双编译 EXITCODE=0 + Sim 冒烟 SMOKE_INIT_COMPLETE=True 仅防回归，**Sim 覆盖不到本路径**）| **根因**：单步会话阻塞在 `WaitForStep`（`SequenceWorker.cpp:85-99`），退出条件只有 `stepGo`/`cancel`；清理代码 `StartExecution:402-408` 在执行循环**之后**。而一键回零 `OnGlobalHome` 只调 `HardwareManager::HomeAxis`，**完全不触碰 SequenceWorker** → 回零后 `state=Paused / pauseReason=Step / running=true / stepMode=true / stepGatePending=true` 全残留 → `OnGoSafePos` 的 `st != Idle` 拦截并误报。**为何必须终止（安全）**：① 位姿不连续——回零后机器人位于机械原点，残留会话下一步 = 从原点直冲旧方案下一**绝对**目标，而 `MoveToPoint` 是四轴独立 MoveAbs（无"先抬 Z"前提，这正是 RunSafePos 拆两段的原因）→ 可能扫过夹具；② 异步竞争致 Fault 锁存——回零期间若误点「下一步」，`MoveAbs` 被回零门禁拒 → 非 cancel 失败 → `ExecuteActions:466` 置 Fault（TR-080 同族）；③ 工艺页「单步执行」在 Paused+Step 恰好可点，误终止=重跑、误续跑=撞机。**注意（更正初始假设）**：不是"回零使坐标基准失效"——开环步进回零恰恰是**重建**基准。**处置矩阵**：Paused→`Stop()` 终止+放行回零；Fault→**不动锁存**（ClearFault 保持自动页唯一入口，保留故障上下文）仅预告；Running→**拒绝回零**（用户确认；`Stop()` 不下发硬件停止属假安全，回零仅靠 `IsAxisBusy` 时间估算软门禁不可靠）。**实现要点**：`PrepareHomingSession()` 在 HomeAxis **之前**调用（Paused 退出路径不产生硬件命令→无竞争；回零耗时秒级而 worker ≤20ms 收敛→回零完成时必 Idle，保证自动回安全位不被 running 门禁静默拒=9.6 回归点）；**不等待** Stop 完成；`interrupted` 经 `ProcessPage.cpp:934` 自动复位工艺页 UI → 无需跨页耦合。`GetPauseReason()` 已存在，未新增 API。**不做**：单轴回零 `OnHomeAxis` 不处理（9.12 语义不同、危害面小）；不加确认弹窗/配置项。**教训**：凡"不经过状态机的硬件操作"（回零/点动/使能）都必须回头确认是否要同步清理状态机残留——异步挂起点的清理代码在执行循环之后，靠"操作完成"自动收敛的假设不成立 |
| TR-084 | 退出软件时崩溃（悬垂指针访问违规，2026-09-09 由 log/crash.txt 发现并修复）| 关闭软件时若存在未完成会话（Running/Paused/挂起）→ `0xc0000005` 崩溃：栈 = `~MainWindow` → `ShutdownWorker:229`(processEvents) → `AutoRunPage::OnInterrupted:532` → `UpdateControlsEnabled:563` → `m_worker->GetState()` → `std::_Atomic_storage<WorkerState>::load` 访问违规 | ① 有未完成会话时关软件：不再崩溃、不再写 crash.txt；② 正常业务行为不变（双编译 + 冒烟 + 既有用例回归）| 🟡 已修复待真机验证（Debug+Release 双编译 EXITCODE=0 + Sim 冒烟 SMOKE_INIT_COMPLETE=True；**退出复现需真机**——Sim 难构造 Paused 会话，冒烟只启动即关无会话不触发原崩溃路径）| **根因（结构性）**：`SequenceWorker` 由 `MainWindow.cpp:159` `connect(workerThread_, &QThread::finished, sequenceWorker_, &QObject::deleteLater)` 交给事件循环销毁，而 4 个观察者（MainWindow/AutoRunPage/ManualControlPage/ProcessPage）持**裸指针**——判空只能判 null、判不出悬垂。退出时序：ShutdownWorker 调 `Stop()`（有会话时产生 `interrupted`）→ `quit()` → 线程收尾执行 deleteLater → 主线程 `processEvents` 派发**已排队**的 `interrupted`（Qt 不会因 sender 析构回撤已 post 的 QMetaCallEvent）→ 槽访问已释放内存。叠加结构隐患：`sequenceWorker_` 声明在页面**之后**（MainWindow.h:54-56 vs :60）→ 成员逆序析构时 worker 早于页面。**决策（用户选方案 C）**：观察者指针改 `QPointer`（对象析构自动置空），**不动退出时序**——`ShutdownWorker:229` 的 processEvents 是为防 `BlockingQueuedConnection`(`InMainThread`) 挂死刻意加的，改错会变成"退出挂死"（比崩溃更难查）；方案 D（去 deleteLater / 重排析构）据此否决。**实施**：4 处声明 `SequenceWorker*` → `QPointer<SequenceWorker>` + 4 个头加 `#include <QPointer>`（ManualControlPage.h 另需 include 完整定义——其内联 setter 赋值需完整类型）+ 补 `MainWindow.cpp:166` lambda 判空（唯一无判空访问点）。**关键便利**：Qt 6.11.1 的 QPointer 保留隐式 `operator T*()`（`qpointer.h:83`）→ 22 处 connect sender/receiver、全部 `->`/传参/三目/赋值**原样可用，0 处需 `.data()`**；成员声明只需前向声明，无需改 include 为完整定义。已核实无裸指针临时副本、无容器存储、无 invokeMethod（`ProcessPage.cpp:875/922` lambda 虽无判空但 sender 即 m_worker、析构自动断连，安全）。**规则沉淀**：`doc/architecture.md` §5 关键不变量新增「跨线程/长生命周期 QObject 的观察者指针一律用 QPointer，禁用裸指针长期持有」。**教训**：Qt 对象若由 `deleteLater`/父对象销毁而观察者持裸指针，必须用 QPointer——判空只防 null 不防悬垂；且"已 post 的跨线程 Queued 信号"在 sender 析构后仍会派发，不要假设断连/析构能回撤 |
| TR-085 | 点动停止语义评审答疑：点动停止**本就是减速停**（非缺陷，零代码改动，2026-09-10 评审）| 用户评审问"自动暂停是减速停止，点动停止有必要也做成减速停么"——隐含前提「自动=减速/点动=立即」与代码不符 | ① 卡轴点动松键 `BoPaiCard::StopJog`（`BoPaiCard.cpp:462`）与自动暂停 `StopAxis`（:334）**同一指令同一实参** `MC_Stop(mask,0)` 减速斜坡（`StopJog` 区别仅在 HardwareManager 层"仅停当前点动轴"门禁，`HardwareManager.cpp:541`）；② 舵机两者同为 `XRServo::Stop()` CMD24 立即停+锁力（协议 cmd 24 无减速档，梯形加减速已主动关闭）| 🟢 查证定性非缺陷、零代码改动（纯文档入档，无需编译）| 真正可讨论的是**滑行距离**：`jogSpeed == maxSpeed`（J1/Z/J2/R 四轴相等，config.json）→ 点动全速松键滑行与自动全速停相当（J1 高速约 9°）。三方向：① **降点动速度**（UI 速度框，零成本，先试）；② `MC_SetStopDec` 调陡停止减速（SDK 有接口未接线 `MultiCardCPP.h:716`、**按轴全局**与自动流程共用 → 须跨页互斥防并发改档 + 真机验证不丢步）；③ 断脉冲 = **红线**（开环丢步，仅 EmergencyStop 允许）。**决策：维持现状**，有实测痛点再走 ②（排跨页互斥 TR-066 之后）。**教训**：评审问题先查证隐含前提——答案有时是"已经是"而非"需要做"；同源代码不等于同语义（`StopJog` 与 `StopAxis` 卡端行为一致但门禁不同），结论必须落到指令级证据 |
| TR-086 | 用例 10.12「Paused+Step 点启动/继续放行下一步」场景不可构造（用例失效，2026-09-10 真机阶段 10 发现，零代码改动）| 用户执行 10.12 时对期望文本「提示『继续运行…』并放行下一步（resume requested + stepGo）；『⏸ 暂停』在该态灰」**看不懂具体操作步骤**；实测"单步暂停切自动可以执行启动/继续"但无"放行下一步"现象 | 场景不可达（三重阻断，见下）；真实行为以阶段 8 的 8.15「五钮全灰」为准；单步放行唯一入口 = 工艺页「单步执行」`NextStep()` | ⚪ 用例失效、非缺陷、零代码改动（仅文档标注与溯源入档；无需编译）| **不可达链条**：① `Paused(Step)` 唯一产生入口 = 工艺流程页「单步执行」（全项目唯一 `SetStepMode(true)` 调用点 `ProcessPage.cpp:1077`）→ 自动模式下工艺页**置灰不可达**（导航白名单 `{0 自动运行, 3 视觉检测}`，`MainWindow.cpp:32-33`、生效 `:705-708`）→ 自动模式产生不了 Paused(Step)；② 手动模式下自动页 `gate=false` → 五钮全灰（`AutoRunPage.cpp:566`、`:575-582`），启动钮点不了；③ 带单步会话切自动 → `MainWindow.cpp:657-667` 强制 `AbortStepSession()`（Stop + ResetStepSession）→ 残留被清理成 Idle。⇒「Paused(Step)」与「可点的启动/继续」**永不同时存在**。**用户实测现象的真解释**：切自动后残留已变 Idle，启动钮按 `st==Idle ? homed` 变亮（`AutoRunPage.cpp:585`），点它走「空闲→启动新流程」分支（`:365-385`）→ **从方案第一个动作重新跑**（与"停止/清报警后废弃上下文"同语义），**不是放行挂起的下一步**。**竞态假象**：切自动瞬间 <20ms 窗口内若点到，`Resume()` 会置 `stepGo=true` 并显示"继续运行…"，但因 `AbortStepSession` 已置 `cancel=true`，`WaitForStep`（`SequenceWorker.cpp:98`）返回 false → 下一步不会真执行，不可稳定复现。**溯源**：本用例照抄自 TR-073 设计稿按钮矩阵（`doc/auto_run_button_refactor.md:133`、`:141`，前提 `gate=true` 即按钮与手动/自动模式无关）；而 TR-075 互锁与 TR-073/074 **同在提交 `2be4083` 落地**，测试文档 `50d37cc` 写于其后 → **从写下之日起即不可执行**。**决策**：标注不可达、不改代码（`Resume`/`Pause`/白名单逻辑均不动）。**教训**：用例必须随设计变更**同步复审**；照抄设计矩阵/接口表时须逐条核对其**前提假设**（此处是"gate 与模式是否绑定"）在落地后是否仍成立——设计稿与互锁同批落地时，测试文档最容易遗漏前提失效 |
| TR-087 | 急停恢复指导被「舵机已重连」提示覆盖（缺陷，2026-09-10 真机阶段 10 用例 10.14 发现并修复）| 急停 → 切手动：功能全通过（Fault 放行、断使能、急停锁存、导航全开、可点动脱困），但手动页显示黄字 **「舵机已重连（扭矩已释放），请重新执行全局轴使能」** 而非红色急停恢复指导 | ① 急停 → 切手动 → 观察约 1-2s：手动页应**保持红色急停指导**不被黄字覆盖（舵机状态点刷新正常）；② 急停恢复全链路 10.15-10.17 顺带确认 | 🟡 已修复待真机复测（Debug+Release 双编译 EXITCODE=0 + Sim 冒烟 SMOKE_INIT_COMPLETE=True；**Sim 覆盖不到舵机离线边沿**——SimServo 无遥测失败语义）| **根因**：手动页 `SetHint`（`ManualControlPage.cpp:928-936`）**无优先级/防覆盖机制**，后写必覆盖先写。覆盖链：急停 `TorqueOff` → 舵机短暂离线（**硬件侧**，代码不置 offline——遥测 `online` 只认 `QueryMonitor` 成败 `XRServo.cpp:500-527`，疑 EMI/总线抢占总线，TR-028 同族，需现场日志佐证）→ 约 1s 后离线降频周期（`pollDiv=20`，`HardwareManager.cpp:1217-1218`）内 Ping/重连恢复 → `servoStateUpdated` → `OnServoStateUpdated` 的 **offline→online 边沿**（:868-872）且 `anyServoDisabled` 恰好满足（急停已把舵机轴置未使能）→ 黄字覆盖红字。**危害**：重连提示语义降级——只指"重新使能"，丢「一键回零→回安全位」两步与急停锁存状态，操作员会漏步骤。**修复（守卫抑制）**：`OnServoStateUpdated` 两个边沿提示分支加 `IsEStopPending()` 守卫——急停锁存期间不显示重连/通信异常提示（急停文案已含"所有轴已断使能"语义；状态点刷新与 `servoAllOnlinePrev_` 更新保留）。**附带澄清**：急停文案后半句「回零完成即解除急停锁定…」在 `ManualControlPage.cpp:178-179` **被用户有意注释**（嫌长），10.14 期望文本同步缩短与代码现状一致。**同类先例对比**：同函数内覆盖（急停 lambda 先 `ResetAxisStates` 再 `SetHint` 防 `RefreshSoftLimitHint`，09-07）可**改调用顺序**解决；本例是**跨事件、跨时间**（急停后 ≥1s）的覆盖，顺序手段不适用 → 需状态守卫（本例）或提示优先级机制（影响 40+ 调用点，否决）。**遗留排查项（已定位，2026-09-10 用户确认）**：舵机短暂离线的根因是 **COM 转 USB 转换器硬件不稳定**（廉价转换器，连接不稳定）——非软件问题，无需软件侧抑制离线判定；**后续更换稳定型号的转换器后再实验**，届时离线/重连频率应显著下降。在此之前，舵机离线类现象（TR-028 热重连、TR-087 本例）**优先怀疑转换器硬件而非软件**。**教训**：单条覆盖式提示条 + 多信号源 = 必然的提示竞争；安全关键指导（红字）与非安全提示（黄字）共占一条提示条时，必须有状态守卫或优先级，否则低优先级事件总会吞掉高优先级指导 |
| TR-088 | 10.16 绿字③被「回零完成」覆盖 + 10.17 安全位完成态误导（2026-09-10 真机阶段 10 发现并修复）| ① 10.16：急停恢复回零完成后手动页显示「回零完成」而非绿字③「急停锁定已解除…」；② 10.17：安全位走位完成后自动页显示「✅ 完成/方案执行完成」（安全位不在方案列表，误导操作员以为跑完生产方案），且用例期望「⏸ 待机」无实现依据 | ① 急停恢复回零完成后：手动页绿字③**保持显示**不被覆盖；② 安全位会话完成：自动页显示 **「✅ 已回安全位」+ 提示「已返回安全位，可启动方案」**，普通方案仍「✅ 完成/方案执行完成」 | 🟡 已修复待真机复测（Debug+Release 双编译 EXITCODE=0 + Sim 冒烟 SMOKE_INIT_COMPLETE=True；10.16 绿字③需真机急停链路复现）| **根因①（10.16）**：最后一轴完成时同线程顺序 = `MarkAxisHomed` 内 `emit estopCleared()`（绿字③）→ 返回后 `emit axisMoveFinished`（卡轴 `:1153`/舵机 `:1261`）→ `OnAxisMoveFinished:797` 写「回零完成」覆盖。**必然发生**（非环境偶发）；且 `OnAxisMoveFinished` 到达时 `estopPending_` 已清，无法用 `IsEStopPending()` 区分 → 需页面侧锁存。**修复①**：`ManualControlPage` 加 `estopClearPending_` 锁存——`estopCleared` 置位+立即写绿字③（双保险：`axisBusyNotified_` 去重致 axisMoveFinished 不来时绿字③也已显示）；`OnAxisMoveFinished` 全清时按锁存分流（置位 → 重写绿字③并清锁存）；`OnGlobalHome` 新轮次清锁存（防跨轮误显）。**根因②（10.17）**：两层——(a)「⏸ 待机」仅是 `m_statusLabel` **构造初值**（`AutoRunPage.cpp:143`），全项目无 Idle 写入路径（`UpdateStatusByState` Idle 分支故意不覆盖终态 `:623`）→ 用例期望笔误；(b) 安全位临时方案不在 `ProcessManager::schemes()` 列表，`OnSchemeFinished` 统一显示「✅ 完成/方案执行完成」→ 误导。**修复②**：`OnActionStarted` 时记录 `m_safePosSessionActive = IsSafePosSession()`（此刻会话仍在；`OnSchemeFinished` 被 `StartExecution:402` 先清 `safeSession` 后才派发，**不能届时直查**——同 TR-083 的"清理代码在执行循环之后"模式），完成槽按标志分流。**架构决策（用户经两轮追问后确认方案 A）**：10.16 备选 B=硬件层调 `MarkAxisHomed` 内 emit 顺序——**长远否决**：① UI 正确性将依赖 `MarkAxisHomed` 内部 emit **相对顺序**这一隐式契约，无编译保护、分散在卡轴/舵机两处并随轴类型线性增长、重构易碎；② B 治不了 TR-087 类跨时间竞争（两套心智模型）；③ 方案 A 消费抽象信号与驱动型号解耦（新卡/新舵机零改动，扩展性已专项核实）。**教训**：**UI 正确性不应依赖生产侧 emit 相对顺序——隐式契约无编译保护、随实现路径数量线性增长且重构易碎；因果关系应编码在消费侧状态或显式信号语义中**；另"跨线程排队后才能处理的判断"不能直查实时状态，须在事件源头（OnActionStarted）先记录快照（与 TR-083"清理代码在执行循环之后"同族：信号的可见性时序要先想清楚） |
| TR-089 | 阶段 10 真机验证汇总（TR-075 UI 权限与全局互锁 + 急停恢复全链路，用例 10.1–10.20，2026-09-10）| — | 10.1–10.20 全 🟢（10.12 场景不可构造 ⚪，见 TR-086）；**急停恢复闭环 = 使能→回零→手动回安全位→切自动，全链路验证通过**（10.13→10.17）；TR-087 守卫复测通过（10.14 复测 OK）；TR-088 两项显示修复复测通过（10.16/10.17 复测 OK）| 🟢 已通过（真机 2026-09-10 收口）| **汇总条目**：过程缺陷/澄清交叉引用 TR-081（急停恢复链改手动回安全位）、TR-083（一键回零终止残留会话，10.10/10.11 复测载体）、TR-086（10.12 用例失效溯源）、TR-087（10.14 提示覆盖修复）、TR-088（10.16/10.17 显示修复）；硬件环境：舵机离线根因=COM 转换器不稳定待换型（TR-087 备注）。**阶段 10 完成 ⇒ phase3 三阶段（8/9/10）真机验证全部收口**。**下一批次**：循环生产（`doc/auto_run_loop_production.md`，用例**追加进 phase3 编号体系** = 8.18–8.26 / 10.21–10.26（phase3 现最大 8.17 / 10.20，**无撞号**）；2026-09-14 更正：原记「独立编号 10.1–10.26，与 phase3 的 10.x 撞号」与实际编号不符。其 10.21–10.23 为循环运行场景的互锁/急停恢复链）|
| TR-090 | 100% 速度下暂停/停止滑行角度大（用户需求，2026-09-10；TR-085 预留手段落地）| 工艺动作速度 100% 时，自动运行「⏸ 暂停」/「■ 停止」减速停滑行大（J1 高速约 9°），影响节拍与安全余量 | ① 设备配置页给轴填「停止减速度」`stopDecSmooth`（0.1~2 系数）；② 跑 100% 方案暂停观察滑行（应随系数增大而缩短）；③ 逐步 1.0→1.5→2.0 标定，出现丢步迹象（停稳后位置漂移/后续定位偏移）即回退一档；④ 启动日志 `[BoPaiCard] SetStopDec ... cardSmoothPrev=0.5 cardAbrupt=1` 反读验证语义；⑤ 填 0 随时回退卡默认 | 🟡 代码完成待真机标定（Debug+Release 双编译 EXITCODE=0 + Sim 冒烟 SMOKE_INIT_COMPLETE=True；Sim 无滑行模型，标定必须真机）| **需求响应**：TR-085 预留的 `MC_SetStopDec` 手段，用户提供《ETH_GAS_N 系列用户手册 V2.9》（`D:\workspace\博派\`）确认语义——`MC_SetStopDec(nAxisNum[单轴,1基], decSmoothStop[0.1~2,默认0.5], decAbruptStop[1~20,默认1])`，参数为**停止减速度系数**（0.1~2 的取值范围排除脉冲单位假设，**不做 AccelToPulse 换算直接透传**）；`MC_Stop(mask,0)` 走 smooth 档、`0xFFFFFFFF` 走 abrupt 档（推断）。**实施**：`IMotionCard::SetStopDec` 纯虚 + `BoPaiCard` 实现（`MC_GetStopDec` 读卡端 abrupt 保留 + `MC_SetStopDec` 下发 + 按 `stopDecSmoothApplied` 去重）+ `SimCard` no-op + `AxisConfig` 加 `stopDecSmooth/Applied` + config 每轴 `stopDecSmooth`（默认 0=不下发保持卡默认，安全回退）+ ConfigPage「停止减速度」输入框（0~2 step0.1，无量纲不随轴类型换单位）+ `MoveAbs/MoveJog` 前实时读 config 去重下发（config 改动免重启）。**无并发改档问题**：系数是持久设置，非运行时切换（TR-085 当时的顾虑随"全局统一调陡"诉求消失）。**作用路径**：⏸暂停/■停止/点动松键/回零清理（全部 `MC_Stop(mask,0)`）；**不受影响**：Trap/Jog 运动减速（走 TrapPrm/JogPrm 的 dec）、舵机（CMD24 立即停）、急停突停档（读卡端保留）。**风险与回退**：减速过陡开环丢步——标定从小到大；填 0 还原卡默认。**教训**：SDK 头文件无注释时，厂商手册是语义的唯一权威——本例若按"脉冲单位"假设复用 AccelToPulse 换算，0.1~2 的合法值域会被换算成极小值导致功能静默失效；用户提供手册后应第一时间提取参数取值范围反推单位。**首轮标定（2026-09-10 用户配置）**：`Axis_J1`/`Axis_Z` `stopDecSmooth=1.0` 已写入 config（其余轴暂不设）；复测项：100% 速度下 ⏸/■ 停距对比（原 J1 约 9° 应约减半）+ 丢步观察，不满意再调 1.5/2.0 或回退 |
| TR-091 | 循环生产 Loop Production（用户需求，2026-09-14；方案 `doc/auto_run_loop_production.md` D1–D19 全定稿后实施）| 自动运行页新增「循环」控件行（模式 单轮/指定次数/无限循环 + 次数 1~9999 + 「回安全位」复选框 + 进度标签），引擎在**会话内**加循环外壳：每轮之间按需内联回安全位、每轮发 `cycleChanged`，`schemeFinished` 仍只在全部循环完成时发一次 | ① 单轮 = 与历史逐字节一致（不碰循环控件直接启动，等价 8.18）；② N 次：日志 `=== 循环 i/N 开始/完成 ===` + 标签 `循环 i/N` + 「✅ 完成」只出现一次；③ 无限循环：启动前二次确认（急停在手/工作区无人员肢体/软限位已核对），标签 `循环 i/∞`，**不发 schemeFinished**；④ 勾「回安全位」且非单轮但安全位未启用 → 拒绝启动且无运动；⑤ 循环间隔回安全位**严格先抬 Z 到位再做水平移动**；⑥ 重试口子 `production.loop.retryCount` 默认 0 = 今天行为，>0 时仅 Vision/Move 可重试且**重试期间不报故障** | 🟢 真机验证通过（2026-09-15，台账 TR-094；8.18–8.26 / 10.21–10.23、10.26 🟢，10.24/10.25 ⚪ 用例不可达，详见 `doc/test/real_machine_plan_phase3.md` 第四批）| **实施范围（S1–S4/S8，全在 `SequenceWorker.*` / `AutoRunPage.*` / `main.cpp` 三处，`MainWindow`/`ProcessPage`/`ManualControlPage`/`HardwareManager` 零改动）**：① S1 头文件增量（`LoopConfig` 值语义 + `cycleChanged(int,int)` 信号 + `RunSequence(scheme, loop = LoopConfig{})` **默认参数** → 既有 3 个调用点零改动、行为零变化）；② S2 循环外壳——`ExecuteActions()` 改为循环壳、原主体**原样搬迁**为 `ExecuteOnce()`（仅动作调用点一处改为 `ExecuteActionWithRetry`），`RunSafePos` 的安全位构造抽为 `BuildSafePosActionFrom()`（循环间隔 `ReturnToSafePosInline()` 与手动回安全位**共用同一构造与执行链**，故 8.20 的核验对 9.6 同样成立）；③ S2b `errorOccurred` 单点化（D17 方案 A）；④ S2c 「严格先抬 Z」加固（D19）；⑤ S3 UI 控件行 + 两项门禁 + `!busy` 置灰；⑥ S4 持久化 `production.loop.*`；⑦ S8 日志保留 7 天。**命名偏差（以本条为准）**：`Impl` 成员命名为 **`loopCfg`** 而非方案文档写的 `loop`——`Impl::WaitForCancelOrTime`/`WaitForStep` 内已有局部 `QEventLoop loop`，同名触发 C4458（声明隐藏类成员），故改己方命名而不动既有等待循环。**D17 落地**：`ExecuteAction` 子树 8 处 `emit errorOccurred` 全部改为 `impl_->lastError = ...`，emit 收敛到 **2 处会话级出口**（`ExecuteOnce` 失败分支 / `StartSingleExecution` 兜底），两处均用 `lastError.isEmpty() ? action.name : lastError`；顺带修掉既有瑕疵——原先失败会发两条（内部具体原因 + 外层 `action.name`）致清报警提示丢失具体原因。**D19 落地**：新增匿名 namespace 函数 `DispatchPointMove()` 统一 4 轴下发（先舵机 J2/R 再卡轴 J1/Z），**目标与读回当前值之差 ≤ 0.01 的轴跳过下发**并写 `SPDLOG_INFO skip axis X (in position)`；跳过前仍过 `IsWithinSoftLimits`（不旁路软限位硬拦截，越界照走 `MoveAbs` 完整拒绝路径）；`reissue` 复用同函数 → 恢复瞬间不把已在位轴重下发。正面副作用：每点 4 次 `InMainThread` 合为 1 次；同高度平移不再白等 200ms/轴。**S8 实测生效**：`main.cpp` 启动时 `PurgeOldLogs(logDir, 7)`（只匹配 `^creampuff_YYYY-MM-DD\.log$`，早于「今天 − 7 天」才删）——本次冒烟即删除 29 个过期日期文件（cutoff 2026-09-07），`crash.txt` 与 7 天内文件未动，日志留痕 `[Main] Log retention: removed 29 file(s) older than 7 days`。**刻意不做**：常驻提示条（D13，单槽提示必被覆盖）、循环期间一键回零门禁（D14，物理不可达）、重试次数进 UI、断点续产、完成后回安全位（D8）。**待真机核验重点**：8.20 点1 日志应出现 `skip axis J1/J2/R (in position)` → 证明抬 Z 阶段零水平运动由代码保证。**真机验证（2026-09-15，TR-094）**：8.18–8.26 / 10.21–10.23、10.26 全部通过（含 8.20 ★核心「先抬 Z」核验、8.21 停止终止循环、8.22 暂停续走计数不丢、10.23 循环中急停后恢复链 使能→回零→手动回安全位→切自动 仍成立、10.26 单次和多次循环完成后切手动正常放行）；**10.24/10.25 ⚪ 用例不可达**——自动模式导航白名单 `{自动运行, 视觉检测}` → 工艺流程页/设备配置页置灰，切手动被模式互锁拒绝 → 操作路径物理不存在（同 10.12/TR-086 根因族、与 D14 同源；用户裁决标 ⚪ 不单开 TR）。实测记录未逐条留存日志串（用户口头确认通过，phase3 实测记录块已如实注明，未补写细节）|
| TR-092 | 自动运行页底部提示迁至顶栏「全局消息横幅」（用户需求，2026-09-15；10 寸触控屏竖向空间优化）| `AutoRunPage` 底部 `m_hintLabel` 常占一行竖向空间，10 寸触控屏上不可接受；需求：移除底部提示，改挂顶栏【手动/自动】滑块左侧，超长显示 `...`，点击弹出完整消息（**明确要求弹窗，不是鼠标悬停 tooltip**）| ① 自动运行页底部不再有提示行，右侧栏下方留白回收；② 顶栏标题与【手动/自动】之间出现横幅，初始文案「系统就绪」；③ 触发任一提示（未使能启动/无方案/未回零/急停/执行出错）→ 横幅文字与颜色随 `SetHint` 变化；④ 长文案（急停指导约 60 字）→ 显示为 `...` 截断且**顶栏其余控件不被挤压**；⑤ 点击横幅 → 弹出非模态「消息详情」框，全文完整且可同时操作主界面；⑥ 窗口从 1480 拖到最小 1200 → 截断长度随之重算、无裁切 | 🟡 代码完成待真机目视（Debug+Release 双编译 EXITCODE=0 + Sim 冒烟 SMOKE_INIT_COMPLETE=True；UI 视觉与点击交互须真机/桌面目视确认）| **范围（用户 2026-09-15 裁定）**：**手动控制页提示原地不动**，横幅**只接自动运行页**——单一信号源，规避 TR-087「单条覆盖式提示 + 多信号源 = 必然竞争」的放大。**实施**：① `AutoRunPage.h` 新增 `signals: requestGlobalHint(text,color)`、删 `m_hintLabel` 成员；② `AutoRunPage.cpp` 删底部标签三行、`SetHint` 改为 `emit requestGlobalHint`（30 余处调用点零改动）；③ `MainWindow.h` 前向声明 `HintBanner` + 成员 `globalHint_` + 槽 `UpdateGlobalHint`；④ `MainWindow.cpp` 定义 `HintBanner : QLabel`、`CreateTopBar` 中 `leftSection->addStretch()` 之后插入、`SetupUI` 建页后接线。**三个易错点（实施前已识别，勿回退）**：① 横幅必须提为 MainWindow 成员——`CreateTopBar()` 在 `SetupUI` 中**先于子页面创建**被调用，局部变量无法在接线处引用；② 宽度用 `setFixedWidth(320)`——除限上界外，还经 `qSmartMinSize` 的 `boundedTo(maxSize)` 把 QLabel 的最小宽度一并夹住，否则长文案的 `minimumSizeHint` 会把顶栏顶爆（顶栏余量核算：最小窗口 1200 时左区约 602px、标题占约 192px）；③ 点击弹窗用**非模态** `QMessageBox`（`setModal(false)+show()+WA_DeleteOnClose`）——静态 `QMessageBox::information()` 内部是 `exec()`，运行中会阻塞主线程事件循环，遥测刷新与日志追加全部停摆（worker 在独立线程故运动不停，但界面表现为卡死）。**用 QLabel 派生而非 QPushButton**：全局 QSS 的 `QPushButton` 规则带 `padding:10px 18px / font-size:15px / radius:10px`，实例样式漏项即成大按钮。**`elidedText` 必须双点重算**（`SetMessage` + `resizeEvent`），否则窗口拉伸后截断长度停留在上一次。**初始文案**由 `CreateTopBar` 给出（构造期信号在 `connect` 之前 emit 会被静默丢弃）。**未做**：横幅优先级/防覆盖（单源下不需要，见 TR-087 备注）；`ManualControlPage` 提示未动。|
| TR-093 | 硬件未连接时「⟳ 初始化」误报成功（缺陷，2026-09-15 用户真机发现并修复）| 运动卡与舵机**均未连接**状态下，切到【自动】点「⟳ 初始化」：日志显示「初始化完成」，提示「硬件已连接。请到【手动控制】页「全部使能」→「一键回零」后方可启动」——实际硬件全未连接，初始化应失败 | ① 硬件断开（卡 MC_Open 失败 + COM 口不存在）→ 点「⟳ 初始化」：横幅红字「初始化失败：运动卡: 未连接 | 舵机: 未连接…」，日志框追加同名行，**不再出现**「初始化完成 / 硬件已连接」；② 接好硬件后**再点一次**「⟳ 初始化」：转为「硬件已连接。请到【手动控制】页…」，**无需重启程序**；③ Sim 模式启动 → 提示照旧为「硬件已连接…」（仿真恒就绪，无回归）；④ 首次启动（MainWindow 构造）忽略返回值，启动行为不变 | 🟡 代码完成待真机目视（Debug+Release 双编译 EXITCODE=0 + Sim 冒烟 SMOKE_INIT_COMPLETE=True + 真实配置未连接场景日志复现；UI 提示文案与重试链路须真机点击目视——项目内无 GUI 自动化脚本）| **根因**：`HardwareManager::Initialize()` 末段**无条件 `return true`**，连接失败仅 `SPDLOG_WARN`（卡 `MC_Open failed` / 舵机 `Open port COMx failed`），返回值不携带就绪信息 → `AutoRunPage::OnInitClicked` 的 `if (!hw.Initialize())` 失败分支为**死代码**。**连带缺陷**：`initialized_` 是一次性闩锁（`if (initialized_) return true;`），第二次及以后调用**直接返回、不重连**——与按钮 tooltip 承诺的「重新加载配置并连接硬件」不符；且运动卡**全项目无自动重连路径**（`motionCard_->Connect` 仅在 Initialize 内调用），失败后只能重启程序。**修复**：① 抽出私有 `ConnectMotionCard()`（含 `IsConnected()` 守卫——`BoPaiCard::Connect` 内 `MC_Open` 不先 Close，重复调用会重复开卡），首跑与重试共用；② 末段改为 `return IsMotionCardConnected() && IsServoConnected();`，**`Initialize complete` 日志行保持无条件打印**（Sim 冒烟判据依赖它）；③ 重入分支：不重建对象、不重启轮询，仅对未就绪项补连接（卡 `ConnectMotionCard()`、舵机 `ReconnectServos()`），随后同步边沿缓存并 `emit connectionChanged()`（避免 PollTick 边沿检测重复广播）；④ `OnInitClicked` 失败分支补 `ConnectionStatus()` 明细 + 日志框一行 + `UpdateControlsEnabled()`。**舵机重连为何能复用 `ReconnectServos()`**：它内部先 `Disconnect()` 再 `Connect()`——`XRServo::Connect` 对已打开串口是「复用共享句柄 + refs+1」，不先断开则拔线后的失效句柄永远关不掉、refs 只增不减，重连必失败；其批量 `axisMoveFinished` emit 在 ManualControlPage 侧有 `homingAxes_[axis]` 守卫（`ManualControlPage.cpp:793`），初始化期全为 false，不产生误导文案。**验证**：真实配置（Bopai + XRServo，COM3 不存在）启动复现日志 `MotionCard 'Bopai' connect FAILED: MC_Open failed, code=-6` + `Servo J2/R connect FAILED: 打开串口失败: COM3` + 紧随 `Initialize complete`（正是误报来源），进程存活无崩溃。**教训**：`bool Initialize()` 这种「流程型返回值」极易被写成恒真——一旦下游用它做门禁，失败分支就是死代码；「返回值语义」必须在函数注释与调用点同时写死。**未做**：UI 端到端目视（见验证列）|
| TR-094 | 循环生产真机验证（TR-091 功能验证，2026-09-15；用例 = `doc/test/real_machine_plan_phase3.md` 第四批 8.18–8.26 / 10.21–10.26，方案 `doc/auto_run_loop_production.md`）| 在真机上验证 TR-091 循环生产功能：单轮零回归、N 次/无限循环计数与终态、循环间隔回安全位严格先抬 Z（8.20）、循环中停止/暂停/急停语义、未启用安全位拒绝启动、失败单点化提示、循环完成后切手动放行 | ① 8.18 单轮与 8.17 一致（无 `=== 循环` 日志）；② 8.19/8.26 计数标签与「✅ 完成」单次；③ 8.20 间隔回安全位点1 只下发 Z（`skip axis J1/J2/R (in position)`）；④ 8.21 停止终止整个循环且使能保持；⑤ 8.22 暂停续走计数不丢；⑥ 8.23 IK 失败不进第 2 轮 + 提示具体原因（D17）；⑦ 8.24 循环控件 `!busy` 置灰；⑧ 8.25 未启用安全位拒绝启动；⑨ 10.21 模式互锁弹窗回弹；⑩ 10.22 视觉页白名单可达；⑪ 10.23 急停后恢复链 使能→回零→手动回安全位→切自动 仍成立；⑫ 10.26 完成后切手动放行 | 🟢 已通过（真机验证，2026-09-15，详见 `doc/test/real_machine_plan_phase3.md` 第四批；10.24/10.25 ⚪ 用例不可达）| **结果**：8.18–8.26 / 10.21–10.23、10.26 全部 🟢（用户口头确认通过；**实测记录未逐条留存日志串/数值**，phase3 实测记录块已如实注明，未补写细节；如需取证查 `log/creampuff_2026-09-15.log`）。**10.24/10.25 ⚪ 用例不可达**：循环必在自动模式启动，自动模式导航白名单 `{自动运行, 视觉检测}`（`MainWindow.cpp:32-33`）→ 工艺流程页/设备配置页置灰；切【手动】被模式互锁弹窗拒绝（10.6）→ 两用例的操作路径物理不存在（同 10.12/TR-086 根因族、与 D14「循环期间一键回零不可达」同源；用户 2026-09-15 裁决标 ⚪，不单开 TR；代码门禁 `rejected: already running` / `ReloadFromConfig skipped: scheme running` 兜底逻辑保留）。10.26 附加确认：单次和多次循环后均正常完成。**至此 phase3 全部批次（8/9/10 + 循环生产第四批）收口** |
| TR-095 | 循环生产 15 轮后 J1 目视偏移（精度缺陷，2026-09-15 用户发现；机理分析完成，待真机判别——复测用例 8.27–8.30）| 循环生产运行（2026-09-15 14:10:50–14:19:26，约 15 轮）结束后回到方案 1 初始位置：**J2 与末端夹爪实际位置较第一轮向 Y+ 目视偏移；Z 无偏移；目测 J1（大臂根部）逆时针偏移**，J2 组件随 J1 基座偏移 | ① 8.27 回零重建基准后目视恢复 → 证明运行中累积而非装配基准错；② 8.28 方案降速 100%→50% 复跑 15 轮无偏移 → 高速丢步实锤；③ 8.29 臂尖标记逐轮记录偏移：线性累积→丢步、单轮突变→机械打滑；④ 8.30（可选）J1 maxAccel 下调佐证 | 🟡 机理分析完成待真机判别（**结论：最高概率 = J1 开环步进高速丢步，次级候选 = J1 输出端机械打滑；软件/舵机/正逆解已排除**）| **证据链（`log/creampuff_2026-09-15.log` 6267–8490 行，15 轮逐轮核对）**：① **软件指令层 15 轮恒定一字不差**——J1 卡轴脉冲 `-725333/-514155/-513280/-197069` 各精确出现 15 次、Z `-820224/-615424/...` 各 15 次恒定、J2 舵机 28.0/62.9°（=逻辑 0/34.9 + homeOffset 28）、R 179.9/180.1° 恒定 → **排除正逆解与软件累积**；② **J2/R 舵机闭环自报角度首尾一致**（J2 稳态 28.1°、63.0~63.3°；R ±179.9 为回绕同位表示）→ 舵机/其固件加减速无漂移，**排除舵机丢步**；③ `SendSetAngle` 220 次零失败 → **排除指令丢包**（时段内虽有 7 次遥测超时 + 14:12:21 `Servo offline but Ping ok`，系 TR-087 已知 COM 转换器问题，未造成指令丢失）；④ J2 读回 ±0.2° 浮动为位置环稳态波动、无单向趋势；⑤ Z 同为卡轴却不漂移——负载形态差异（垂直沿导轨 vs 水平大惯量回转）指向 J1 特异性。**指向 J1 的机理**：开环步进 + 100:1 减速（`gearRatio 0.01`），输出 30°/s = 电机 **500 rpm**（步进失步高发区）；生产方案 `speedPercent=100`（`config/process.json:47`）——**8.x 验证全用 50%**；每轮 102° 大行程频繁加减速、循环单向载荷 → 同向丢步逐轮累积；开环读回=指令计数 → 丢步后软限位/安全位/日志**全无感知**（解释零报警）。TR-085 已实测 J1 高速停止滑行 9°，速度/力矩余量本在边界。**已排除**：正逆解（指令恒定）、J2 松动（不解释 J1 偏移，用户自排）、舵机谐波（闭环读回恒定）、速度加减速"匹配"（软件参数恒定且舵机为固件闭环）。**次要发现（非根因）**：R 工作点 179.9° 压在 ±180° 回绕奇点（`AlignNear` 已对齐，XRServo.cpp:427，日志无 7.2s 卡顿复现）；`stopDecSmooth`（TR-090）只管 `MC_Stop` 停止档，正常运动加减速走 config `maxAccel`（J1 现 200）。**判别与缓解**：见 8.27–8.30；短期方案降速 ≤70% + 长循环前回零重建基准；长期若速度不可让则 J1 换闭环步进/加编码器 |
| TR-096 | 部署与日志路径（`src/main.cpp` / `build_release.bat`，2026-09-16 用户提问驱动）| ① Release 版整目录拷到**源码树外**的机器运行，日志写到哪、是否需要预建目录；② 改判定后开发机 Debug/Release 日志是否改道（Sim 冒烟与真机文档均以工程根 `log/` 为判据）；③ `crash.txt` 硬编码绝对路径在无 `D:` 盘机器上静默失败；④ `build_release.bat` 此前**只编译不打包**（AGENTS.md 声称 one-click packaging，实际 `out/dist/` 是 2026-08-04 手工产物，之后未更新）| ① 开发机 Debug/Release 仍写工程根 `log/creampuff_YYYY-MM-DD.log`（行为零变化，冒烟判定链不受影响）；② 源码树外运行时日志落 **exe 旁 `log/`**，与目标机有无 `D:` 盘无关；③ `crash.txt` 与符号搜索路径跟随日志目录，不再硬编码；④ `build_release.bat` build 成功后产出 `out\dist\CreamPuffRobot\`（80 文件）+ `CreamPuffRobot_x64_Release.zip`，文件集与 08-04 手工包**逐文件一致** | 🟢 已验证（Debug+Release 双编译通过；日志落点**实跑实测**；打包文件集静态比对一致。**未做**：在沙箱内执行 .bat 本身——cmd 类进程被策略拦截，打包段需用户实跑一次确认）| **根因**：原判定 `candidates = {PROJECT_SOURCE_DIR/log, exe旁/log, %APPDATA%/log}` 取「第一个 `mkpath` 成功者」，而 `PROJECT_SOURCE_DIR` 是**编译期**烧进 exe 的绝对路径 → 目标机只要有 `D:` 盘，`mkpath` 就会**凭空建出整条工程目录树**并把日志写进去（exe 旁反而不出现 log）；`crash.txt` 更是硬编码 ASCII `D:/workspace/projects/CreamPuffRobot/log/crash.txt`（实测 exe 内 ascii=1），无 `D:` 盘时静默写不出。**修复**：新增 `ResolveLogDir()`，判定改为「**exe 实际位置是否位于编译期源码树之下**」（比较 exe 目录与烧入字符串，**不是**判盘符）→ 是则工程根 `log/`，否则 exe 旁 `log/`，再兜底 `%APPDATA%/CreamPuffRobot/log`；叠 `CREAMPUFF_LOG_DIR` 环境变量覆盖作现场排障逃生门。崩溃转储与 `SymInitialize` 符号路径改为 `main()` 预填窄字符缓冲 `g_crashLogPath`/`g_symSearchPath`（崩溃上下文**不触碰 Qt**，避免二次崩溃），`main()` 完成前崩溃则退回相对路径 `crash.txt`。**刻意不做「exe 旁一律优先」**：工程根 `log/` 被 `out\smoke\sim_smoke.ps1:29`（`$root` 硬编码 `D:\workspace\projects\CreamPuffRobot` + 读 `$root\log\creampuff_YYYY-MM-DD.log` 判 `SMOKE_INIT_COMPLETE`）、`doc/compile_guide.md:103/116`、`doc/test/*.md` 硬依赖，改了会造成冒烟**假失败**。**实测证据**：① 源码树外 `C:/Users/11318/.workbuddy-ai/tmp/cpr_portable/`（按打包文件集拷 80 文件）跑 Release → exe 旁生成 `log/`，日志 `[Main] Log initialized: .../cpr_portable/log/creampuff.log (rule: next to exe)`，`Initialize complete` 命中；② 开发机 Debug → `D:/workspace/projects/CreamPuffRobot/log/creampuff.log (rule: source tree)`，`out\build\x64-Debug\log` **未**被误建；③ Sim 冒烟 `Initialize complete` 计数 1→2（增量 1，通过）；④ `build_release.bat` 打包段按字节抽出行静态验证：选中 80 文件，与 08-04 手工包 `comm` **双向零差异**，无误选构建产物（`CMakeCache.txt`/`CMakeFiles`/`vcpkg_installed`/`src`/`tests`/`.qt`/`*.pdb`/`*.lib`），脚本纯 ASCII、`goto` 目标齐备、打包段无多行括号块（LF 行尾安全）。**副产物（保留现状）**：Debug/Release 在本机仍**混写同一** `creampuff_YYYY-MM-DD.log`（daily sink 只按日期命名、无 build 标识）——拆分需动上述 4 处文档，收益不足。**验证副产品**：本次实跑触发 S8 保留策略，`log/creampuff_2026-09-08.log` 被按 7 天策略清理（cutoff 2026-09-09），属预期行为。**未做**：`build_release.bat` 全流程实跑（沙箱拦截 .bat，需用户双击一次；脚本内已加 exe 与 `platforms\qwindows.dll` 存在性校验、`SKIP_PACKAGE=1` 跳过开关、`TARGET` 非主程序自动跳过打包）|
